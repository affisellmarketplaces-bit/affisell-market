/** Buyer-facing ranked bestsellers hub (7-day network sales). */
export const BUYER_BESTSELLERS_PATH = "/marketplace/bestsellers" as const

/** Legacy alias kept for redirects and warm links. */
export const BUYER_BESTSELLERS_LEGACY_PATH = "/bestsellers" as const
