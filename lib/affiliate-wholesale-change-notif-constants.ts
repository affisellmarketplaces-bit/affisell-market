/** Inbox + email when supplier wholesale increases vs affiliate listing prices. */
export const SUPPLIER_PRICE_CHANGE_NOTIF = "SUPPLIER_PRICE_CHANGE" as const

/** `Notification.orderId` stores `AffiliateProduct.id` for this type (deep link). */
export const SUPPLIER_PRICE_CHANGE_LISTING_ID_FIELD = "orderId" as const
