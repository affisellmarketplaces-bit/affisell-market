/**
 * Disclosure constants for payout copy — mirrors runtime in order-payout-policy / transfer gating.
 * Copy-only module (no settlement side effects).
 */
export {
  AUTO_CONFIRM_DAYS_AFTER_DELIVERY,
  PAYOUT_DAYS_AFTER_DELIVERY_CONFIRM,
} from "@/lib/order-payout-policy"
