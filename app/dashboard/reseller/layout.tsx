export const dynamic = "force-dynamic"

/** Reseller subtree — FAB/shortcut provided by parent dashboard layout chrome. */
export default function ResellerDashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
