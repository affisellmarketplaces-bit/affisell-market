/** Legacy admin HOLD view — stubbed pending schema alignment. */

export default function AdminHoldsPage() {
  return (
    <main className="mx-auto max-w-3xl px-4 py-12">
      <h1 className="text-xl font-semibold tracking-tight">Admin holds</h1>
      <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
        This page is disabled until the escrow / hold schema is wired to the current marketplace models.
      </p>
    </main>
  )
}
