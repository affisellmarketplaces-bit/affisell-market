import RadarAppShell from "@/components/radar/radar-app-shell"

export default function RadarLayout({ children }: { children: React.ReactNode }) {
  return <RadarAppShell>{children}</RadarAppShell>
}
