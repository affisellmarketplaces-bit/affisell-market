# Affisell taxonomy extensions (idempotent upsert via scripts/taxonomy-upsert-extensions.mjs)
#
# Formats:
#   Parent Full Path > New Node Name     (parent must exist in Google FR taxonomy)
#   __ROOT__ > L1 Affisell-only name     (no googleId — catch-all / transversal)
#
# Used by the 26-branch commission grid where Google has no equivalent leaf.

Vêtements et accessoires > Bijoux > Joaillerie fine
__ROOT__ > Occasion et reconditionné
__ROOT__ > Autre / Non classé
