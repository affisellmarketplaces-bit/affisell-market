import { HeroSection } from "@/components/HeroSection"

/** Buyer home hero — premium audit mockup (no reseller/supplier CTAs). */
export async function BuyerHeroBlock() {
  return <HeroSection variant="buyer" />
}
