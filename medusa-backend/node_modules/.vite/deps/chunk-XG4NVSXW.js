import {
  Adjustments,
  ArrowDownMini,
  ArrowUpMini,
  CalendarMini,
  Check,
  CheckCircleMiniSolid,
  CheckCircleSolid,
  CheckMini,
  ChevronRightMini,
  CircleDottedLine,
  CircleHalfSolid,
  Clock,
  DescendingSorting,
  EllipseMiniSolid,
  EllipsisHorizontal,
  ExclamationCircleSolid,
  Eye,
  EyeSlash,
  Funnel,
  InformationCircleSolid,
  MagnifyingGlass,
  MagnifyingGlassMini,
  Minus,
  MinusMini,
  Plus,
  Spinner,
  SquareTwoStack,
  SquareTwoStackMini,
  TriangleLeftMini,
  TriangleRightMini,
  TrianglesMini,
  XCircleSolid,
  XMark,
  XMarkMini,
  require_lodash
} from "./chunk-UWYHFZGT.js";
import {
  $28aae683a942e141$export$87194bb378cc3ac2,
  $2aaf608024c21ca1$export$99faa760c7908e4f,
  $2aaf608024c21ca1$export$ca871e8dbb80966f,
  $2d54ee2ec11ddab8$export$cb95147730a423f5,
  $2eb8e6d23f3d0cb0$export$43bb16f9c6d9e3f7,
  $2eb8e6d23f3d0cb0$export$a54013f0d02a8f82,
  $6902a2bb26917a07$export$42df105a73306d51,
  $7541913c0284d84b$export$4c842f6a241dc825,
  $7541913c0284d84b$export$5591b0b878c1a989,
  $84102b64e5ca022f$export$dd0bbc9b26defe37,
  $8e7461aabf74661f$export$60e84778edff6d26,
  $a1efc3efb5426bf7$export$3ee915f8151bd4f1,
  $ac4318a9c075bb9f$export$ea18c227d4417cc3,
  $ad063034c8620db8$export$aa8b41735afcabd2,
  $ad063034c8620db8$export$ccc1b2479e7dd654,
  $b822555cf9fe955c$export$fd53cef0cc796101,
  $be61a22f9aa88c7c$export$136073280381448e,
  $d51706f903be7eab$export$1315d136e6f7581,
  $e,
  $e260d131964da0f9$export$872b660ac5a1ff98,
  $ea0ea6e62ae0d7ca$export$6d095e787d2b5e1f,
  CSS,
  CurrencyInput,
  DndContext,
  Highlight2,
  KeyboardSensor,
  PointerSensor,
  Prism,
  SortableContext,
  arrayMove,
  closestCenter,
  clsx_m_default,
  createColumnHelper,
  cva,
  dist_exports,
  dist_exports10 as dist_exports9,
  dist_exports11 as dist_exports10,
  dist_exports12 as dist_exports11,
  dist_exports13 as dist_exports12,
  dist_exports14 as dist_exports13,
  dist_exports15 as dist_exports14,
  dist_exports16 as dist_exports15,
  dist_exports3 as dist_exports2,
  dist_exports4 as dist_exports3,
  dist_exports5 as dist_exports4,
  dist_exports6 as dist_exports5,
  dist_exports7 as dist_exports6,
  dist_exports8 as dist_exports7,
  dist_exports9 as dist_exports8,
  flexRender,
  getCoreRowModel,
  horizontalListSortingStrategy,
  require_copy_to_clipboard,
  sortableKeyboardCoordinates,
  themes_exports,
  twMerge,
  ue,
  useReactTable,
  useSensor,
  useSensors,
  useSortable
} from "./chunk-B3BFY5QH.js";
import {
  require_client
} from "./chunk-5GLF3XJW.js";
import {
  require_react
} from "./chunk-QCHXOAYK.js";
import {
  __toESM
} from "./chunk-WOOG5QLI.js";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/utils/clx.js
function clx(...args) {
  return twMerge(clsx_m_default(...args));
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/icon-button/icon-button.js
var React = __toESM(require_react());
var iconButtonVariants = cva({
  base: clx("transition-fg inline-flex w-fit items-center justify-center overflow-hidden rounded-md outline-none", "disabled:bg-ui-bg-disabled disabled:shadow-buttons-neutral disabled:text-ui-fg-disabled "),
  variants: {
    variant: {
      primary: clx("shadow-buttons-neutral text-ui-fg-subtle bg-ui-button-neutral", "hover:bg-ui-button-neutral-hover", "active:bg-ui-button-neutral-pressed", "focus-visible:shadow-buttons-neutral-focus"),
      transparent: clx("text-ui-fg-subtle bg-ui-button-transparent", "hover:bg-ui-button-transparent-hover", "active:bg-ui-button-transparent-pressed", "focus-visible:shadow-buttons-neutral-focus focus-visible:bg-ui-bg-base", "disabled:!bg-transparent disabled:!shadow-none")
    },
    size: {
      "2xsmall": "h-5 w-5",
      xsmall: "h-6 w-6 p-1",
      small: "h-7 w-7 p-1",
      base: "h-8 w-8 p-1.5",
      large: "h-10 w-10 p-2.5",
      xlarge: "h-12 w-12 p-3.5"
    }
  },
  defaultVariants: {
    variant: "primary",
    size: "base"
  }
});
var IconButton = React.forwardRef(({
  /**
   * The button's style.
   */
  variant = "primary",
  /**
   * The button's size.
   */
  size = "base",
  /**
   * Whether to remove the wrapper `button` element and use the
   * passed child element instead.
   */
  asChild = false,
  className,
  children,
  /**
   * Whether to show a loading spinner.
   */
  isLoading = false,
  disabled,
  ...props
}, ref) => {
  const Component = asChild ? dist_exports.Root : "button";
  const renderInner = () => {
    if (isLoading) {
      return React.createElement(
        "span",
        { className: "pointer-events-none" },
        React.createElement(
          "div",
          { className: clx("bg-ui-bg-disabled absolute inset-0 flex items-center justify-center rounded-md") },
          React.createElement(Spinner, { className: "animate-spin" })
        ),
        children
      );
    }
    return children;
  };
  return React.createElement(Component, { ref, ...props, className: clx(iconButtonVariants({ variant, size }), className), disabled: disabled || isLoading }, renderInner());
});
IconButton.displayName = "IconButton";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/alert/alert.js
var React2 = __toESM(require_react());
var Alert = React2.forwardRef(({
  /**
   * The variant of the alert
   */
  variant = "info",
  /**
   * Whether the alert is dismissible
   */
  dismissible = false,
  className,
  children,
  ...props
}, ref) => {
  const [dismissed, setDismissed] = React2.useState(false);
  const Icon = {
    info: InformationCircleSolid,
    error: XCircleSolid,
    success: CheckCircleSolid,
    warning: ExclamationCircleSolid
  }[variant];
  const handleDismiss = () => {
    setDismissed(true);
  };
  if (dismissed) {
    return null;
  }
  return React2.createElement(
    "div",
    { ref, className: clx("bg-ui-bg-subtle txt-compact-small grid items-start gap-x-2 text-pretty rounded-lg border p-3", {
      "grid-cols-[20px_1fr]": !dismissible,
      "grid-cols-[20px_1fr_20px]": dismissible
    }, className), ...props },
    React2.createElement(Icon, { className: clx({
      "text-ui-tag-red-icon": variant === "error",
      "text-ui-tag-green-icon": variant === "success",
      "text-ui-tag-orange-icon": variant === "warning",
      "text-ui-tag-neutral-icon": variant === "info"
    }) }),
    React2.createElement("div", { className: "w-full" }, children),
    dismissible && React2.createElement(
      IconButton,
      { size: "2xsmall", variant: "transparent", type: "button", onClick: handleDismiss },
      React2.createElement(XMarkMini, { className: "text-ui-fg-muted" })
    )
  );
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/avatar/avatar.js
var React3 = __toESM(require_react());
var avatarVariants = cva({
  base: "flex shrink-0 items-center justify-center overflow-hidden shadow-borders-base bg-ui-bg-base",
  variants: {
    variant: {
      squared: "",
      rounded: "rounded-full"
    },
    size: {
      "2xsmall": "h-5 w-5",
      xsmall: "h-6 w-6",
      small: "h-7 w-7",
      base: "h-8 w-8",
      large: "h-10 w-10",
      xlarge: "h-12 w-12"
    }
  },
  compoundVariants: [
    {
      variant: "squared",
      size: "2xsmall",
      className: "rounded"
    },
    {
      variant: "squared",
      size: "xsmall",
      className: "rounded-md"
    },
    {
      variant: "squared",
      size: "small",
      className: "rounded-md"
    },
    {
      variant: "squared",
      size: "base",
      className: "rounded-md"
    },
    {
      variant: "squared",
      size: "large",
      className: "rounded-lg"
    },
    {
      variant: "squared",
      size: "xlarge",
      className: "rounded-xl"
    }
  ],
  defaultVariants: {
    variant: "rounded",
    size: "base"
  }
});
var innerVariants = cva({
  base: "aspect-square object-cover object-center",
  variants: {
    variant: {
      squared: "",
      rounded: "rounded-full"
    },
    size: {
      "2xsmall": "txt-compact-xsmall-plus size-4",
      xsmall: "txt-compact-xsmall-plus size-5",
      small: "txt-compact-small-plus size-6",
      base: "txt-compact-small-plus size-7",
      large: "txt-compact-medium-plus size-9",
      xlarge: "txt-compact-large-plus size-11"
    }
  },
  compoundVariants: [
    {
      variant: "squared",
      size: "2xsmall",
      className: "rounded-sm"
    },
    {
      variant: "squared",
      size: "xsmall",
      className: "rounded"
    },
    {
      variant: "squared",
      size: "small",
      className: "rounded"
    },
    {
      variant: "squared",
      size: "base",
      className: "rounded"
    },
    {
      variant: "squared",
      size: "large",
      className: "rounded-md"
    },
    {
      variant: "squared",
      size: "xlarge",
      className: "rounded-[10px]"
    }
  ],
  defaultVariants: {
    variant: "rounded",
    size: "base"
  }
});
var Avatar = React3.forwardRef(({
  /**
   * The URL of the image used in the Avatar.
   */
  src,
  /**
   * Text to show in the avatar if the URL provided in `src` can't be opened.
   */
  fallback,
  /**
   * The style of the avatar.
   */
  variant = "rounded",
  /**
   * The size of the avatar's border radius.
   */
  size = "base",
  className,
  ...props
}, ref) => {
  return React3.createElement(
    dist_exports5.Root,
    { ref, ...props, className: clx(avatarVariants({ variant, size }), className) },
    src && React3.createElement(dist_exports5.Image, { src, className: innerVariants({ variant, size }) }),
    React3.createElement(dist_exports5.Fallback, { className: clx(innerVariants({ variant, size }), "bg-ui-bg-component-hover text-ui-fg-subtle pointer-events-none flex select-none items-center justify-center") }, fallback)
  );
});
Avatar.displayName = "Avatar";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/badge/badge.js
var React4 = __toESM(require_react());
var badgeColorVariants = cva({
  variants: {
    color: {
      green: "bg-ui-tag-green-bg text-ui-tag-green-text [&_svg]:text-ui-tag-green-icon border-ui-tag-green-border",
      red: "bg-ui-tag-red-bg text-ui-tag-red-text [&_svg]:text-ui-tag-red-icon border-ui-tag-red-border",
      blue: "bg-ui-tag-blue-bg text-ui-tag-blue-text [&_svg]:text-ui-tag-blue-icon border-ui-tag-blue-border",
      orange: "bg-ui-tag-orange-bg text-ui-tag-orange-text [&_svg]:text-ui-tag-orange-icon border-ui-tag-orange-border",
      grey: "bg-ui-tag-neutral-bg text-ui-tag-neutral-text [&_svg]:text-ui-tag-neutral-icon border-ui-tag-neutral-border",
      purple: "bg-ui-tag-purple-bg text-ui-tag-purple-text [&_svg]:text-ui-tag-purple-icon border-ui-tag-purple-border"
    }
  },
  defaultVariants: {
    color: "grey"
  }
});
var badgeSizeVariants = cva({
  base: "inline-flex items-center gap-x-0.5 border box-border",
  variants: {
    size: {
      "2xsmall": "txt-compact-xsmall-plus h-5",
      xsmall: "txt-compact-xsmall-plus py-px h-6",
      small: "txt-compact-xsmall-plus py-[3px] h-7",
      base: "txt-compact-small-plus py-[5px] h-8",
      large: "txt-compact-medium-plus py-[7px] h-10"
    },
    rounded: {
      base: "rounded-md",
      full: "rounded-full"
    }
  },
  compoundVariants: [
    {
      size: "2xsmall",
      rounded: "full",
      className: "px-1.5"
    },
    {
      size: "2xsmall",
      rounded: "base",
      className: "px-1"
    },
    {
      size: "xsmall",
      rounded: "full",
      className: "px-2"
    },
    {
      size: "xsmall",
      rounded: "base",
      className: "px-1.5"
    },
    {
      size: "small",
      rounded: "full",
      className: "px-2.5"
    },
    {
      size: "small",
      rounded: "base",
      className: "px-2"
    },
    {
      size: "base",
      rounded: "full",
      className: "px-3"
    },
    {
      size: "base",
      rounded: "base",
      className: "px-2.5"
    },
    {
      size: "large",
      rounded: "full",
      className: "px-3.5"
    },
    {
      size: "large",
      rounded: "base",
      className: "px-3"
    }
  ],
  defaultVariants: {
    size: "base",
    rounded: "base"
  }
});
var Badge = React4.forwardRef(({
  className,
  /**
   * The badge's size.
   */
  size = "base",
  /**
   * The style of the badge's border radius.
   */
  rounded = "base",
  /**
   * The badge's color.
   */
  color = "grey",
  /**
   * Whether to remove the wrapper `span` element and use the
   * passed child element instead.
   */
  asChild = false,
  ...props
}, ref) => {
  const Component = asChild ? dist_exports.Root : "span";
  return React4.createElement(Component, { ref, className: clx(badgeColorVariants({ color }), badgeSizeVariants({ size, rounded }), className), ...props });
});
Badge.displayName = "Badge";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/button/button.js
var React5 = __toESM(require_react());
var buttonVariants = cva({
  base: clx("transition-fg relative inline-flex w-fit items-center justify-center overflow-hidden rounded-md outline-none", "disabled:bg-ui-bg-disabled disabled:border-ui-border-base disabled:text-ui-fg-disabled disabled:shadow-buttons-neutral disabled:after:hidden", "after:transition-fg after:absolute after:inset-0 after:content-['']"),
  variants: {
    variant: {
      primary: clx("shadow-buttons-inverted text-ui-contrast-fg-primary bg-ui-button-inverted after:button-inverted-gradient", "hover:bg-ui-button-inverted-hover hover:after:button-inverted-hover-gradient", "active:bg-ui-button-inverted-pressed active:after:button-inverted-pressed-gradient", "focus-visible:!shadow-buttons-inverted-focus"),
      secondary: clx("shadow-buttons-neutral text-ui-fg-base bg-ui-button-neutral after:button-neutral-gradient", "hover:bg-ui-button-neutral-hover hover:after:button-neutral-hover-gradient", "active:bg-ui-button-neutral-pressed active:after:button-neutral-pressed-gradient", "focus-visible:shadow-buttons-neutral-focus"),
      transparent: clx("after:hidden", "text-ui-fg-base bg-ui-button-transparent", "hover:bg-ui-button-transparent-hover", "active:bg-ui-button-transparent-pressed", "focus-visible:shadow-buttons-neutral-focus focus-visible:bg-ui-bg-base", "disabled:!bg-transparent disabled:!shadow-none"),
      danger: clx("shadow-buttons-colored shadow-buttons-danger text-ui-fg-on-color bg-ui-button-danger after:button-danger-gradient", "hover:bg-ui-button-danger-hover hover:after:button-danger-hover-gradient", "active:bg-ui-button-danger-pressed active:after:button-danger-pressed-gradient", "focus-visible:shadow-buttons-danger-focus")
    },
    size: {
      small: "txt-compact-small-plus gap-x-1.5 px-2 py-1",
      base: "txt-compact-small-plus gap-x-1.5 px-3 py-1.5",
      large: "txt-compact-medium-plus gap-x-1.5 px-4 py-2.5",
      xlarge: "txt-compact-large-plus gap-x-1.5 px-5 py-3.5"
    }
  },
  defaultVariants: {
    size: "base",
    variant: "primary"
  }
});
var Button = React5.forwardRef(({
  /**
   * The button's style.
   */
  variant = "primary",
  /**
   * The button's size.
   */
  size = "base",
  className,
  /**
   * Whether to remove the wrapper `button` element and use the
   * passed child element instead.
   */
  asChild = false,
  children,
  /**
   * Whether to show a loading spinner.
   */
  isLoading = false,
  disabled,
  ...props
}, ref) => {
  const Component = asChild ? dist_exports.Root : "button";
  const renderInner = () => {
    if (isLoading) {
      return React5.createElement(
        "span",
        { className: "pointer-events-none" },
        React5.createElement(
          "div",
          { className: clx("bg-ui-bg-disabled absolute inset-0 flex items-center justify-center rounded-md") },
          React5.createElement(Spinner, { className: "animate-spin" })
        ),
        children
      );
    }
    return children;
  };
  return React5.createElement(Component, { ref, ...props, className: clx(buttonVariants({ variant, size }), className), disabled: disabled || isLoading }, renderInner());
});
Button.displayName = "Button";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/checkbox/checkbox.js
var React6 = __toESM(require_react());
var Checkbox = React6.forwardRef(({ className, checked, ...props }, ref) => {
  return React6.createElement(
    dist_exports6.Root,
    { ...props, ref, checked, className: clx("group inline-flex h-5 w-5 items-center justify-center outline-none ", className) },
    React6.createElement(
      "div",
      { className: clx("text-ui-fg-on-inverted bg-ui-bg-base shadow-borders-base [&_path]:shadow-details-contrast-on-bg-interactive transition-fg h-[15px] w-[15px] rounded-[3px]", "group-disabled:cursor-not-allowed group-disabled:opacity-50", "group-focus-visible:!shadow-borders-interactive-with-focus", "group-hover:group-enabled:group-data-[state=unchecked]:bg-ui-bg-base-hover", "group-data-[state=checked]:bg-ui-bg-interactive group-data-[state=checked]:shadow-borders-interactive-with-shadow", "group-data-[state=indeterminate]:bg-ui-bg-interactive group-data-[state=indeterminate]:shadow-borders-interactive-with-shadow") },
      React6.createElement(dist_exports6.Indicator, null, checked === "indeterminate" ? React6.createElement(MinusMini, null) : React6.createElement(CheckMini, null))
    )
  );
});
Checkbox.displayName = "Checkbox";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/tooltip/tooltip.js
var React7 = __toESM(require_react());
var Tooltip = ({
  /**
   * The element to trigger the tooltip.
   *
   * @keep
   */
  children,
  /**
   * The content to display in the tooltip.
   */
  content,
  /**
   * Whether the tooltip is currently open.
   *
   * @keep
   */
  open,
  /**
   * Whether the tooltip is open by default.
   *
   * @keep
   */
  defaultOpen,
  /**
   * A function that is called when the tooltip's open state changes.
   *
   * @keep
   */
  onOpenChange,
  /**
   * The time in milliseconds to delay the tooltip's appearance.
   *
   * @keep
   */
  delayDuration,
  /**
   * The maximum width of the tooltip.
   */
  maxWidth = 220,
  className,
  /**
   * The side to position the tooltip.
   *
   * @defaultValue top
   */
  side,
  /**
   * The distance in pixels between the tooltip and its trigger.
   *
   * @keep
   */
  sideOffset = 8,
  /**
   * A function that is triggered when the tooltip is clicked.
   */
  onClick,
  ...props
}) => {
  return React7.createElement(
    dist_exports15.Root,
    { open, defaultOpen, onOpenChange, delayDuration },
    React7.createElement(dist_exports15.Trigger, { onClick, asChild: true }, children),
    React7.createElement(
      dist_exports15.Portal,
      null,
      React7.createElement(dist_exports15.Content, { side, sideOffset, align: "center", className: clx("txt-compact-xsmall text-ui-fg-subtle bg-ui-bg-base shadow-elevation-tooltip rounded-lg px-2.5 py-1", "animate-in fade-in-0 zoom-in-95 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2", className), ...props, style: { ...props.style, maxWidth } }, content)
    )
  );
};
var TooltipProvider = ({ children, delayDuration = 100, skipDelayDuration = 300, ...props }) => {
  return React7.createElement(dist_exports15.TooltipProvider, { delayDuration, skipDelayDuration, ...props }, children);
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/copy/copy.js
var import_copy_to_clipboard = __toESM(require_copy_to_clipboard());
var import_react = __toESM(require_react());
var Copy = import_react.default.forwardRef(({
  children,
  className,
  /**
   * The content to copy.
   */
  content,
  /**
   * The variant of the copy button.
   */
  variant = "default",
  /**
   * Whether to remove the wrapper `button` element and use the
   * passed child element instead.
   */
  asChild = false,
  ...props
}, ref) => {
  const [done, setDone] = (0, import_react.useState)(false);
  const [open, setOpen] = (0, import_react.useState)(false);
  const [text, setText] = (0, import_react.useState)("Copy");
  const copyToClipboard = (e) => {
    e.stopPropagation();
    setDone(true);
    (0, import_copy_to_clipboard.default)(content);
    setTimeout(() => {
      setDone(false);
    }, 2e3);
  };
  import_react.default.useEffect(() => {
    if (done) {
      setText("Copied");
      return;
    }
    setTimeout(() => {
      setText("Copy");
    }, 500);
  }, [done]);
  const isDefaultVariant = (variant2) => {
    return variant2 === "default";
  };
  const isDefault = isDefaultVariant(variant);
  const Component = asChild ? dist_exports.Root : "button";
  return import_react.default.createElement(
    Tooltip,
    { content: text, open: done || open, onOpenChange: setOpen },
    import_react.default.createElement(Component, { ref, "aria-label": "Copy code snippet", type: "button", className: clx("h-fit w-fit", className), onClick: copyToClipboard, ...props }, children ? children : done ? isDefault ? import_react.default.createElement(CheckCircleSolid, { className: "text-ui-fg-subtle" }) : import_react.default.createElement(CheckCircleMiniSolid, { className: "text-ui-fg-subtle" }) : isDefault ? import_react.default.createElement(SquareTwoStack, { className: "text-ui-fg-subtle" }) : import_react.default.createElement(SquareTwoStackMini, { className: "text-ui-fg-subtle" }))
  );
});
Copy.displayName = "Copy";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/code-block/code-block.js
var React9 = __toESM(require_react());
(typeof global !== "undefined" ? global : window).Prism = Prism;
import("./prism-json-CZ7I2BOC.js");
var CodeBlockContext = React9.createContext(null);
var useCodeBlockContext = () => {
  const context = React9.useContext(CodeBlockContext);
  if (context === null)
    throw new Error("useCodeBlockContext can only be used within a CodeBlockContext");
  return context;
};
var Root = ({
  /**
   * The code snippets.
   */
  snippets,
  className,
  children,
  ...props
}) => {
  const [active, setActive] = React9.useState(snippets[0]);
  return React9.createElement(
    CodeBlockContext.Provider,
    { value: { snippets, active, setActive } },
    React9.createElement("div", { className: clx("bg-ui-contrast-bg-base shadow-elevation-code-block flex flex-col overflow-hidden rounded-xl", className), ...props }, children)
  );
};
Root.displayName = "CodeBlock";
var HeaderComponent = ({
  children,
  className,
  /**
   * Whether to hide the code snippets' labels.
   */
  hideLabels = false,
  ...props
}) => {
  const { snippets, active, setActive } = useCodeBlockContext();
  const tabRefs = React9.useRef([]);
  const tabIndicatorRef = React9.useRef(null);
  React9.useEffect(() => {
    const activeTabRef = tabRefs.current.find((ref) => (ref === null || ref === void 0 ? void 0 : ref.dataset.label) === active.label);
    if (activeTabRef && tabIndicatorRef.current) {
      const activeTabIndex = tabRefs.current.indexOf(activeTabRef);
      const prevTabRef = activeTabIndex > 0 ? tabRefs.current[activeTabIndex - 1] : null;
      tabIndicatorRef.current.style.width = `${activeTabRef.offsetWidth}px`;
      tabIndicatorRef.current.style.left = prevTabRef ? `${tabRefs.current.slice(0, activeTabIndex).reduce((total, tab) => total + ((tab === null || tab === void 0 ? void 0 : tab.offsetWidth) || 0) + 12, 0) + 15}px` : "15px";
    }
  }, [active]);
  return React9.createElement(
    "div",
    null,
    React9.createElement(
      "div",
      { className: clx("flex items-start px-4 pt-2.5", className), ...props },
      !hideLabels && snippets.map((snippet, idx) => React9.createElement(
        "div",
        { className: clx("text-ui-contrast-fg-secondary txt-compact-small-plus transition-fg relative cursor-pointer pb-[9px] pr-3", {
          "text-ui-contrast-fg-primary cursor-default": active.label === snippet.label
        }), key: snippet.label, onClick: () => setActive(snippet) },
        React9.createElement("span", { ref: (ref) => {
          tabRefs.current[idx] = ref;
          return void 0;
        }, "data-label": snippet.label }, snippet.label)
      )),
      children
    ),
    React9.createElement(
      "div",
      { className: "w-full px-0.5" },
      React9.createElement(
        "div",
        { className: "bg-ui-contrast-border-top relative h-px w-full" },
        React9.createElement(
          "div",
          { ref: tabIndicatorRef, className: clx("absolute bottom-0 transition-all motion-reduce:transition-none", "duration-150 ease-linear") },
          React9.createElement("div", { className: "bg-ui-contrast-fg-primary h-px rounded-full" })
        )
      )
    )
  );
};
HeaderComponent.displayName = "CodeBlock.Header";
var Meta = ({ className, ...props }) => {
  return React9.createElement("div", { className: clx("txt-compact-small text-ui-contrast-fg-secondary ml-auto", className), ...props });
};
Meta.displayName = "CodeBlock.Header.Meta";
var Header = Object.assign(HeaderComponent, { Meta });
var Body = ({ className, children, ...props }) => {
  const { active } = useCodeBlockContext();
  const showToolbar = children || !active.hideCopy;
  return React9.createElement(
    "div",
    null,
    showToolbar && React9.createElement(
      "div",
      { className: "border-ui-contrast-border-bot flex min-h-10 items-center gap-x-3 border-t px-4 py-2" },
      React9.createElement("div", { className: "code-body text-ui-contrast-fg-secondary flex-1" }, children),
      !active.hideCopy && React9.createElement(Copy, { content: active.code, className: "text-ui-contrast-fg-secondary" })
    ),
    React9.createElement(
      "div",
      { className: "flex h-full flex-col overflow-hidden px-[5px] pb-[5px]" },
      React9.createElement(
        "div",
        { className: clx("bg-ui-contrast-bg-subtle border-ui-contrast-border-bot relative h-full overflow-y-auto rounded-lg border p-4", className), ...props },
        React9.createElement(
          "div",
          { className: "max-w-[90%]" },
          React9.createElement(Highlight2, { theme: {
            ...themes_exports.palenight,
            plain: {
              color: "rgba(249, 250, 251, 1)",
              backgroundColor: "var(--contrast-fg-primary)"
            },
            styles: [
              ...themes_exports.palenight.styles,
              {
                types: ["keyword"],
                style: {
                  fontStyle: "normal",
                  color: "rgb(187,160,255)"
                }
              },
              {
                types: ["punctuation", "operator"],
                style: {
                  fontStyle: "normal",
                  color: "rgb(255,255,255)"
                }
              },
              {
                types: ["constant", "boolean"],
                style: {
                  fontStyle: "normal",
                  color: "rgb(187,77,96)"
                }
              },
              {
                types: ["function"],
                style: {
                  fontStyle: "normal",
                  color: "rgb(27,198,242)"
                }
              },
              {
                types: ["number"],
                style: {
                  color: "rgb(247,208,25)"
                }
              },
              {
                types: ["property"],
                style: {
                  color: "rgb(247,208,25)"
                }
              },
              {
                types: ["maybe-class-name"],
                style: {
                  color: "rgb(255,203,107)"
                }
              },
              {
                types: ["string"],
                style: {
                  color: "rgb(73,209,110)"
                }
              },
              {
                types: ["comment"],
                style: {
                  color: "var(--contrast-fg-secondary)",
                  fontStyle: "normal"
                }
              }
            ]
          }, code: active.code, language: active.language }, ({ style, tokens, getLineProps, getTokenProps }) => React9.createElement(
            "pre",
            { className: clx("code-body whitespace-pre-wrap bg-transparent", {
              "grid grid-cols-[auto,1fr] gap-x-4": !active.hideLineNumbers
            }), style: {
              ...style,
              background: "transparent"
            } },
            !active.hideLineNumbers && React9.createElement("div", { role: "presentation", className: "flex flex-col text-right" }, tokens.map((_, i) => React9.createElement("span", { key: i, className: "text-ui-contrast-fg-secondary tabular-nums" }, i + 1))),
            React9.createElement("div", null, tokens.map((line, i) => React9.createElement("div", { key: i, ...getLineProps({ line }) }, line.map((token, key) => React9.createElement("span", { key, ...getTokenProps({ token }) })))))
          ))
        )
      )
    )
  );
};
Body.displayName = "CodeBlock.Body";
var CodeBlock = Object.assign(Root, { Body, Header, Meta });

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/otp-input/otp-input.js
var React10 = __toESM(require_react());
var sanitizeOtp = (value, length) => {
  return value.replace(/\D/g, "").slice(0, length);
};
var getDigits = (value, length) => {
  return sanitizeOtp(value, length).padEnd(length, " ").split("");
};
var getWritableDigits = (value, length) => {
  return getDigits(value, length).map((digit) => digit === " " ? "" : digit);
};
var getDigitGroups = (length, groupSize) => {
  return Array.from({ length: Math.ceil(length / groupSize) }).map((_, groupIndex) => {
    const start = groupIndex * groupSize;
    return {
      start,
      length: Math.min(groupSize, length - start)
    };
  });
};
var applyOtpAtIndex = (digits, index, value, length) => {
  value.split("").forEach((digit, offset) => {
    if (index + offset < length) {
      digits[index + offset] = digit;
    }
  });
  return digits;
};
var OtpInput = React10.forwardRef(({ "aria-invalid": ariaInvalid, "aria-label": ariaLabel = "One-time password", autoFocus, className, disabled, groupSize = 3, inputClassName, length = 6, onChange, onComplete, readOnly, separator = "-", value, ...props }, ref) => {
  const inputRefs = React10.useRef([]);
  const codeLength = Math.max(1, length);
  const digits = getDigits(value, codeLength);
  const safeGroupSize = Math.max(1, groupSize);
  const digitGroups = getDigitGroups(codeLength, safeGroupSize);
  const focusInput = (index) => {
    var _a;
    const nextIndex = Math.min(Math.max(index, 0), codeLength - 1);
    (_a = inputRefs.current[nextIndex]) === null || _a === void 0 ? void 0 : _a.focus();
  };
  const updateValue = (nextValue, nextFocusIndex) => {
    const sanitizedValue = sanitizeOtp(nextValue, codeLength);
    onChange(sanitizedValue);
    if (sanitizedValue.length === codeLength) {
      onComplete === null || onComplete === void 0 ? void 0 : onComplete(sanitizedValue);
    }
    if (typeof nextFocusIndex === "number") {
      requestAnimationFrame(() => focusInput(nextFocusIndex));
    }
  };
  const handleChange = (index, rawValue) => {
    const nextDigits = getWritableDigits(value, codeLength);
    const incoming = sanitizeOtp(rawValue, codeLength);
    if (!incoming) {
      nextDigits[index] = "";
      updateValue(nextDigits.join(""), index);
      return;
    }
    applyOtpAtIndex(nextDigits, index, incoming, codeLength);
    updateValue(nextDigits.join(""), Math.min(index + incoming.length, codeLength - 1));
  };
  const handleKeyDown = (event, index) => {
    if (event.key === "Backspace" && !digits[index].trim()) {
      event.preventDefault();
      if (index === 0) {
        return;
      }
      const nextDigits = getWritableDigits(value, codeLength);
      nextDigits[index - 1] = "";
      updateValue(nextDigits.join(""), index - 1);
    }
    if (event.key === "ArrowLeft") {
      event.preventDefault();
      focusInput(index - 1);
    }
    if (event.key === "ArrowRight") {
      event.preventDefault();
      focusInput(index + 1);
    }
    if (event.key === "Home") {
      event.preventDefault();
      focusInput(0);
    }
    if (event.key === "End") {
      event.preventDefault();
      focusInput(codeLength - 1);
    }
  };
  const handlePaste = (event, index) => {
    event.preventDefault();
    const pastedValue = sanitizeOtp(event.clipboardData.getData("text"), codeLength);
    if (!pastedValue) {
      return;
    }
    if (pastedValue.length === codeLength) {
      updateValue(pastedValue, codeLength - 1);
      return;
    }
    const nextDigits = getWritableDigits(value, codeLength);
    applyOtpAtIndex(nextDigits, index, pastedValue, codeLength);
    updateValue(nextDigits.join(""), Math.min(index + pastedValue.length, codeLength - 1));
  };
  const renderInput = (index) => {
    return React10.createElement("input", { key: index, ref: (element) => {
      inputRefs.current[index] = element;
    }, "aria-invalid": ariaInvalid, "aria-label": `${ariaLabel} ${index + 1}`, autoComplete: index === 0 ? "one-time-code" : "off", autoFocus: autoFocus && index === 0, className: clx("txt-compact-large font-mono text-ui-fg-base bg-ui-bg-field hover:bg-ui-bg-field-hover shadow-borders-base transition-fg relative -ml-px flex size-10 appearance-none items-center justify-center border-0 text-center outline-none first:ml-0 first:rounded-l-md last:rounded-r-md", "focus-visible:z-10 focus-visible:shadow-borders-interactive-with-active", "disabled:text-ui-fg-disabled disabled:!bg-ui-bg-disabled disabled:cursor-not-allowed", "aria-[invalid=true]:!shadow-borders-error", inputClassName), disabled, inputMode: "numeric", maxLength: codeLength, onChange: (event) => handleChange(index, event.target.value), onFocus: (event) => event.target.select(), onKeyDown: (event) => handleKeyDown(event, index), onPaste: (event) => handlePaste(event, index), pattern: "[0-9]*", readOnly, type: "text", value: digits[index].trim() });
  };
  return React10.createElement("div", { ref, "aria-invalid": ariaInvalid, "aria-label": ariaLabel, className: clx("flex items-center justify-center gap-x-3", className), role: "group", ...props }, digitGroups.map(({ start, length: length2 }, groupIndex) => {
    return React10.createElement(
      React10.Fragment,
      { key: groupIndex },
      groupIndex > 0 && separator && React10.createElement("span", { "aria-hidden": "true", className: "txt-compact-small-plus text-ui-fg-muted" }, separator),
      React10.createElement("div", { className: "flex" }, Array.from({ length: length2 }).map((_, offset) => renderInput(start + offset)))
    );
  }));
});
OtpInput.displayName = "OtpInput";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/command-bar/command-bar.js
var React12 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/kbd/kbd.js
var React11 = __toESM(require_react());
var Kbd = React11.forwardRef(({ children, className, ...props }, ref) => {
  return React11.createElement("kbd", { ...props, ref, className: clx("bg-ui-tag-neutral-bg text-ui-tag-neutral-text border-ui-tag-neutral-border inline-flex h-5 w-fit min-w-[20px] items-center justify-center rounded-md border px-1", "txt-compact-xsmall-plus", className) }, children);
});
Kbd.displayName = "Kbd";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/utils/is-input-element.js
var isInputElement = (element) => {
  return element instanceof HTMLElement && (element.isContentEditable || element.tagName === "INPUT" || element.tagName === "TEXTAREA" || element.tagName === "SELECT");
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/command-bar/command-bar.js
var Root2 = ({
  /**
   * Whether to open (show) the command bar.
   */
  open = false,
  /**
   * Specify a function to handle the change of `open`'s value.
   */
  onOpenChange,
  /**
   * Whether the command bar is open by default.
   */
  defaultOpen = false,
  /**
   * Whether to disable focusing automatically on the command bar when it's opened.
   */
  disableAutoFocus = true,
  children
}) => {
  return React12.createElement(
    dist_exports10.Root,
    { open, onOpenChange, defaultOpen },
    React12.createElement(
      dist_exports3.Root,
      null,
      React12.createElement(dist_exports10.Anchor, { className: clx("fixed bottom-8 left-1/2 h-px w-px -translate-x-1/2") })
    ),
    React12.createElement(
      dist_exports10.Portal,
      null,
      React12.createElement(dist_exports10.Content, { side: "top", sideOffset: 0, onOpenAutoFocus: (e) => {
        if (disableAutoFocus) {
          e.preventDefault();
        }
      }, className: clx("data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2") }, children)
    )
  );
};
Root2.displayName = "CommandBar";
var Value = React12.forwardRef(({ className, ...props }, ref) => {
  return React12.createElement("div", { ref, className: clx("txt-compact-small-plus text-ui-contrast-fg-secondary px-3 py-2.5", className), ...props });
});
Value.displayName = "CommandBar.Value";
var Bar = React12.forwardRef(({ className, ...props }, ref) => {
  return React12.createElement("div", { ref, className: clx("bg-ui-contrast-bg-base relative flex items-center overflow-hidden rounded-full px-1", "after:shadow-elevation-flyout after:pointer-events-none after:absolute after:inset-0 after:rounded-full after:content-['']", className), ...props });
});
Bar.displayName = "CommandBar.Bar";
var Seperator = React12.forwardRef(({ className, ...props }, ref) => {
  return React12.createElement("div", { ref, className: clx("bg-ui-contrast-border-base h-10 w-px", className), ...props });
});
Seperator.displayName = "CommandBar.Seperator";
var Command = React12.forwardRef(({
  className,
  /**
   * @ignore
   */
  type = "button",
  /**
   * The command's label.
   */
  label,
  /**
   * The function to execute when the command is triggered.
   */
  action,
  /**
   * The command's shortcut
   */
  shortcut,
  disabled,
  ...props
}, ref) => {
  React12.useEffect(() => {
    const handleKeyDown = (event) => {
      if (isInputElement(document.activeElement)) {
        return;
      }
      if (event.key.toLowerCase() === shortcut.toLowerCase()) {
        event.preventDefault();
        event.stopPropagation();
        action();
      }
    };
    if (!disabled) {
      document.addEventListener("keydown", handleKeyDown);
    }
    return () => {
      document.removeEventListener("keydown", handleKeyDown);
    };
  }, [action, shortcut, disabled]);
  return React12.createElement(
    "button",
    { ref, className: clx("bg-ui-contrast-bg-base txt-compact-small-plus transition-fg text-ui-contrast-fg-primary flex items-center gap-x-2 px-3 py-2.5 outline-none", "focus-visible:bg-ui-contrast-bg-highlight focus-visible:hover:bg-ui-contrast-bg-base-hover hover:bg-ui-contrast-bg-base-hover active:bg-ui-contrast-bg-base-pressed focus-visible:active:bg-ui-contrast-bg-base-pressed disabled:!bg-ui-bg-disabled disabled:!text-ui-fg-disabled", "last-of-type:-mr-1 last-of-type:pr-4", className), type, onClick: action, ...props },
    React12.createElement("span", null, label),
    React12.createElement(Kbd, { className: "bg-ui-contrast-bg-subtle border-ui-contrast-border-base text-ui-contrast-fg-secondary" }, shortcut.toUpperCase())
  );
});
Command.displayName = "CommandBar.Command";
var CommandBar = Object.assign(Root2, {
  Command,
  Value,
  Bar,
  Seperator
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/container/container.js
var React13 = __toESM(require_react());
var Container = React13.forwardRef(({ className, ...props }, ref) => {
  return React13.createElement("div", { ref, className: clx("shadow-elevation-card-rest bg-ui-bg-base w-full rounded-lg px-6 py-4", className), ...props });
});
Container.displayName = "Container";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/text/text.js
var React14 = __toESM(require_react());
var textVariants = cva({
  variants: {
    size: {
      xsmall: "",
      small: "",
      base: "",
      large: "",
      xlarge: ""
    },
    weight: {
      regular: "font-normal",
      plus: "font-medium"
    },
    family: {
      sans: "font-sans",
      mono: "font-mono"
    },
    leading: {
      normal: "",
      compact: ""
    }
  },
  defaultVariants: {
    family: "sans",
    size: "base",
    weight: "regular",
    leading: "normal"
  },
  compoundVariants: [
    {
      size: "xsmall",
      leading: "normal",
      className: "txt-xsmall"
    },
    {
      size: "xsmall",
      leading: "compact",
      className: "txt-compact-xsmall"
    },
    {
      size: "small",
      leading: "normal",
      className: "txt-small"
    },
    {
      size: "small",
      leading: "compact",
      className: "txt-compact-small"
    },
    {
      size: "base",
      leading: "normal",
      className: "txt-medium"
    },
    {
      size: "base",
      leading: "compact",
      className: "txt-compact-medium"
    },
    {
      size: "large",
      leading: "normal",
      className: "txt-large"
    },
    {
      size: "large",
      leading: "compact",
      className: "txt-compact-large"
    },
    {
      size: "xlarge",
      leading: "normal",
      className: "txt-xlarge"
    },
    {
      size: "xlarge",
      leading: "compact",
      className: "txt-compact-xlarge"
    }
  ]
});
var Text = React14.forwardRef(({
  className,
  /**
   * Whether to remove the wrapper `button` element and use the
   * passed child element instead.
   */
  asChild = false,
  /**
   * The wrapper element to use when `asChild` is disabled.
   */
  as = "p",
  /**
   * The text's size.
   */
  size = "base",
  /**
   * The text's font weight.
   */
  weight = "regular",
  /**
   * The text's font family.
   */
  family = "sans",
  /**
   * The text's line height.
   */
  leading = "normal",
  children,
  ...props
}, ref) => {
  const Component = asChild ? dist_exports.Root : as;
  return React14.createElement(Component, { ref, className: clx(textVariants({ size, weight, family, leading }), className), ...props }, children);
});
Text.displayName = "Text";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/currency-input/currency-input.js
var React15 = __toESM(require_react());
var currencyInputVariants = cva({
  base: clx("flex items-center gap-x-1", "bg-ui-bg-field hover:bg-ui-bg-field-hover shadow-buttons-neutral placeholder-ui-fg-muted text-ui-fg-base transition-fg relative w-full rounded-md", "focus-within:shadow-borders-interactive-with-active"),
  variants: {
    size: {
      base: "txt-compact-medium h-8",
      small: "txt-compact-small h-7"
    }
  },
  defaultVariants: {
    size: "base"
  }
});
var CurrencyInput2 = React15.forwardRef(({
  /**
   * The input's size.
   */
  size = "base",
  /**
   * The symbol to show in the input.
   */
  symbol,
  /**
   * The currency code to show in the input.
   */
  code,
  /**
   * Whether the input is disabled.
   *
   * @keep
   * @defaultValue false
   */
  disabled,
  /**
   * A function that is triggered when the input is invalid.
   *
   * @keep
   */
  onInvalid,
  className,
  ...props
}, ref) => {
  const innerRef = React15.useRef(null);
  React15.useImperativeHandle(ref, () => innerRef.current);
  const [valid, setValid] = React15.useState(true);
  const onInnerInvalid = React15.useCallback((event) => {
    setValid(event.currentTarget.validity.valid);
    if (onInvalid) {
      onInvalid(event);
    }
  }, [onInvalid]);
  return React15.createElement(
    "div",
    { onClick: () => {
      if (innerRef.current) {
        innerRef.current.focus();
      }
    }, className: clx("w-full cursor-text justify-between overflow-hidden", currencyInputVariants({ size }), {
      "text-ui-fg-disabled !bg-ui-bg-disabled !shadow-buttons-neutral !placeholder-ui-fg-disabled cursor-not-allowed": disabled,
      "!shadow-borders-error invalid:!shadow-borders-error": props["aria-invalid"] || !valid
    }, className) },
    React15.createElement(
      "span",
      { className: clx("w-fit min-w-[32px] border-r px-2", {
        "py-[9px]": size === "base",
        "py-[5px]": size === "small"
      }), role: "presentation" },
      React15.createElement(Text, { size: "small", leading: "compact", className: clx("text-ui-fg-muted pointer-events-none select-none uppercase", {
        "text-ui-fg-disabled": disabled
      }) }, code)
    ),
    React15.createElement(CurrencyInput, { className: "h-full min-w-0 flex-1 appearance-none bg-transparent text-right outline-none disabled:cursor-not-allowed", disabled, onInvalid: onInnerInvalid, ref: innerRef, ...props }),
    React15.createElement(
      "span",
      { className: clx("flex w-fit min-w-[32px] items-center justify-center border-l px-2 text-right", {
        "py-[9px]": size === "base",
        "py-[5px]": size === "small"
      }), role: "presentation" },
      React15.createElement(Text, { size: "small", leading: "compact", className: clx("text-ui-fg-muted pointer-events-none select-none", {
        "text-ui-fg-disabled": disabled
      }) }, symbol)
    )
  );
});
CurrencyInput2.displayName = "CurrencyInput";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/popover/popover.js
var React16 = __toESM(require_react());
var Root3 = (props) => {
  return React16.createElement(dist_exports10.Root, { ...props });
};
Root3.displayName = "Popover";
var Trigger = React16.forwardRef((props, ref) => {
  return React16.createElement(dist_exports10.Trigger, { ref, ...props });
});
Trigger.displayName = "Popover.Trigger";
var Anchor = React16.forwardRef((props, ref) => {
  return React16.createElement(dist_exports10.Anchor, { ref, ...props });
});
Anchor.displayName = "Popover.Anchor";
var Close = React16.forwardRef((props, ref) => {
  return React16.createElement(dist_exports10.Close, { ref, ...props });
});
Close.displayName = "Popover.Close";
var Content = React16.forwardRef(({
  className,
  /**
   * The distance in pixels from the anchor.
   */
  sideOffset = 8,
  /**
   * The preferred side of the anchor to render against when open.
   * Will be reversed when collisions occur and `avoidCollisions` is enabled.
   */
  side = "bottom",
  /**
   * The preferred alignment against the anchor. May change when collisions occur.
   */
  align = "start",
  collisionPadding,
  ...props
}, ref) => {
  return React16.createElement(
    dist_exports10.Portal,
    null,
    React16.createElement(dist_exports10.Content, { ref, sideOffset, side, align, collisionPadding, className: clx("bg-ui-bg-base text-ui-fg-base shadow-elevation-flyout max-h-[var(--radix-popper-available-height)] min-w-[220px] overflow-hidden rounded-lg p-1", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2", className), ...props })
  );
});
Content.displayName = "Popover.Content";
var Seperator2 = React16.forwardRef(({ className, ...props }, ref) => {
  return React16.createElement("div", { ref, className: clx("bg-ui-border-base -mx-1 my-1 h-px", className), ...props });
});
Seperator2.displayName = "Popover.Seperator";
var Popover = Object.assign(Root3, {
  Trigger,
  Anchor,
  Close,
  Content,
  Seperator: Seperator2
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/date-picker/date-picker.js
var React27 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/_internal-calendar.js
var React20 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/calendar-button.js
var React17 = __toESM(require_react());
var CalendarButton = React17.forwardRef(({ children, ...props }, ref) => {
  const innerRef = React17.useRef(null);
  React17.useImperativeHandle(ref, () => innerRef.current);
  const { buttonProps } = $ac4318a9c075bb9f$export$ea18c227d4417cc3(props, innerRef);
  return React17.createElement(IconButton, { size: "small", variant: "transparent", className: "rounded-[4px]", ...buttonProps }, children);
});
CalendarButton.displayName = "CalendarButton";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/calendar-grid.js
var React19 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/calendar-cell.js
var React18 = __toESM(require_react());
var CalendarCell = ({ state, date }) => {
  const ref = React18.useRef(null);
  const { cellProps, buttonProps, isSelected, isOutsideVisibleRange, isDisabled, isUnavailable, formattedDate } = $be61a22f9aa88c7c$export$136073280381448e({ date }, state, ref);
  const isToday = getIsToday(date);
  return React18.createElement(
    "td",
    { ...cellProps, className: "p-1" },
    React18.createElement(
      "div",
      { ...buttonProps, ref, hidden: isOutsideVisibleRange, className: clx("bg-ui-bg-base txt-compact-small relative flex size-8 items-center justify-center rounded-md outline-none transition-fg border border-transparent", "hover:bg-ui-bg-base-hover", "focus-visible:shadow-borders-focus focus-visible:border-ui-border-interactive", {
        "!bg-ui-bg-interactive !text-ui-fg-on-color": isSelected,
        "hidden": isOutsideVisibleRange,
        "text-ui-fg-muted hover:!bg-ui-bg-base cursor-default": isDisabled || isUnavailable
      }) },
      formattedDate,
      isToday && React18.createElement("div", { role: "none", className: clx("bg-ui-bg-interactive absolute bottom-[3px] left-1/2 size-[3px] -translate-x-1/2 rounded-full transition-fg", {
        "bg-ui-fg-on-color": isSelected
      }) })
    )
  );
};
function getIsToday(date) {
  const today = /* @__PURE__ */ new Date();
  return [date.year, date.month, date.day].join("-") === [today.getFullYear(), today.getMonth() + 1, today.getDate()].join("-");
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/calendar-grid.js
var CalendarGrid = ({ state, ...props }) => {
  const { locale } = $2eb8e6d23f3d0cb0$export$43bb16f9c6d9e3f7();
  const { gridProps, headerProps, weekDays } = $2d54ee2ec11ddab8$export$cb95147730a423f5(props, state);
  const weeksInMonth = $ad063034c8620db8$export$ccc1b2479e7dd654(state.visibleRange.start, locale);
  return React19.createElement(
    "table",
    { ...gridProps },
    React19.createElement(
      "thead",
      { ...headerProps },
      React19.createElement("tr", null, weekDays.map((day, index) => React19.createElement("th", { key: index, className: "txt-compact-small-plus text-ui-fg-muted size-8 p-1 rounded-md" }, day)))
    ),
    React19.createElement("tbody", null, [...new Array(weeksInMonth).keys()].map((weekIndex) => React19.createElement("tr", { key: weekIndex }, state.getDatesInWeek(weekIndex).map((date, i) => date ? React19.createElement(CalendarCell, { key: i, state, date }) : React19.createElement("td", { key: i })))))
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/_internal-calendar.js
var InternalCalendar = (props) => {
  const { locale } = $2eb8e6d23f3d0cb0$export$43bb16f9c6d9e3f7();
  const state = $ea0ea6e62ae0d7ca$export$6d095e787d2b5e1f({
    ...props,
    locale,
    createCalendar: $84102b64e5ca022f$export$dd0bbc9b26defe37
  });
  const { calendarProps, prevButtonProps, nextButtonProps, title } = $a1efc3efb5426bf7$export$3ee915f8151bd4f1(props, state);
  return React20.createElement(
    "div",
    { ...calendarProps, className: "flex flex-col gap-y-2" },
    React20.createElement(
      "div",
      { className: "bg-ui-bg-field border-base grid grid-cols-[28px_1fr_28px] items-center gap-1 rounded-md border p-0.5" },
      React20.createElement(
        CalendarButton,
        { ...prevButtonProps },
        React20.createElement(TriangleLeftMini, null)
      ),
      React20.createElement(
        "div",
        { className: "flex items-center justify-center" },
        React20.createElement("h2", { className: "txt-compact-small-plus" }, title)
      ),
      React20.createElement(
        CalendarButton,
        { ...nextButtonProps },
        React20.createElement(TriangleRightMini, null)
      )
    ),
    React20.createElement(CalendarGrid, { state })
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/calendar/calendar.js
var React21 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/utils/calendar.js
function createCalendarDateTime(date) {
  return new $2aaf608024c21ca1$export$ca871e8dbb80966f(date.getFullYear(), date.getMonth() + 1, date.getDate(), date.getHours(), date.getMinutes(), date.getSeconds(), date.getMilliseconds());
}
function updateCalendarDateTime(date, value) {
  if (!date) {
    return createCalendarDateTime(value);
  }
  date.set({
    day: value.getDate(),
    month: value.getMonth() + 1,
    year: value.getFullYear(),
    hour: value.getHours(),
    minute: value.getMinutes(),
    second: value.getSeconds(),
    millisecond: value.getMilliseconds()
  });
  return date;
}
function createCalendarDate(date) {
  return new $2aaf608024c21ca1$export$99faa760c7908e4f(date.getFullYear(), date.getMonth() + 1, date.getDate());
}
function updateCalendarDate(date, value) {
  if (!date) {
    return createCalendarDate(value);
  }
  date.set({
    day: value.getDate(),
    month: value.getMonth() + 1,
    year: value.getFullYear()
  });
  return date;
}
var USES_TIME = /* @__PURE__ */ new Set(["hour", "minute", "second"]);
function createCalendarDateFromDate(date, granularity) {
  if (granularity && USES_TIME.has(granularity)) {
    return createCalendarDateTime(date);
  }
  return createCalendarDate(date);
}
function updateCalendarDateFromDate(date, value, granularity) {
  if (granularity && USES_TIME.has(granularity)) {
    return updateCalendarDateTime(date, value);
  }
  return updateCalendarDate(date, value);
}
function getDefaultCalendarDateFromDate(value, defaultValue, granularity) {
  if (value) {
    return createCalendarDateFromDate(value, granularity);
  }
  if (defaultValue) {
    return createCalendarDateFromDate(defaultValue, granularity);
  }
  return null;
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/time-input/time-input.js
var React23 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/date-segment/date-segment.js
var React22 = __toESM(require_react());
var DateSegment = ({ segment, state }) => {
  const ref = React22.useRef(null);
  const { segmentProps } = $d51706f903be7eab$export$1315d136e6f7581(segment, state, ref);
  const isComma = segment.type === "literal" && segment.text === ", ";
  if (isComma) {
    return React22.createElement("span", { className: "mx-1" });
  }
  return (
    /**
     * We wrap the segment in a span to prevent the segment from being
     * focused when the user clicks outside of the component.
     *
     * See: https://github.com/adobe/react-spectrum/issues/3164
     */
    React22.createElement(
      "span",
      null,
      React22.createElement("div", { ref, className: clx("transition-fg outline-none", "focus-visible:bg-ui-bg-interactive focus-visible:text-ui-fg-on-color", {
        "text-ui-fg-muted uppercase": segment.isPlaceholder,
        "text-ui-fg-muted": !segment.isEditable && !state.value
      }), ...segmentProps }, segment.text)
    )
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/time-input/time-input.js
var TimeInput = (props) => {
  const ref = React23.useRef(null);
  const { locale } = $2eb8e6d23f3d0cb0$export$43bb16f9c6d9e3f7();
  const state = $b822555cf9fe955c$export$fd53cef0cc796101({
    ...props,
    locale
  });
  const { fieldProps } = $7541913c0284d84b$export$4c842f6a241dc825(props, state, ref);
  return React23.createElement("div", { ref, ...fieldProps, "aria-label": "Time input", className: clx("bg-ui-bg-field shadow-borders-base txt-compact-small flex items-center rounded-md px-2 py-1", {
    "": props.isDisabled
  }) }, state.segments.map((segment, index) => {
    return React23.createElement(DateSegment, { key: index, segment, state });
  }));
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/date-picker/date-picker-button.js
var React24 = __toESM(require_react());
var DatePickerButton = React24.forwardRef(({ children, size = "base", ...props }, ref) => {
  const innerRef = React24.useRef(null);
  React24.useImperativeHandle(ref, () => innerRef.current);
  const { buttonProps } = $ac4318a9c075bb9f$export$ea18c227d4417cc3(props, innerRef);
  return React24.createElement("button", { type: "button", className: clx("text-ui-fg-muted transition-fg flex items-center justify-center border-r outline-none", "disabled:text-ui-fg-disabled", "hover:bg-ui-button-transparent-hover", "focus-visible:bg-ui-bg-interactive focus-visible:text-ui-fg-on-color", {
    "size-7": size === "small",
    "size-8": size === "base"
  }), "aria-label": "Open calendar", ...buttonProps }, children);
});
DatePickerButton.displayName = "DatePickerButton";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/date-picker/date-picker-clear-button.js
var React25 = __toESM(require_react());
var ALLOWED_KEYS = ["ArrowDown", "ArrowUp", "ArrowLeft", "ArrowRight"];
var DatePickerClearButton = React25.forwardRef(({ type = "button", className, children, ...props }, ref) => {
  const stopPropagation = (e) => {
    if (!ALLOWED_KEYS.includes(e.key)) {
      e.stopPropagation();
    }
  };
  return React25.createElement("button", { ref, type, className: clx("text-ui-fg-muted transition-fg flex size-full items-center justify-center outline-none", "hover:bg-ui-button-transparent-hover", "focus-visible:bg-ui-bg-interactive focus-visible:text-ui-fg-on-color", className), "aria-label": "Clear date", onKeyDown: stopPropagation, ...props }, children);
});
DatePickerClearButton.displayName = "DatePickerClearButton";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/date-picker/date-picker-field.js
var React26 = __toESM(require_react());
var datePickerFieldStyles = cva({
  base: "flex items-center tabular-nums",
  variants: {
    size: {
      small: "py-1",
      base: "py-1.5"
    }
  },
  defaultVariants: {
    size: "base"
  }
});
var DatePickerField = ({ size = "base", ...props }) => {
  const { locale } = $2eb8e6d23f3d0cb0$export$43bb16f9c6d9e3f7();
  const state = $8e7461aabf74661f$export$60e84778edff6d26({
    ...props,
    locale,
    createCalendar: $84102b64e5ca022f$export$dd0bbc9b26defe37
  });
  const ref = React26.useRef(null);
  const { fieldProps } = $7541913c0284d84b$export$5591b0b878c1a989(props, state, ref);
  return React26.createElement("div", { ref, "aria-label": "Date input", className: datePickerFieldStyles({ size }), ...fieldProps }, state.segments.map((segment, index) => {
    return React26.createElement(DateSegment, { key: index, segment, state });
  }));
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/date-picker/date-picker.js
var datePickerStyles = (isOpen, isInvalid, value) => cva({
  base: clx("bg-ui-bg-field shadow-borders-base txt-compact-small text-ui-fg-base transition-fg grid items-center gap-2 overflow-hidden rounded-md h-fit", "focus-within:shadow-borders-interactive-with-active focus-visible:shadow-borders-interactive-with-active", "aria-[invalid=true]:shadow-borders-error invalid:shadow-borders-error", {
    "shadow-borders-interactive-with-active": isOpen,
    "shadow-borders-error": isInvalid,
    "pr-2": !value
  }),
  variants: {
    size: {
      small: clx("grid-cols-[28px_1fr]", {
        "grid-cols-[28px_1fr_28px]": !!value
      }),
      base: clx("grid-cols-[32px_1fr]", {
        "grid-cols-[32px_1fr_32px]": !!value
      })
    }
  }
});
var HAS_TIME = /* @__PURE__ */ new Set(["hour", "minute", "second"]);
var DatePicker = React27.forwardRef(({ size = "base", shouldCloseOnSelect = true, className, modal = false, ...props }, ref) => {
  const [value, setValue] = React27.useState(getDefaultCalendarDateFromDate(props.value, props.defaultValue, props.granularity));
  const innerRef = React27.useRef(null);
  React27.useImperativeHandle(ref, () => innerRef.current);
  const contentRef = React27.useRef(null);
  const _props = convertProps(props, setValue);
  const state = $28aae683a942e141$export$87194bb378cc3ac2({
    ..._props,
    shouldCloseOnSelect
  });
  const { groupProps, fieldProps, buttonProps, dialogProps, calendarProps } = $6902a2bb26917a07$export$42df105a73306d51(_props, state, innerRef);
  React27.useEffect(() => {
    setValue(props.value ? updateCalendarDateFromDate(value, props.value, props.granularity) : null);
    state.setValue(props.value ? updateCalendarDateFromDate(value, props.value, props.granularity) : null);
  }, [props.value]);
  function clear(e) {
    var _a;
    e.preventDefault();
    e.stopPropagation();
    (_a = props.onChange) === null || _a === void 0 ? void 0 : _a.call(props, null);
    state.setValue(null);
  }
  $e260d131964da0f9$export$872b660ac5a1ff98({
    ref: contentRef,
    onInteractOutside: () => {
      state.setOpen(false);
    }
  });
  const hasTime = props.granularity && HAS_TIME.has(props.granularity);
  const Icon = hasTime ? Clock : CalendarMini;
  return React27.createElement(
    Popover,
    { modal, open: state.isOpen, onOpenChange: state.setOpen },
    React27.createElement(
      Popover.Anchor,
      { asChild: true },
      React27.createElement(
        "div",
        { ref, className: clx(datePickerStyles(state.isOpen, state.isInvalid, state.value)({ size }), className), ...groupProps },
        React27.createElement(
          DatePickerButton,
          { ...buttonProps, size },
          React27.createElement(Icon, null)
        ),
        React27.createElement(DatePickerField, { ...fieldProps, size }),
        !!state.value && React27.createElement(
          DatePickerClearButton,
          { onClick: clear },
          React27.createElement(XMarkMini, null)
        )
      )
    ),
    React27.createElement(
      Popover.Content,
      { ref: contentRef, ...dialogProps, className: "flex flex-col divide-y p-0" },
      React27.createElement(
        "div",
        { className: "p-3" },
        React27.createElement(InternalCalendar, { autoFocus: true, ...calendarProps })
      ),
      state.hasTime && React27.createElement(
        "div",
        { className: "p-3" },
        React27.createElement(TimeInput, { value: state.timeValue, onChange: (value2) => state.setTimeValue(value2), hourCycle: props.hourCycle })
      )
    )
  );
});
DatePicker.displayName = "DatePicker";
function convertProps(props, setValue) {
  const { minValue, maxValue, isDateUnavailable: _isDateUnavailable, onChange: _onChange, value: __value__, defaultValue: __defaultValue__, ...rest } = props;
  const onChange = (value) => {
    setValue(value);
    _onChange === null || _onChange === void 0 ? void 0 : _onChange(value ? value.toDate($ad063034c8620db8$export$aa8b41735afcabd2()) : null);
  };
  const isDateUnavailable = (date) => {
    const _date = date.toDate($ad063034c8620db8$export$aa8b41735afcabd2());
    return _isDateUnavailable ? _isDateUnavailable(_date) : false;
  };
  return {
    ...rest,
    onChange,
    isDateUnavailable,
    minValue: minValue ? createCalendarDateFromDate(minValue, props.granularity) : minValue,
    maxValue: maxValue ? createCalendarDateFromDate(maxValue, props.granularity) : maxValue
  };
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/divider/divider.js
var React28 = __toESM(require_react());
var Divider = ({ orientation = "horizontal", variant = "solid", className, ...props }) => {
  return React28.createElement("div", { "aria-orientation": orientation, role: "separator", className: clx("border-ui-border-base", {
    "w-full border-t": orientation === "horizontal" && variant === "solid",
    "h-full border-l": orientation === "vertical" && variant === "solid",
    "bg-transparent": variant === "dashed",
    "h-px w-full bg-[linear-gradient(90deg,var(--border-strong)_1px,transparent_1px)] bg-[length:4px_1px]": variant === "dashed" && orientation === "horizontal",
    "h-full w-px bg-[linear-gradient(0deg,var(--border-strong)_1px,transparent_1px)] bg-[length:1px_4px]": variant === "dashed" && orientation === "vertical"
  }, className), ...props });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/dropdown-menu/dropdown-menu.js
var React29 = __toESM(require_react());
var Root4 = dist_exports8.Root;
Root4.displayName = "DropdownMenu";
var Trigger2 = dist_exports8.Trigger;
Trigger2.displayName = "DropdownMenu.Trigger";
var Group = dist_exports8.Group;
Group.displayName = "DropdownMenu.Group";
var SubMenu = dist_exports8.Sub;
SubMenu.displayName = "DropdownMenu.SubMenu";
var RadioGroup = dist_exports8.RadioGroup;
RadioGroup.displayName = "DropdownMenu.RadioGroup";
var SubMenuTrigger = React29.forwardRef(({ className, children, ...props }, ref) => React29.createElement(
  dist_exports8.SubTrigger,
  { ref, className: clx("bg-ui-bg-component text-ui-fg-base txt-compact-small relative flex cursor-pointer select-none items-center rounded-md px-2 py-1.5 outline-none transition-colors", "focus-visible:bg-ui-bg-component-hover focus:bg-ui-bg-component-hover", "active:bg-ui-bg-component-hover", "data-[disabled]:text-ui-fg-disabled data-[disabled]:pointer-events-none", "data-[state=open]:!bg-ui-bg-component-hover", className), ...props },
  children,
  React29.createElement(ChevronRightMini, { className: "text-ui-fg-muted ml-auto" })
));
SubMenuTrigger.displayName = "DropdownMenu.SubMenuTrigger";
var SubMenuContent = React29.forwardRef(({ className, collisionPadding = 8, ...props }, ref) => React29.createElement(
  dist_exports8.Portal,
  null,
  React29.createElement(dist_exports8.SubContent, { ref, collisionPadding, className: clx("bg-ui-bg-component text-ui-fg-base shadow-elevation-flyout max-h-[var(--radix-popper-available-height)] min-w-[220px] overflow-hidden rounded-lg p-1", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2", className), ...props })
));
SubMenuContent.displayName = "DropdownMenu.SubMenuContent";
var Content2 = React29.forwardRef(({
  className,
  /**
   * The space in pixels between the dropdown menu and its trigger element.
   */
  sideOffset = 8,
  /**
   * The distance in pixels from the boundary edges where collision detection should occur.
   */
  collisionPadding = 8,
  /**
   * The alignment of the dropdown menu relative to its trigger element.
   *
   * @defaultValue center
   */
  align = "center",
  ...props
}, ref) => React29.createElement(
  dist_exports8.Portal,
  null,
  React29.createElement(dist_exports8.Content, { ref, sideOffset, align, collisionPadding, className: clx("bg-ui-bg-component text-ui-fg-base shadow-elevation-flyout max-h-[var(--radix-popper-available-height)] min-w-[220px] overflow-hidden rounded-lg p-1", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2", className), ...props })
));
Content2.displayName = "DropdownMenu.Content";
var Item = React29.forwardRef(({ className, ...props }, ref) => React29.createElement(dist_exports8.Item, { ref, className: clx("bg-ui-bg-component text-ui-fg-base txt-compact-small relative flex cursor-pointer select-none items-center rounded-md px-2 py-1.5 outline-none transition-colors", "focus-visible:bg-ui-bg-component-hover focus:bg-ui-bg-component-hover", "active:bg-ui-bg-component-hover", "data-[disabled]:text-ui-fg-disabled data-[disabled]:pointer-events-none", className), ...props }));
Item.displayName = "DropdownMenu.Item";
var CheckboxItem = React29.forwardRef(({ className, children, checked, ...props }, ref) => React29.createElement(
  dist_exports8.CheckboxItem,
  { ref, className: clx("bg-ui-bg-component text-ui-fg-base txt-compact-small relative flex cursor-pointer select-none items-center rounded-md py-1.5 pl-[31px] pr-2 outline-none transition-colors", "focus-visible:bg-ui-bg-component-hover focus:bg-ui-bg-component-hover", "active:bg-ui-bg-component-hover", "data-[disabled]:text-ui-fg-disabled data-[disabled]:pointer-events-none", "data-[state=checked]:txt-compact-small-plus", className), checked, ...props },
  React29.createElement(
    "span",
    { className: "absolute left-2 flex size-[15px] items-center justify-center" },
    React29.createElement(
      dist_exports8.ItemIndicator,
      null,
      React29.createElement(CheckMini, null)
    )
  ),
  children
));
CheckboxItem.displayName = "DropdownMenu.CheckboxItem";
var RadioItem = React29.forwardRef(({ className, children, ...props }, ref) => React29.createElement(
  dist_exports8.RadioItem,
  { ref, className: clx("bg-ui-bg-component txt-compact-small relative flex cursor-pointer select-none items-center rounded-md py-1.5 pl-[31px] pr-2 outline-none transition-colors", "focus-visible:bg-ui-bg-component-hover focus:bg-ui-bg-component-hover", "active:bg-ui-bg-component-hover", "data-[disabled]:text-ui-fg-disabled data-[disabled]:pointer-events-none", "data-[state=checked]:txt-compact-small-plus", className), ...props },
  React29.createElement(
    "span",
    { className: "absolute left-2 flex size-[15px] items-center justify-center" },
    React29.createElement(
      dist_exports8.ItemIndicator,
      null,
      React29.createElement(EllipseMiniSolid, { className: "text-ui-fg-base" })
    )
  ),
  children
));
RadioItem.displayName = "DropdownMenu.RadioItem";
var Label = React29.forwardRef(({ className, ...props }, ref) => React29.createElement(dist_exports8.Label, { ref, className: clx("text-ui-fg-subtle txt-compact-xsmall-plus", className), ...props }));
Label.displayName = "DropdownMenu.Label";
var Separator = React29.forwardRef(({ className, ...props }, ref) => React29.createElement(dist_exports8.Separator, { ref, className: clx("bg-ui-border-component border-t-ui-border-menu-top border-b-ui-border-menu-bot -mx-1 my-1 h-0.5 border-b border-t", className), ...props }));
Separator.displayName = "DropdownMenu.Separator";
var Shortcut = ({ className, ...props }) => {
  return React29.createElement("span", { className: clx("text-ui-fg-subtle txt-compact-small ml-auto tracking-widest", className), ...props });
};
Shortcut.displayName = "DropdownMenu.Shortcut";
var Hint = ({ className, ...props }) => {
  return React29.createElement("span", { className: clx("text-ui-fg-subtle txt-compact-small ml-auto tracking-widest", className), ...props });
};
Hint.displayName = "DropdownMenu.Hint";
var DropdownMenu = Object.assign(Root4, {
  Trigger: Trigger2,
  Group,
  SubMenu,
  SubMenuContent,
  SubMenuTrigger,
  Content: Content2,
  Item,
  CheckboxItem,
  RadioGroup,
  RadioItem,
  Label,
  Separator,
  Shortcut,
  Hint
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/focus-modal/focus-modal.js
var React30 = __toESM(require_react());
var FocusModalRoot = (props) => {
  return React30.createElement(dist_exports7.Root, { ...props });
};
FocusModalRoot.displayName = "FocusModal";
var FocusModalTrigger = React30.forwardRef((props, ref) => {
  return React30.createElement(dist_exports7.Trigger, { ref, ...props });
});
FocusModalTrigger.displayName = "FocusModal.Trigger";
var FocusModalClose = dist_exports7.Close;
FocusModalClose.displayName = "FocusModal.Close";
var FocusModalPortal = (props) => {
  return React30.createElement(dist_exports7.DialogPortal, { ...props });
};
FocusModalPortal.displayName = "FocusModal.Portal";
var FocusModalOverlay = React30.forwardRef(({ className, ...props }, ref) => {
  return React30.createElement(dist_exports7.Overlay, { ref, className: clx("bg-ui-bg-overlay fixed inset-0", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0", className), ...props });
});
FocusModalOverlay.displayName = "FocusModal.Overlay";
var FocusModalContent = React30.forwardRef(({ className, overlayProps, portalProps, ...props }, ref) => {
  return React30.createElement(
    FocusModalPortal,
    { ...portalProps },
    React30.createElement(FocusModalOverlay, { ...overlayProps }),
    React30.createElement(dist_exports7.Content, { ref, className: clx("bg-ui-bg-base shadow-elevation-modal fixed inset-2 flex flex-col overflow-hidden rounded-lg border outline-none", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=open]:slide-in-from-bottom-0 data-[state=closed]:slide-in-from-bottom-2  duration-200", className), ...props })
  );
});
FocusModalContent.displayName = "FocusModal.Content";
var FocusModalHeader = React30.forwardRef(({ children, className, ...props }, ref) => {
  return React30.createElement(
    "div",
    { ref, className: clx("border-ui-border-base flex items-center justify-between gap-x-4 border-b px-4 py-2", className), ...props },
    React30.createElement(
      "div",
      { className: "flex items-center gap-x-2" },
      React30.createElement(
        dist_exports7.Close,
        { asChild: true },
        React30.createElement(
          IconButton,
          { size: "small", type: "button", variant: "transparent" },
          React30.createElement(XMark, null)
        )
      ),
      React30.createElement(Kbd, null, "esc")
    ),
    children
  );
});
FocusModalHeader.displayName = "FocusModal.Header";
var FocusModalFooter = React30.forwardRef(({ children, className, ...props }, ref) => {
  return React30.createElement("div", { ref, className: clx("border-ui-border-base flex items-center justify-end gap-x-2 border-t p-4", className), ...props }, children);
});
FocusModalFooter.displayName = "FocusModal.Footer";
var FocusModalTitle = React30.forwardRef(({ className, ...props }, ref) => {
  return React30.createElement(dist_exports7.Title, { ref, ...props });
});
FocusModalTitle.displayName = "FocusModal.Title";
var FocusModalDescription = dist_exports7.Description;
FocusModalDescription.displayName = "FocusModal.Description";
var FocusModalBody = React30.forwardRef(({ className, ...props }, ref) => {
  return React30.createElement("div", { ref, className: clx("flex-1", className), ...props });
});
FocusModalBody.displayName = "FocusModal.Body";
var FocusModal = Object.assign(FocusModalRoot, {
  Trigger: FocusModalTrigger,
  Title: FocusModalTitle,
  Description: FocusModalDescription,
  Content: FocusModalContent,
  Header: FocusModalHeader,
  Body: FocusModalBody,
  Close: FocusModalClose,
  Footer: FocusModalFooter
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/heading/heading.js
var React31 = __toESM(require_react());
var headingVariants = cva({
  base: "font-sans font-medium",
  variants: {
    level: {
      h1: "h1-core",
      h2: "h2-core",
      h3: "h3-core"
    }
  },
  defaultVariants: {
    level: "h1"
  }
});
var Heading = ({
  /**
   * The heading level which specifies which heading element is used.
   */
  level = "h1",
  className,
  ...props
}) => {
  const Component = level || "h1";
  return React31.createElement(Component, { className: clx(headingVariants({ level }), className), ...props });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/hint/hint.js
var React32 = __toESM(require_react());
var hintVariants = cva({
  base: "txt-small",
  variants: {
    variant: {
      info: "text-ui-fg-subtle",
      error: "text-ui-fg-error grid grid-cols-[20px_1fr] gap-1 items-start"
    }
  },
  defaultVariants: {
    variant: "info"
  }
});
var Hint2 = React32.forwardRef(({
  className,
  /**
   * The hint's style.
   */
  variant = "info",
  children,
  ...props
}, ref) => {
  return React32.createElement(
    "span",
    { ref, className: clx(hintVariants({ variant }), className), ...props },
    variant === "error" && React32.createElement(
      "div",
      { className: "size-5 flex items-center justify-center" },
      React32.createElement(ExclamationCircleSolid, null)
    ),
    children
  );
});
Hint2.displayName = "Hint";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/inline-tip/inline-tip.js
var React33 = __toESM(require_react());
var InlineTip = React33.forwardRef(({
  /**
   * The variant of the tip.
   */
  variant = "info",
  /**
   * The label to display in the tip.
   */
  label,
  className,
  children,
  ...props
}, ref) => {
  return React33.createElement(
    "div",
    { ref, className: clx("bg-ui-bg-component txt-small text-ui-fg-subtle grid grid-cols-[4px_1fr] items-start gap-3 rounded-lg border p-3", className), ...props },
    React33.createElement("div", { role: "presentation", className: clx("bg-ui-tag-neutral-icon h-full w-1 rounded-full", {
      "bg-ui-tag-orange-icon": variant === "warning",
      "bg-ui-tag-red-icon": variant === "error",
      "bg-ui-tag-green-icon": variant === "success"
    }) }),
    React33.createElement(
      "div",
      { className: "text-pretty" },
      React33.createElement(
        "strong",
        { className: "txt-small-plus text-ui-fg-base" },
        label,
        ":"
      ),
      " ",
      children
    )
  );
});
InlineTip.displayName = "InlineTip";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/input/input.js
var React34 = __toESM(require_react());
var inputBaseStyles = clx("caret-ui-fg-base bg-ui-bg-field hover:bg-ui-bg-field-hover shadow-borders-base placeholder-ui-fg-muted text-ui-fg-base transition-fg relative w-full appearance-none rounded-md outline-none", "focus-visible:shadow-borders-interactive-with-active", "disabled:text-ui-fg-disabled disabled:!bg-ui-bg-disabled disabled:placeholder-ui-fg-disabled disabled:cursor-not-allowed", "aria-[invalid=true]:!shadow-borders-error  invalid:!shadow-borders-error");
var inputVariants = cva({
  base: clx(inputBaseStyles, "[&::--webkit-search-cancel-button]:hidden [&::-webkit-search-cancel-button]:hidden [&::-webkit-search-decoration]:hidden"),
  variants: {
    size: {
      base: "txt-compact-small h-8 px-2 py-1.5",
      small: "txt-compact-small h-7 px-2 py-1"
    }
  },
  defaultVariants: {
    size: "base"
  }
});
var Input = React34.forwardRef(({
  className,
  type,
  /**
   * The input's size.
   */
  size = "base",
  ...props
}, ref) => {
  const [typeState, setTypeState] = React34.useState(type);
  const isPassword = type === "password";
  const isSearch = type === "search";
  return React34.createElement(
    "div",
    { className: "relative" },
    React34.createElement("input", { ref, type: isPassword ? typeState : type, className: clx(inputVariants({ size }), {
      "pl-8": isSearch && size === "base",
      "pr-8": isPassword && size === "base",
      "pl-7": isSearch && size === "small",
      "pr-7": isPassword && size === "small"
    }, className), ...props }),
    isSearch && React34.createElement(
      "div",
      { className: clx("text-ui-fg-muted pointer-events-none absolute bottom-0 left-0 flex items-center justify-center", {
        "h-8 w-8": size === "base",
        "h-7 w-7": size === "small"
      }), role: "img" },
      React34.createElement(MagnifyingGlassMini, null)
    ),
    isPassword && React34.createElement(
      "div",
      { className: clx("absolute bottom-0 right-0 flex items-center justify-center border-l", {
        "h-8 w-8": size === "base",
        "h-7 w-7": size === "small"
      }) },
      React34.createElement(
        "button",
        { className: "text-ui-fg-muted hover:text-ui-fg-base focus-visible:text-ui-fg-base focus-visible:shadow-borders-interactive-w-focus active:text-ui-fg-base h-fit w-fit rounded-sm outline-none transition-all", type: "button", onClick: () => {
          setTypeState(typeState === "password" ? "text" : "password");
        } },
        React34.createElement("span", { className: "sr-only" }, typeState === "password" ? "Show password" : "Hide password"),
        typeState === "password" ? React34.createElement(Eye, null) : React34.createElement(EyeSlash, null)
      )
    )
  );
});
Input.displayName = "Input";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/label/label.js
var React35 = __toESM(require_react());
var labelVariants = cva({
  base: "font-sans",
  variants: {
    size: {
      xsmall: "txt-compact-xsmall",
      small: "txt-compact-small",
      base: "txt-compact-medium",
      large: "txt-compact-large"
    },
    weight: {
      regular: "font-normal",
      plus: "font-medium"
    }
  },
  defaultVariants: {
    size: "base",
    weight: "regular"
  }
});
var Label2 = React35.forwardRef(({
  className,
  /**
   * The label's size.
   */
  size = "base",
  /**
   * The label's font weight.
   */
  weight = "regular",
  ...props
}, ref) => {
  return React35.createElement(dist_exports9.Root, { ref, className: clx(labelVariants({ size, weight }), className), ...props });
});
Label2.displayName = "Label";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/progress-tabs/progress-tabs.js
var React36 = __toESM(require_react());
var ProgressTabsRoot = (props) => {
  return React36.createElement(dist_exports14.Root, { ...props });
};
ProgressTabsRoot.displayName = "ProgressTabs";
var ProgressIndicator = ({ status, className, ...props }) => {
  const Icon = React36.useMemo(() => {
    switch (status) {
      case "not-started":
        return CircleDottedLine;
      case "in-progress":
        return CircleHalfSolid;
      case "completed":
        return CheckCircleSolid;
      default:
        return CircleDottedLine;
    }
  }, [status]);
  return React36.createElement(
    "span",
    { className: clx("text-ui-fg-muted group-data-[state=active]/trigger:text-ui-fg-interactive", className), ...props },
    React36.createElement(Icon, null)
  );
};
ProgressIndicator.displayName = "ProgressTabs.ProgressIndicator";
var ProgressTabsTrigger = React36.forwardRef(({ className, children, status = "not-started", ...props }, ref) => React36.createElement(
  dist_exports14.Trigger,
  { ref, className: clx("txt-compact-small-plus transition-fg text-ui-fg-muted bg-ui-bg-subtle border-r-ui-border-base inline-flex h-[52px] w-full max-w-[200px] flex-1 items-center gap-x-2 border-r px-4 text-left outline-none", "group/trigger overflow-hidden text-ellipsis whitespace-nowrap", "disabled:bg-ui-bg-disabled disabled:text-ui-fg-muted", "hover:bg-ui-bg-subtle-hover", "focus-visible:bg-ui-bg-base focus:z-[1]", "data-[state=active]:text-ui-fg-base data-[state=active]:bg-ui-bg-base", className), ...props },
  React36.createElement(ProgressIndicator, { status }),
  children
));
ProgressTabsTrigger.displayName = "ProgressTabs.Trigger";
var ProgressTabsList = React36.forwardRef(({ className, ...props }, ref) => React36.createElement(dist_exports14.List, { ref, className: clx("flex items-center", className), ...props }));
ProgressTabsList.displayName = "ProgressTabs.List";
var ProgressTabsContent = React36.forwardRef(({ className, ...props }, ref) => {
  return React36.createElement(dist_exports14.Content, { ref, className: clx("outline-none", className), ...props });
});
ProgressTabsContent.displayName = "ProgressTabs.Content";
var ProgressTabs = Object.assign(ProgressTabsRoot, {
  Trigger: ProgressTabsTrigger,
  List: ProgressTabsList,
  Content: ProgressTabsContent
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/prompt/prompt.js
var React37 = __toESM(require_react());
var PromptContext = React37.createContext({
  variant: "danger"
});
var usePromptContext = () => {
  const context = React37.useContext(PromptContext);
  if (!context) {
    throw new Error("usePromptContext must be used within a PromptProvider");
  }
  return context;
};
var PromptProvider = ({ variant, children }) => {
  return React37.createElement(PromptContext.Provider, { value: { variant } }, children);
};
var Root5 = ({
  /**
   * The variant of the prompt.
   */
  variant = "danger",
  ...props
}) => {
  return React37.createElement(
    PromptProvider,
    { variant },
    React37.createElement(dist_exports4.Root, { ...props })
  );
};
Root5.displayName = "Prompt";
var Trigger3 = dist_exports4.Trigger;
Trigger3.displayName = "Prompt.Trigger";
var Portal = (props) => {
  return React37.createElement(dist_exports4.AlertDialogPortal, { ...props });
};
Portal.displayName = "Prompt.Portal";
var Overlay = React37.forwardRef(({ className, ...props }, ref) => {
  return React37.createElement(dist_exports4.Overlay, { ref, className: clx("bg-ui-bg-overlay fixed inset-0", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0", className), ...props });
});
Overlay.displayName = "Prompt.Overlay";
var Title = React37.forwardRef(({ className, children, ...props }, ref) => {
  return React37.createElement(
    dist_exports4.Title,
    { ref, className: clx(className), ...props, asChild: true },
    React37.createElement(Heading, { level: "h2", className: "text-ui-fg-base" }, children)
  );
});
Title.displayName = "Prompt.Title";
var Content3 = React37.forwardRef(({ className, ...props }, ref) => {
  return React37.createElement(
    Portal,
    null,
    React37.createElement(Overlay, null),
    React37.createElement(dist_exports4.Content, { ref, className: clx("bg-ui-bg-base shadow-elevation-flyout fixed left-[50%] top-[50%] flex w-full max-w-[400px] translate-x-[-50%] translate-y-[-50%] flex-col rounded-lg border focus-visible:outline-none", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%] data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%] duration-200", className), ...props })
  );
});
Content3.displayName = "Prompt.Content";
var Description = React37.forwardRef(({ className, ...props }, ref) => {
  return React37.createElement(dist_exports4.Description, { ref, className: clx("text-ui-fg-subtle txt-compact-medium", className), ...props });
});
Description.displayName = "Prompt.Description";
var Action = React37.forwardRef(({ className, children, type, ...props }, ref) => {
  const { variant } = usePromptContext();
  return React37.createElement(
    dist_exports4.Action,
    { ref, className, ...props, asChild: true },
    React37.createElement(Button, { size: "small", type, variant: variant === "danger" ? "danger" : "primary" }, children)
  );
});
Action.displayName = "Prompt.Action";
var Cancel = React37.forwardRef(({ className, children, ...props }, ref) => {
  return React37.createElement(
    dist_exports4.Cancel,
    { ref, className: clx(className), ...props, asChild: true },
    React37.createElement(Button, { size: "small", variant: "secondary" }, children)
  );
});
Cancel.displayName = "Prompt.Cancel";
var Header2 = ({ className, ...props }) => {
  return React37.createElement("div", { className: clx("flex flex-col gap-y-1 px-6 pt-6", className), ...props });
};
Header2.displayName = "Prompt.Header";
var Footer = ({ className, ...props }) => {
  return React37.createElement("div", { className: clx("flex items-center justify-end gap-x-2 p-6", className), ...props });
};
Footer.displayName = "Prompt.Footer";
var Prompt = Object.assign(Root5, {
  Trigger: Trigger3,
  Content: Content3,
  Title,
  Description,
  Action,
  Cancel,
  Header: Header2,
  Footer
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/radio-group/radio-group.js
var React38 = __toESM(require_react());
var Root6 = React38.forwardRef(({ className, ...props }, ref) => {
  return React38.createElement(dist_exports11.Root, { className: clx("grid gap-2", className), ...props, ref });
});
Root6.displayName = "RadioGroup";
var Indicator = React38.forwardRef(({ className, ...props }, ref) => {
  return React38.createElement(
    dist_exports11.Indicator,
    { ref, className: clx("flex items-center justify-center", className), ...props },
    React38.createElement("div", { className: clx("bg-ui-bg-base shadow-details-contrast-on-bg-interactive h-1.5 w-1.5 rounded-full") })
  );
});
Indicator.displayName = "RadioGroup.Indicator";
var Item2 = React38.forwardRef(({ className, ...props }, ref) => {
  return React38.createElement(
    dist_exports11.Item,
    { ref, className: clx("group relative flex h-5 w-5 items-center justify-center outline-none", className), ...props },
    React38.createElement(
      "div",
      { className: clx("shadow-borders-base bg-ui-bg-base transition-fg flex h-[14px] w-[14px] items-center justify-center rounded-full", "group-hover:group-enabled:group-data-[state=unchecked]:bg-ui-bg-base-hover", "group-data-[state=checked]:bg-ui-bg-interactive group-data-[state=checked]:shadow-borders-interactive-with-shadow", "group-focus-visible:!shadow-borders-interactive-with-focus", "group-disabled:cursor-not-allowed group-disabled:opacity-50") },
      React38.createElement(Indicator, null)
    )
  );
});
Item2.displayName = "RadioGroup.Item";
var ChoiceBox = React38.forwardRef(({
  className,
  id,
  /**
   * The label for the radio button.
   */
  label,
  /**
   * The description for the radio button.
   */
  description,
  /**
   * The value of the radio button.
   */
  value,
  ...props
}, ref) => {
  const generatedId = React38.useId();
  if (!id) {
    id = generatedId;
  }
  const descriptionId = `${id}-description`;
  return React38.createElement(
    dist_exports11.Item,
    { ref, className: clx("shadow-borders-base bg-ui-bg-base focus-visible:shadow-borders-interactive-with-focus transition-fg group flex items-start gap-x-2 rounded-lg p-3 outline-none", "hover:enabled:bg-ui-bg-base-hover", "data-[state=checked]:shadow-borders-interactive-with-shadow", "group-disabled:cursor-not-allowed group-disabled:opacity-50", className), ...props, value, id, "aria-describedby": descriptionId },
    React38.createElement(
      "div",
      { className: "flex h-5 w-5 items-center justify-center" },
      React38.createElement(
        "div",
        { className: clx("shadow-borders-base bg-ui-bg-base group-data-[state=checked]:bg-ui-bg-interactive group-data-[state=checked]:shadow-borders-interactive-with-shadow transition-fg flex h-3.5 w-3.5 items-center justify-center rounded-full", "group-hover:group-enabled:group-data-[state=unchecked]:bg-ui-bg-base-hover") },
        React38.createElement(Indicator, null)
      )
    ),
    React38.createElement(
      "div",
      { className: "flex flex-col items-start" },
      React38.createElement(Label2, { htmlFor: id, size: "small", weight: "plus", className: "group-disabled:text-ui-fg-disabled cursor-pointer group-disabled:cursor-not-allowed" }, label),
      React38.createElement(Hint2, { className: "txt-small text-ui-fg-subtle group-disabled:text-ui-fg-disabled text-left", id: descriptionId }, description)
    )
  );
});
ChoiceBox.displayName = "RadioGroup.ChoiceBox";
var RadioGroup2 = Object.assign(Root6, { Item: Item2, ChoiceBox });

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/select/select.js
var React39 = __toESM(require_react());
var SelectContext = React39.createContext(null);
var useSelectContext = () => {
  const context = React39.useContext(SelectContext);
  if (context === null) {
    throw new Error("useSelectContext must be used within a SelectProvider");
  }
  return context;
};
var Root7 = ({
  children,
  /**
   * The select's size.
   */
  size = "base",
  ...props
}) => {
  return React39.createElement(
    SelectContext.Provider,
    { value: React39.useMemo(() => ({ size }), [size]) },
    React39.createElement(dist_exports12.Root, { ...props }, children)
  );
};
Root7.displayName = "Select";
var Group2 = dist_exports12.Group;
Group2.displayName = "Select.Group";
var Value2 = dist_exports12.Value;
Value2.displayName = "Select.Value";
var triggerVariants = cva({
  base: clx("bg-ui-bg-field shadow-buttons-neutral transition-fg flex w-full select-none items-center justify-between rounded-md outline-none", "data-[placeholder]:text-ui-fg-muted text-ui-fg-base", "hover:bg-ui-bg-field-hover", "focus-visible:shadow-borders-interactive-with-active data-[state=open]:!shadow-borders-interactive-with-active", "aria-[invalid=true]:border-ui-border-error aria-[invalid=true]:shadow-borders-error", "invalid:border-ui-border-error invalid:shadow-borders-error", "disabled:!bg-ui-bg-disabled disabled:!text-ui-fg-disabled", "group/trigger"),
  variants: {
    size: {
      base: "h-8 px-2 py-1.5 txt-compact-small",
      small: "h-7 px-2 py-1 txt-compact-small"
    }
  }
});
var Trigger4 = React39.forwardRef(({ className, children, ...props }, ref) => {
  const { size } = useSelectContext();
  return React39.createElement(
    dist_exports12.Trigger,
    { ref, className: clx(triggerVariants({ size }), className), ...props },
    children,
    React39.createElement(
      dist_exports12.Icon,
      { asChild: true },
      React39.createElement(TrianglesMini, { className: "text-ui-fg-muted group-disabled/trigger:text-ui-fg-disabled" })
    )
  );
});
Trigger4.displayName = "Select.Trigger";
var Content4 = React39.forwardRef(({
  className,
  children,
  onScroll,
  /**
   * Whether to show the select items below (`popper`) or over (`item-aligned`) the select input.
   */
  position = "popper",
  /**
   * The distance of the content pop-up in pixels from the select input. Only available when position is set to popper.
   */
  sideOffset = 8,
  /**
   * The distance in pixels from the boundary edges where collision detection should occur. Only available when position is set to popper.
   */
  collisionPadding = 24,
  ...props
}, ref) => React39.createElement(
  dist_exports12.Portal,
  null,
  React39.createElement(
    dist_exports12.Content,
    { ref, className: clx("bg-ui-bg-component text-ui-fg-base shadow-elevation-flyout relative max-h-[200px] min-w-[var(--radix-select-trigger-width)] overflow-hidden rounded-lg", "data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95", "data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95", "data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2", {
      "data-[side=bottom]:translate-y-1 data-[side=left]:-translate-x-1 data-[side=right]:translate-x-1 data-[side=top]:-translate-y-1": position === "popper"
    }, className), position, sideOffset, collisionPadding, ...props },
    React39.createElement(dist_exports12.Viewport, { className: clx("p-1", position === "popper" && "h-[var(--radix-select-trigger-height)] w-full min-w-[var(--radix-select-trigger-width)]"), onScroll }, children)
  )
));
Content4.displayName = "Select.Content";
var Label3 = React39.forwardRef(({ className, ...props }, ref) => React39.createElement(dist_exports12.Label, { ref, className: clx("txt-compact-xsmall-plus text-ui-fg-muted px-2 py-1.5", className), ...props }));
Label3.displayName = "Select.Label";
var Item3 = React39.forwardRef(({ className, children, ...props }, ref) => {
  return React39.createElement(
    dist_exports12.Item,
    { ref, className: clx("bg-ui-bg-component txt-compact-small grid cursor-pointer grid-cols-[15px_1fr] items-center gap-x-2 rounded-[4px] px-2 py-1.5 outline-none transition-colors", "focus-visible:bg-ui-bg-component-hover", "active:bg-ui-bg-component-pressed", "data-[state=checked]:txt-compact-small-plus", "disabled:text-ui-fg-disabled", className), ...props },
    React39.createElement(
      "span",
      { className: "flex h-[15px] w-[15px] items-center justify-center" },
      React39.createElement(
        dist_exports12.ItemIndicator,
        { className: "flex items-center justify-center" },
        React39.createElement(Check, null)
      )
    ),
    React39.createElement(dist_exports12.ItemText, { className: "flex-1 truncate" }, children)
  );
});
Item3.displayName = "Select.Item";
var Separator2 = React39.forwardRef(({ className, ...props }, ref) => React39.createElement(dist_exports12.Separator, { ref, className: clx("bg-ui-border-component border-t-ui-border-menu-top border-b-ui-border-menu-bot -mx-1 my-1 h-0.5 border-b border-t", className), ...props }));
Separator2.displayName = "Select.Separator";
var Select = Object.assign(Root7, {
  Group: Group2,
  Value: Value2,
  Trigger: Trigger4,
  Content: Content4,
  Label: Label3,
  Item: Item3,
  Separator: Separator2
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/status-badge/status-badge.js
var React40 = __toESM(require_react());
var statusBadgeVariants = cva({
  base: "flex items-center justify-center w-5 h-[18px] [&_div]:w-2 [&_div]:h-2 [&_div]:rounded-sm",
  variants: {
    color: {
      green: "[&_div]:bg-ui-tag-green-icon",
      red: "[&_div]:bg-ui-tag-red-icon",
      orange: "[&_div]:bg-ui-tag-orange-icon",
      blue: "[&_div]:bg-ui-tag-blue-icon",
      purple: "[&_div]:bg-ui-tag-purple-icon",
      grey: "[&_div]:bg-ui-tag-neutral-icon"
    }
  },
  defaultVariants: {
    color: "grey"
  }
});
var StatusBadge = React40.forwardRef(({
  children,
  className,
  /**
   * The status's color.
   */
  color = "grey",
  ...props
}, ref) => {
  return React40.createElement(
    "span",
    { ref, className: clx("txt-compact-xsmall-plus bg-ui-bg-subtle text-ui-fg-subtle border-ui-border-base box-border flex w-fit select-none items-center overflow-hidden rounded-md border pl-0 pr-1 leading-none", className), ...props },
    React40.createElement(
      "div",
      { role: "presentation", className: statusBadgeVariants({ color }) },
      React40.createElement("div", null)
    ),
    children
  );
});
StatusBadge.displayName = "StatusBadge";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/switch/switch.js
var React41 = __toESM(require_react());
var switchVariants = cva({
  base: "bg-ui-bg-switch-off hover:bg-ui-bg-switch-off-hover data-[state=unchecked]:hover:after:bg-switch-off-hover-gradient before:shadow-details-switch-background focus-visible:shadow-details-switch-background-focus data-[state=checked]:bg-ui-bg-interactive disabled:opacity-50 group relative inline-flex items-center rounded-full outline-none transition-all before:absolute before:inset-0 before:rounded-full before:content-[''] after:absolute after:inset-0 after:rounded-full after:content-[''] disabled:cursor-not-allowed",
  variants: {
    size: {
      small: "h-[16px] w-[28px]",
      base: "h-[18px] w-[32px]"
    }
  },
  defaultVariants: {
    size: "base"
  }
});
var thumbVariants = cva({
  base: "bg-ui-fg-on-color shadow-details-switch-handle pointer-events-none h-[14px] w-[14px] rounded-full transition-all",
  variants: {
    size: {
      small: "h-[12px] w-[12px] data-[state=checked]:translate-x-3.5 data-[state=unchecked]:translate-x-0.5",
      base: "h-[14px] w-[14px] transition-transform data-[state=checked]:translate-x-4 data-[state=unchecked]:translate-x-0.5"
    }
  },
  defaultVariants: {
    size: "base"
  }
});
var Switch = React41.forwardRef(({
  className,
  /**
   * The switch's size.
   */
  size = "base",
  ...props
}, ref) => React41.createElement(
  dist_exports13.Root,
  { className: clx(switchVariants({ size }), className), ...props, ref },
  React41.createElement(dist_exports13.Thumb, { className: clx(thumbVariants({ size })) })
));
Switch.displayName = "Switch";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/textarea/textarea.js
var React42 = __toESM(require_react());
var Textarea = React42.forwardRef(({ className, ...props }, ref) => {
  return React42.createElement("textarea", { ref, className: clx(inputBaseStyles, "txt-small min-h-[60px] w-full px-2 py-1.5", className), ...props });
});
Textarea.displayName = "Textarea";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/utils/create-data-table-column-helper.js
var React45 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-action-cell.js
var React43 = __toESM(require_react());
var DataTableActionCell = ({ ctx }) => {
  const meta = ctx.column.columnDef.meta;
  const actions = meta === null || meta === void 0 ? void 0 : meta.___actions;
  if (!actions) {
    return null;
  }
  const resolvedActions = typeof actions === "function" ? actions(ctx) : actions;
  if (!Array.isArray(resolvedActions)) {
    return null;
  }
  return React43.createElement(
    DropdownMenu,
    null,
    React43.createElement(
      DropdownMenu.Trigger,
      { asChild: true, className: "ml-1" },
      React43.createElement(
        IconButton,
        { size: "small", variant: "transparent" },
        React43.createElement(EllipsisHorizontal, null)
      )
    ),
    React43.createElement(DropdownMenu.Content, { side: "bottom" }, resolvedActions.map((actionOrGroup, idx) => {
      const isArray = Array.isArray(actionOrGroup);
      const isLast = idx === resolvedActions.length - 1;
      return isArray ? React43.createElement(
        React43.Fragment,
        { key: idx },
        actionOrGroup.map((action) => React43.createElement(
          DropdownMenu.Item,
          { key: action.label, onClick: (e) => {
            e.stopPropagation();
            action.onClick(ctx);
          }, className: "[&>svg]:text-ui-fg-subtle flex items-center gap-2" },
          action.icon,
          action.label
        )),
        !isLast && React43.createElement(DropdownMenu.Separator, null)
      ) : React43.createElement(
        DropdownMenu.Item,
        { key: actionOrGroup.label, onClick: (e) => {
          e.stopPropagation();
          actionOrGroup.onClick(ctx);
        }, className: "[&>svg]:text-ui-fg-subtle flex items-center gap-2" },
        actionOrGroup.icon,
        actionOrGroup.label
      );
    }))
  );
};
DataTableActionCell.displayName = "DataTable.ActionCell";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-select-cell.js
var React44 = __toESM(require_react());
var DataTableSelectCell = (props) => {
  const checked = props.ctx.row.getIsSelected();
  const onChange = props.ctx.row.getToggleSelectedHandler();
  const disabled = !props.ctx.row.getCanSelect();
  return React44.createElement(Checkbox, { onClick: (e) => e.stopPropagation(), checked, onCheckedChange: onChange, disabled });
};
DataTableSelectCell.displayName = "DataTable.SelectCell";
var DataTableSelectHeader = (props) => {
  const checked = props.ctx.table.getIsSomePageRowsSelected() ? "indeterminate" : props.ctx.table.getIsAllPageRowsSelected();
  const onChange = (checked2) => {
    props.ctx.table.toggleAllPageRowsSelected(!!checked2);
  };
  const disabled = !props.ctx.table.getRowModel().rows.some((row) => row.getCanSelect());
  return React44.createElement(Checkbox, { onClick: (e) => e.stopPropagation(), checked, onCheckedChange: onChange, disabled });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/utils/create-data-table-column-helper.js
var createDataTableColumnHelper = () => {
  const { accessor: accessorTanstack, display } = createColumnHelper();
  return {
    accessor: (accessor, column) => {
      const { sortLabel, sortAscLabel, sortDescLabel, headerAlign, meta, enableSorting, ...rest } = column;
      const extendedMeta = {
        ___sortMetaData: { sortLabel, sortAscLabel, sortDescLabel },
        ___alignMetaData: { headerAlign },
        ...meta || {}
      };
      return accessorTanstack(accessor, {
        ...rest,
        enableSorting: enableSorting !== null && enableSorting !== void 0 ? enableSorting : false,
        meta: extendedMeta
      });
    },
    display,
    action: ({ actions, ...props }) => display({
      id: "action",
      cell: (ctx) => React45.createElement(DataTableActionCell, { ctx }),
      meta: {
        ___actions: actions,
        ...props.meta || {}
      },
      ...props
    }),
    select: (props) => display({
      id: "select",
      header: (props === null || props === void 0 ? void 0 : props.header) ? props.header : (ctx) => React45.createElement(DataTableSelectHeader, { ctx }),
      cell: (props === null || props === void 0 ? void 0 : props.cell) ? props.cell : (ctx) => React45.createElement(DataTableSelectCell, { ctx })
    })
  };
};
var helper = createColumnHelper();
helper.accessor("name", {
  meta: {}
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/utils/create-data-table-command-helper.js
var createDataTableCommandHelper = () => ({
  command: (command) => command
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/utils/create-data-table-filter-helper.js
var createDataTableFilterHelper = () => ({
  accessor: (accessor, props) => ({
    id: accessor,
    ...props
  }),
  custom: (props) => props
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/hooks/use-prompt/use-prompt.js
var React47 = __toESM(require_react());
var import_react2 = __toESM(require_react());
var import_client = __toESM(require_client());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/hooks/use-prompt/render-prompt.js
var React46 = __toESM(require_react());
var RenderPrompt = ({
  /**
   * @ignore
   */
  open,
  /**
   * The variant of the prompt.
   */
  variant = "danger",
  /**
   * The prompt's title.
   */
  title,
  /**
   * The prompt's description.
   */
  description,
  /**
   * The text the user has to input in order to confirm the action.
   */
  verificationText,
  /**
   * The instruction for the verification text. Useful for passing a translated string to use instead of the default english one.
   * Should be in the format: "Please type {val} to confirm:"
   */
  verificationInstruction = "Please type {val} to confirm:",
  /**
   * The label for the Cancel button.
   */
  cancelText = "Cancel",
  /**
   * Label for the Confirm button.
   */
  confirmText = "Confirm",
  /**
   * @ignore
   */
  onConfirm,
  /**
   * @ignore
   */
  onCancel
}) => {
  const [userInput, setUserInput] = React46.useState("");
  const handleUserInput = (event) => {
    setUserInput(event.target.value);
  };
  const validInput = React46.useMemo(() => {
    if (!verificationText) {
      return true;
    }
    return userInput === verificationText;
  }, [userInput, verificationText]);
  const handleFormSubmit = (event) => {
    event.preventDefault();
    if (!verificationText) {
      return;
    }
    if (validInput) {
      onConfirm();
    }
  };
  React46.useEffect(() => {
    const handleEscape = (event) => {
      if (event.key === "Escape" && open) {
        onCancel();
      }
    };
    document.addEventListener("keydown", handleEscape);
    return () => {
      document.removeEventListener("keydown", handleEscape);
    };
  }, [onCancel, open]);
  let instructionParts = verificationInstruction.includes("{val}") ? verificationInstruction.split("{val}") : ["Please type", "to confirm:"];
  if (instructionParts.length !== 2) {
    instructionParts = ["Please type", "to confirm:"];
  }
  return React46.createElement(
    Prompt,
    { open, variant },
    React46.createElement(
      Prompt.Content,
      null,
      React46.createElement(
        "form",
        { onSubmit: handleFormSubmit },
        React46.createElement(
          Prompt.Header,
          null,
          React46.createElement(Prompt.Title, null, title),
          React46.createElement(Prompt.Description, null, description)
        ),
        verificationText && React46.createElement(
          "div",
          { className: "border-ui-border-base mt-6 flex flex-col gap-y-4 border-y p-6" },
          React46.createElement(
            Label2,
            { htmlFor: "verificationText", className: "text-ui-fg-subtle" },
            instructionParts[0],
            " ",
            React46.createElement("span", { className: "text-ui-fg-base txt-compact-medium-plus" }, verificationText),
            " ",
            instructionParts[1]
          ),
          React46.createElement(Input, { autoFocus: true, autoComplete: "off", id: "verificationText", placeholder: verificationText, onChange: handleUserInput })
        ),
        React46.createElement(
          Prompt.Footer,
          null,
          React46.createElement(Prompt.Cancel, { onClick: onCancel }, cancelText),
          React46.createElement(Prompt.Action, { disabled: !validInput, type: verificationText ? "submit" : "button", onClick: verificationText ? void 0 : onConfirm }, confirmText)
        )
      )
    )
  );
};
RenderPrompt.displayName = "RenderPrompt";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/hooks/use-prompt/use-prompt.js
var usePrompt = () => {
  const currentPromptPromise = (0, import_react2.useRef)(null);
  const prompt = async (props) => {
    if (currentPromptPromise.current) {
      return currentPromptPromise.current;
    }
    const promptPromise = new Promise((resolve) => {
      let open = true;
      const mountRoot = (0, import_client.createRoot)(document.createElement("div"));
      const onCancel = () => {
        open = false;
        mountRoot.unmount();
        resolve(false);
        currentPromptPromise.current = null;
        document.body.style.pointerEvents = "auto";
      };
      const onConfirm = () => {
        open = false;
        resolve(true);
        mountRoot.unmount();
        currentPromptPromise.current = null;
        document.body.style.pointerEvents = "auto";
      };
      const render = () => {
        mountRoot.render(React47.createElement(RenderPrompt, { open, onConfirm, onCancel, ...props }));
      };
      render();
    });
    currentPromptPromise.current = promptPromise;
    return promptPromise;
  };
  return prompt;
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/toast/toast.js
var React48 = __toESM(require_react());
var Toast = ({
  /**
   * Optional ID of the toast.
   */
  id,
  /**
   * @ignore
   *
   * @privateRemarks
   * As the Toast component is created using
   * the `toast` utility functions, the variant is inferred
   * from the utility function.
   */
  variant = "info",
  /**
   * @ignore
   *
   * @privateRemarks
   * The `toast` utility functions accept this as a parameter.
   */
  title,
  /**
   * @ignore
   *
   * @privateRemarks
   * The `toast` utility functions accept this as a parameter.
   */
  icon: _icon,
  /**
   * The toast's text.
   */
  description,
  /**
   * The toast's action buttons.
   */
  action,
  /**
   * @ignore
   *
   * @privateRemarks
   * The `toast` utility functions accept this as a parameter.
   */
  dismissable = true
}) => {
  let icon = _icon;
  if (!_icon) {
    switch (variant) {
      case "success":
        icon = React48.createElement(CheckCircleSolid, { className: "text-ui-tag-green-icon" });
        break;
      case "warning":
        icon = React48.createElement(ExclamationCircleSolid, { className: "text-ui-tag-orange-icon" });
        break;
      case "error":
        icon = React48.createElement(XCircleSolid, { className: "text-ui-tag-red-icon" });
        break;
      case "loading":
        icon = React48.createElement(Spinner, { className: "text-ui-tag-blue-icon animate-spin" });
        break;
      case "info":
        icon = React48.createElement(InformationCircleSolid, { className: "text-ui-fg-base" });
        break;
      default:
        break;
    }
  }
  return React48.createElement(
    "div",
    { className: "shadow-elevation-flyout bg-ui-bg-component flex w-fit min-w-[360px] max-w-[440px] gap-x-3 overflow-hidden rounded-lg p-3" },
    React48.createElement(
      "div",
      { className: clx("grid flex-1 items-center gap-x-2", {
        "grid-cols-[20px_1fr]": !!icon,
        "grid-cols-1": !icon,
        "items-start": !!description
      }) },
      !!icon && React48.createElement("span", { className: "flex size-5 items-center justify-center", "aria-hidden": true }, icon),
      React48.createElement(
        "div",
        { className: "flex flex-col gap-y-3" },
        React48.createElement(
          "div",
          { className: "flex flex-col" },
          React48.createElement("span", { className: "txt-compact-small-plus text-ui-fg-base" }, title),
          description && React48.createElement("span", { className: "txt-small text-ui-fg-subtle text-pretty" }, description)
        ),
        !!action && React48.createElement("button", { type: "button", className: clx("txt-compact-small-plus text-ui-fg-base bg-ui-bg-base flex w-fit items-center rounded-[4px] transition-colors", "focus-visible:shadow-borders-focus", "hover:text-ui-fg-subtle", "disabled:text-ui-fg-disabled", {
          "text-ui-fg-error": action.variant === "destructive"
        }), onClick: action.onClick }, action.label)
      )
    ),
    !!dismissable && React48.createElement(
      IconButton,
      { size: "2xsmall", variant: "transparent", type: "button", onClick: () => ue.dismiss(id) },
      React48.createElement(XMark, null)
    )
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/utils/toast.js
var React49 = __toESM(require_react());
var DEFAULT_TOAST_POSITION = "top-right";
function create(variant, title, props = {}) {
  const external = {
    position: props.position,
    duration: props.duration,
    dismissible: props.dismissable
  };
  if (props.id) {
    external.id = props.id;
  }
  return ue.custom((t) => {
    return React49.createElement(Toast, { id: t, title, description: props.description, dismissable: props.dismissable, variant, action: props.action, icon: props.icon });
  }, external);
}
function message(title, props = {
  position: DEFAULT_TOAST_POSITION
}) {
  return create("message", title, props);
}
function info(title, props = {
  position: DEFAULT_TOAST_POSITION
}) {
  return create("info", title, props);
}
function error(title, props = {
  position: DEFAULT_TOAST_POSITION
}) {
  return create("error", title, props);
}
function success(title, props = {
  position: DEFAULT_TOAST_POSITION
}) {
  return create("success", title, props);
}
function warning(title, props = {
  position: DEFAULT_TOAST_POSITION
}) {
  return create("warning", title, props);
}
function loading(title, props = {
  position: DEFAULT_TOAST_POSITION
}) {
  return create("loading", title, { ...props, dismissable: false });
}
function createUniqueID() {
  return Math.random().toString(36).slice(2, 8);
}
async function promise(promise2, props) {
  let id = props.id || createUniqueID();
  let shouldDismiss = id !== void 0;
  id = create("loading", typeof props.loading === "string" ? props.loading : props.loading.title, {
    id,
    position: props.position,
    description: typeof props.loading === "string" ? void 0 : props.loading.description,
    duration: Infinity,
    dismissable: false
  });
  const p = promise2 instanceof Promise ? promise2 : promise2();
  p.then(() => {
    shouldDismiss = false;
    create("success", typeof props.success === "string" ? props.success : props.success.title, {
      id,
      position: props.position,
      description: typeof props.success === "string" ? void 0 : props.success.description
    });
  }).catch(() => {
    shouldDismiss = false;
    create("error", typeof props.error === "string" ? props.error : props.error.title, {
      id,
      position: props.position,
      description: typeof props.error === "string" ? void 0 : props.error.description
    });
  }).finally(() => {
    if (shouldDismiss) {
      ue.dismiss(id);
      id = void 0;
    }
  });
  return id;
}
var toast = Object.assign(message, {
  info,
  error,
  warning,
  success,
  promise,
  loading,
  dismiss: ue.dismiss
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/code/code.js
var React50 = __toESM(require_react());
var Code = React50.forwardRef(({ className, ...props }, ref) => {
  return React50.createElement("code", { ref, className: clx("border-ui-tag-neutral-border bg-ui-tag-neutral-bg text-ui-tag-neutral-text txt-compact-small inline-flex rounded-md border px-[6px] font-mono", className), ...props });
});
Code.displayName = "Code";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/command/command.js
var import_react3 = __toESM(require_react());
var CommandComponent = ({ className, ...props }) => {
  return import_react3.default.createElement("div", { className: clx("bg-ui-contrast-bg-base shadow-elevation-code-block flex items-center rounded-lg px-4 py-1.5", "[&>code]:text-ui-contrast-fg-primary [&>code]:code-body [&>code]:mx-2", className), ...props });
};
CommandComponent.displayName = "Command";
var CommandCopy = import_react3.default.forwardRef(({ className, ...props }, ref) => {
  return import_react3.default.createElement(Copy, { ...props, ref, className: clx("!text-ui-contrast-fg-secondary ml-auto", className) });
});
CommandCopy.displayName = "Command.Copy";
var Command2 = Object.assign(CommandComponent, { Copy: CommandCopy });

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/drawer/drawer.js
var React52 = __toESM(require_react());
var DrawerRoot = (props) => {
  return React52.createElement(dist_exports7.Root, { ...props });
};
DrawerRoot.displayName = "Drawer";
var DrawerTrigger = React52.forwardRef(({ className, ...props }, ref) => {
  return React52.createElement(dist_exports7.Trigger, { ref, className: clx(className), ...props });
});
DrawerTrigger.displayName = "Drawer.Trigger";
var DrawerClose = React52.forwardRef(({ className, ...props }, ref) => {
  return React52.createElement(dist_exports7.Close, { ref, className: clx(className), ...props });
});
DrawerClose.displayName = "Drawer.Close";
var DrawerPortal = (props) => {
  return React52.createElement(dist_exports7.DialogPortal, { ...props });
};
DrawerPortal.displayName = "Drawer.Portal";
var DrawerOverlay = React52.forwardRef(({ className, ...props }, ref) => {
  return React52.createElement(dist_exports7.Overlay, { ref, className: clx("bg-ui-bg-overlay fixed inset-0", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0", className), ...props });
});
DrawerOverlay.displayName = "Drawer.Overlay";
var DrawerContent = React52.forwardRef(({ className, overlayProps, portalProps, ...props }, ref) => {
  return React52.createElement(
    DrawerPortal,
    { ...portalProps },
    React52.createElement(DrawerOverlay, { ...overlayProps }),
    React52.createElement(dist_exports7.Content, { ref, className: clx("bg-ui-bg-base shadow-elevation-modal border-ui-border-base fixed inset-y-2 flex w-full flex-1 flex-col rounded-lg border outline-none max-sm:inset-x-2 max-sm:w-[calc(100%-16px)] sm:right-2 sm:max-w-[560px]", "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:slide-out-to-right-1/2 data-[state=open]:slide-in-from-right-1/2 duration-200", className), ...props })
  );
});
DrawerContent.displayName = "Drawer.Content";
var DrawerHeader = React52.forwardRef(({ children, className, ...props }, ref) => {
  return React52.createElement(
    "div",
    { ref, className: "border-ui-border-base flex items-start justify-between gap-x-4 border-b px-6 py-4", ...props },
    React52.createElement("div", { className: clx("flex flex-col gap-y-1", className) }, children),
    React52.createElement(
      "div",
      { className: "flex items-center gap-x-2" },
      React52.createElement(Kbd, null, "esc"),
      React52.createElement(
        dist_exports7.Close,
        { asChild: true },
        React52.createElement(
          IconButton,
          { size: "small", type: "button", variant: "transparent" },
          React52.createElement(XMark, null)
        )
      )
    )
  );
});
DrawerHeader.displayName = "Drawer.Header";
var DrawerBody = React52.forwardRef(({ className, ...props }, ref) => {
  return React52.createElement("div", { ref, className: clx("flex-1 px-6 py-4", className), ...props });
});
DrawerBody.displayName = "Drawer.Body";
var DrawerFooter = ({ className, ...props }) => {
  return React52.createElement("div", { className: clx("border-ui-border-base flex items-center justify-end space-x-2 overflow-y-auto border-t px-6 py-4", className), ...props });
};
DrawerFooter.displayName = "Drawer.Footer";
var DrawerTitle = dist_exports7.Title;
DrawerTitle.displayName = "Drawer.Title";
var DrawerDescription = React52.forwardRef(({ className, children, ...props }, ref) => React52.createElement(
  dist_exports7.Description,
  { ref, className: clx(className), asChild: true, ...props },
  React52.createElement(Text, null, children)
));
DrawerDescription.displayName = "Drawer.Description";
var Drawer = Object.assign(DrawerRoot, {
  Body: DrawerBody,
  Close: DrawerClose,
  Content: DrawerContent,
  Description: DrawerDescription,
  Footer: DrawerFooter,
  Header: DrawerHeader,
  Title: DrawerTitle,
  Trigger: DrawerTrigger
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/i18n-provider/i18n-provider.js
var React53 = __toESM(require_react());
var I18nProvider = (props) => {
  return React53.createElement($2eb8e6d23f3d0cb0$export$a54013f0d02a8f82, { ...props });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/icon-badge/icon-badge.js
var React54 = __toESM(require_react());
var iconBadgeVariants = cva({
  base: "flex items-center justify-center overflow-hidden rounded-md border",
  variants: {
    size: {
      base: "h-6 w-6",
      large: "h-7 w-7"
    }
  }
});
var IconBadge = React54.forwardRef(({
  children,
  className,
  /**
   * The badge's color.
   */
  color = "grey",
  /**
   * The badge's size.
   */
  size = "base",
  /**
   * Whether to remove the wrapper `span` element and use the
   * passed child element instead.
   */
  asChild = false,
  ...props
}, ref) => {
  const Component = asChild ? dist_exports.Root : "span";
  return React54.createElement(Component, { ref, className: clx(badgeColorVariants({ color }), iconBadgeVariants({ size }), className), ...props }, children);
});
IconBadge.displayName = "IconBadge";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/progress-accordion/progress-accordion.js
var React55 = __toESM(require_react());
var Root8 = (props) => {
  return React55.createElement(dist_exports2.Root, { ...props });
};
Root8.displayName = "ProgressAccordion";
var Item4 = React55.forwardRef(({ className, ...props }, ref) => {
  return React55.createElement(dist_exports2.Item, { ref, className: clx("border-ui-border-base border-b last-of-type:border-b-0", className), ...props });
});
Item4.displayName = "ProgressAccordion.Item";
var ProgressIndicator2 = ({
  /**
   * The current status.
   */
  status,
  ...props
}) => {
  const Icon = React55.useMemo(() => {
    switch (status) {
      case "not-started":
        return CircleDottedLine;
      case "in-progress":
        return CircleHalfSolid;
      case "completed":
        return CheckCircleSolid;
      default:
        return CircleDottedLine;
    }
  }, [status]);
  return React55.createElement(
    "span",
    { className: "text-ui-fg-muted group-data-[state=open]:text-ui-fg-interactive flex h-12 w-12 items-center justify-center", ...props },
    React55.createElement(Icon, null)
  );
};
ProgressIndicator2.displayName = "ProgressAccordion.ProgressIndicator";
var Header3 = React55.forwardRef(({
  className,
  /**
   * The current status.
   */
  status = "not-started",
  children,
  ...props
}, ref) => {
  return React55.createElement(
    dist_exports2.Header,
    { ref, className: clx("h3-core text-ui-fg-base group flex w-full flex-1 items-center gap-4 px-6", className), ...props },
    React55.createElement(ProgressIndicator2, { status }),
    children,
    React55.createElement(
      dist_exports2.Trigger,
      { asChild: true, className: "ml-auto" },
      React55.createElement(
        IconButton,
        { variant: "transparent" },
        React55.createElement(Plus, { className: "transform transition-transform group-data-[state=open]:rotate-45" })
      )
    )
  );
});
Header3.displayName = "ProgressAccordion.Header";
var Content5 = React55.forwardRef(({ className, ...props }, ref) => {
  return React55.createElement(dist_exports2.Content, { ref, className: clx("overflow-hidden", "data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down pl-[88px] pr-6", className), ...props });
});
Content5.displayName = "ProgressAccordion.Content";
var ProgressAccordion = Object.assign(Root8, {
  Item: Item4,
  Header: Header3,
  Content: Content5
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/skeleton/skeleton.js
var React56 = __toESM(require_react());
function Skeleton({ className, ...props }) {
  return React56.createElement("div", { className: clx("bg-ui-bg-component animate-pulse rounded-md", className), ...props });
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/table/table.js
var React57 = __toESM(require_react());
var Root9 = React57.forwardRef(({ className, ...props }, ref) => React57.createElement("table", { ref, className: clx("text-ui-fg-subtle txt-compact-small w-full", className), ...props }));
Root9.displayName = "Table";
var Row = React57.forwardRef(({ className, ...props }, ref) => React57.createElement("tr", { ref, className: clx("bg-ui-bg-base hover:bg-ui-bg-base-hover border-ui-border-base transition-fg border-b", "[&_td:last-child]:pr-6 [&_th:last-child]:pr-6", "[&_td:first-child]:pl-6 [&_th:first-child]:pl-6", className), ...props }));
Row.displayName = "Table.Row";
var Cell = React57.forwardRef(({ className, ...props }, ref) => React57.createElement("td", { ref, className: clx("h-12 py-0 pl-0 pr-6", className), ...props }));
Cell.displayName = "Table.Cell";
var Header4 = React57.forwardRef(({ className, ...props }, ref) => React57.createElement("thead", { ref, className: clx("border-ui-border-base txt-compact-small-plus [&_tr]:bg-ui-bg-subtle [&_tr]:hover:bg-ui-bg-subtle border-y", className), ...props }));
Header4.displayName = "Table.Header";
var HeaderCell = React57.forwardRef(({ className, ...props }, ref) => React57.createElement("th", { ref, className: clx("txt-compact-small-plus h-12 py-0 pl-0 pr-6 text-left", className), ...props }));
HeaderCell.displayName = "Table.HeaderCell";
var Body2 = React57.forwardRef(({ className, ...props }, ref) => React57.createElement("tbody", { ref, className: clx("border-ui-border-base border-b", className), ...props }));
Body2.displayName = "Table.Body";
var Pagination = React57.forwardRef(({
  className,
  /**
   * The total number of items.
   */
  count,
  /**
   * The number of items per page.
   */
  pageSize,
  /**
   * The total number of pages.
   */
  pageCount,
  /**
   * The current page index.
   */
  pageIndex,
  /**
   * Whether there's a previous page that can be navigated to.
   */
  canPreviousPage,
  /**
   * Whether there's a next page that can be navigated to.
   */
  canNextPage,
  /**
   * A function that handles navigating to the next page.
   * This function should handle retrieving data for the next page.
   */
  nextPage,
  /**
   * A function that handles navigating to the previous page.
   * This function should handle retrieving data for the previous page.
   */
  previousPage,
  /**
   * An optional object of words to use in the pagination component.
   * Use this to override the default words, or translate them into another language.
   */
  translations = {
    of: "of",
    results: "results",
    pages: "pages",
    prev: "Prev",
    next: "Next"
  },
  ...props
}, ref) => {
  const { from, to } = React57.useMemo(() => {
    const from2 = count === 0 ? count : pageIndex * pageSize + 1;
    const to2 = Math.min(count, (pageIndex + 1) * pageSize);
    return { from: from2, to: to2 };
  }, [count, pageIndex, pageSize]);
  return React57.createElement(
    "div",
    { ref, className: clx("text-ui-fg-subtle txt-compact-small-plus flex w-full items-center justify-between px-3 py-4", className), ...props },
    React57.createElement(
      "div",
      { className: "inline-flex items-center gap-x-1 px-3 py-[5px]" },
      React57.createElement("p", null, from),
      React57.createElement(Minus, { className: "text-ui-fg-muted" }),
      React57.createElement("p", null, `${to} ${translations.of} ${count} ${translations.results}`)
    ),
    React57.createElement(
      "div",
      { className: "flex items-center gap-x-2" },
      React57.createElement(
        "div",
        { className: "inline-flex items-center gap-x-1 px-3 py-[5px]" },
        React57.createElement(
          "p",
          null,
          pageIndex + 1,
          " ",
          translations.of,
          " ",
          Math.max(pageCount, 1),
          " ",
          translations.pages
        )
      ),
      React57.createElement(Button, { type: "button", variant: "transparent", onClick: previousPage, disabled: !canPreviousPage }, translations.prev),
      React57.createElement(Button, { type: "button", variant: "transparent", onClick: nextPage, disabled: !canNextPage }, translations.next)
    )
  );
});
Pagination.displayName = "Table.Pagination";
var Table = Object.assign(Root9, {
  Row,
  Cell,
  Header: Header4,
  HeaderCell,
  Body: Body2,
  Pagination
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/tabs/tabs.js
var React58 = __toESM(require_react());
var TabsRoot = (props) => {
  return React58.createElement(dist_exports14.Root, { ...props });
};
TabsRoot.displayName = "Tabs";
var TabsTrigger = React58.forwardRef(({ className, children, ...props }, ref) => React58.createElement(dist_exports14.Trigger, { ref, className: clx("txt-compact-small-plus transition-fg text-ui-fg-subtle inline-flex items-center justify-center rounded-full border border-transparent bg-transparent px-2.5 py-1 outline-none", "hover:text-ui-fg-base", "focus-visible:border-ui-border-interactive focus-visible:!shadow-borders-focus focus-visible:bg-ui-bg-base", "data-[state=active]:text-ui-fg-base data-[state=active]:bg-ui-bg-base data-[state=active]:shadow-elevation-card-rest", className), ...props }, children));
TabsTrigger.displayName = "Tabs.Trigger";
var TabsList = React58.forwardRef(({ className, ...props }, ref) => React58.createElement(dist_exports14.List, { ref, className: clx("flex items-center gap-2", className), ...props }));
TabsList.displayName = "Tabs.List";
var TabsContent = React58.forwardRef(({ className, ...props }, ref) => React58.createElement(dist_exports14.Content, { ref, className: clx("outline-none", className), ...props }));
TabsContent.displayName = "Tabs.Content";
var Tabs = Object.assign(TabsRoot, {
  Trigger: TabsTrigger,
  List: TabsList,
  Content: TabsContent
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/components/toaster/toaster.js
var React59 = __toESM(require_react());
var Toaster = ({
  /**
   * The position of the created toasts.
   */
  position = "bottom-right",
  /**
   * The gap between the toast components.
   */
  gap = 12,
  /**
   * The space from the edges of the screen.
   */
  offset = 24,
  /**
   * The time in milliseconds that a toast is shown before it's
   * automatically dismissed.
   *
   * @defaultValue 4000
   */
  duration,
  ...props
}) => {
  const primitiveProps = {
    ...props,
    position,
    gap,
    offset,
    cn: clx,
    toastOptions: {
      duration
    }
  };
  return React59.createElement($e, { ...primitiveProps });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/data-table.js
var React75 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-command-bar.js
var React61 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/context/use-data-table-context.js
var React60 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/context/data-table-context.js
var import_react4 = __toESM(require_react());
var DataTableContext = (0, import_react4.createContext)(null);

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/context/use-data-table-context.js
var useDataTableContext = () => {
  const context = React60.useContext(DataTableContext);
  if (!context) {
    throw new Error("useDataTableContext must be used within a DataTableContextProvider");
  }
  return context;
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-command-bar.js
var DataTableCommandBar = (props) => {
  const { instance } = useDataTableContext();
  const commands = instance.getCommands();
  const rowSelection = instance.getRowSelection();
  const count = Object.keys(rowSelection || []).length;
  const open = commands && commands.length > 0 && count > 0;
  function getSelectedLabel(count2) {
    if (typeof props.selectedLabel === "function") {
      return props.selectedLabel(count2);
    }
    return props.selectedLabel;
  }
  if (!commands || commands.length === 0) {
    return null;
  }
  return React61.createElement(
    CommandBar,
    { open },
    React61.createElement(
      CommandBar.Bar,
      null,
      props.selectedLabel && React61.createElement(
        React61.Fragment,
        null,
        React61.createElement(CommandBar.Value, null, getSelectedLabel(count)),
        React61.createElement(CommandBar.Seperator, null)
      ),
      commands.map((command, idx) => React61.createElement(
        React61.Fragment,
        { key: idx },
        React61.createElement(CommandBar.Command, { key: command.label, action: () => command.action(rowSelection), label: command.label, shortcut: command.shortcut }),
        idx < commands.length - 1 && React61.createElement(CommandBar.Seperator, null)
      ))
    )
  );
};
DataTableCommandBar.displayName = "DataTable.CommandBar";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-column-visibility-menu.js
var import_react5 = __toESM(require_react());
var DataTableColumnVisibilityMenu = ({ className, tooltip }) => {
  const { instance, enableColumnVisibility } = useDataTableContext();
  if (!enableColumnVisibility) {
    return null;
  }
  const columns = instance.getAllColumns().filter((column) => column.getCanHide());
  const handleToggleColumn = (column) => {
    column.toggleVisibility();
  };
  const handleToggleAll = (value) => {
    instance.setColumnVisibility(Object.fromEntries(columns.map((column) => [column.id, value])));
  };
  const allColumnsVisible = columns.every((column) => column.getIsVisible());
  const someColumnsVisible = columns.some((column) => column.getIsVisible());
  const Wrapper = tooltip ? Tooltip : import_react5.default.Fragment;
  const wrapperProps = tooltip ? { content: tooltip } : {};
  return import_react5.default.createElement(
    DropdownMenu,
    null,
    import_react5.default.createElement(
      Wrapper,
      { ...wrapperProps },
      import_react5.default.createElement(
        DropdownMenu.Trigger,
        { asChild: true },
        import_react5.default.createElement(
          IconButton,
          { size: "small", className },
          import_react5.default.createElement(Adjustments, null)
        )
      )
    ),
    import_react5.default.createElement(
      DropdownMenu.Content,
      { align: "end", className: "min-w-[200px] max-h-[400px] overflow-hidden" },
      import_react5.default.createElement(DropdownMenu.Label, null, "Toggle columns"),
      import_react5.default.createElement(DropdownMenu.Separator, null),
      import_react5.default.createElement(
        DropdownMenu.Item,
        { onSelect: (e) => {
          e.preventDefault();
          handleToggleAll(!allColumnsVisible);
        } },
        import_react5.default.createElement(
          "div",
          { className: "flex items-center gap-x-2" },
          import_react5.default.createElement(Checkbox, { checked: allColumnsVisible ? true : someColumnsVisible && !allColumnsVisible ? "indeterminate" : false }),
          import_react5.default.createElement("span", null, "Toggle all")
        )
      ),
      import_react5.default.createElement(DropdownMenu.Separator, null),
      import_react5.default.createElement("div", { className: "max-h-[250px] overflow-y-auto" }, columns.map((column) => {
        var _a;
        return import_react5.default.createElement(
          DropdownMenu.Item,
          { key: column.id, onSelect: (e) => {
            e.preventDefault();
            handleToggleColumn(column);
          } },
          import_react5.default.createElement(
            "div",
            { className: "flex items-center gap-x-2" },
            import_react5.default.createElement(Checkbox, { checked: column.getIsVisible() }),
            import_react5.default.createElement("span", { className: "truncate" }, ((_a = column.columnDef.meta) === null || _a === void 0 ? void 0 : _a.name) || column.id)
          )
        );
      }))
    )
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-filter-bar.js
var React66 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-filter.js
var React63 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/utils/is-date-comparison-operator.js
function isDateComparisonOperator(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const validOperators = ["$gte", "$lte", "$gt", "$lt"];
  const hasAtLeastOneOperator = validOperators.some((op) => op in value);
  const allPropertiesValid = Object.entries(value).every(([key, val]) => validOperators.includes(key) && (typeof val === "string" || val === void 0));
  return hasAtLeastOneOperator && allPropertiesValid;
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-filter.js
var DEFAULT_FORMAT_DATE_VALUE = (d) => d.toLocaleDateString(void 0, {
  year: "numeric",
  month: "short",
  day: "numeric"
});
var DEFAULT_RANGE_OPTION_LABEL = "Custom";
var DEFAULT_RANGE_OPTION_START_LABEL = "Starting";
var DEFAULT_RANGE_OPTION_END_LABEL = "Ending";
var DataTableFilter = ({ id, filter, isNew = false, onUpdate, onRemove }) => {
  const { instance } = useDataTableContext();
  const [open, setOpen] = React63.useState(isNew);
  const [hasInteracted, setHasInteracted] = React63.useState(false);
  const meta = instance.getFilterMeta(id);
  if (!meta) {
    return null;
  }
  const { type, label, ...rest } = meta;
  const options = meta.options;
  const hasValue = React63.useMemo(() => {
    if (filter === null || filter === void 0)
      return false;
    if (typeof filter === "string" && filter === "")
      return false;
    if (Array.isArray(filter) && filter.length === 0)
      return false;
    if (typeof filter === "number")
      return true;
    if (isDateComparisonOperator(filter)) {
      return !!(filter.$gte || filter.$lte || filter.$gt || filter.$lt);
    }
    if (typeof filter === "object" && filter !== null) {
      const keys = Object.keys(filter);
      return keys.length > 0 && filter[keys[0]] !== null && filter[keys[0]] !== void 0;
    }
    return true;
  }, [filter]);
  const onOpenChange = React63.useCallback((newOpen) => {
    setOpen(newOpen);
    if (!newOpen && open) {
      setHasInteracted(true);
    }
    if (!newOpen && !hasValue) {
      if (isNew && !hasInteracted || !isNew) {
        if (onRemove) {
          onRemove();
        } else {
          instance.removeFilter(id);
        }
      }
    }
  }, [instance, id, open, hasInteracted, isNew, hasValue, onRemove]);
  const removeFilter = React63.useCallback(() => {
    if (onRemove) {
      onRemove();
    } else {
      instance.removeFilter(id);
    }
  }, [instance, id, onRemove]);
  const { displayValue, isCustomRange } = React63.useMemo(() => {
    var _a, _b, _c, _d, _e;
    let displayValue2 = null;
    let isCustomRange2 = false;
    if (typeof filter === "string") {
      if (!options || options.length === 0) {
        displayValue2 = filter;
      } else {
        displayValue2 = (_b = (_a = options === null || options === void 0 ? void 0 : options.find((o) => o.value === filter)) === null || _a === void 0 ? void 0 : _a.label) !== null && _b !== void 0 ? _b : null;
      }
    }
    if (typeof filter === "number") {
      displayValue2 = String(filter);
    }
    if (Array.isArray(filter)) {
      displayValue2 = (_c = filter.map((v) => {
        var _a2;
        return (_a2 = options === null || options === void 0 ? void 0 : options.find((o) => o.value === v)) === null || _a2 === void 0 ? void 0 : _a2.label;
      }).join(", ")) !== null && _c !== void 0 ? _c : null;
    }
    if (isDateComparisonOperator(filter)) {
      displayValue2 = (_e = (_d = options === null || options === void 0 ? void 0 : options.find((o) => {
        if (!isDateComparisonOperator(o.value)) {
          return false;
        }
        return (filter.$gte === o.value.$gte || !filter.$gte && !o.value.$gte) && (filter.$lte === o.value.$lte || !filter.$lte && !o.value.$lte) && (filter.$gt === o.value.$gt || !filter.$gt && !o.value.$gt) && (filter.$lt === o.value.$lt || !filter.$lt && !o.value.$lt);
      })) === null || _d === void 0 ? void 0 : _d.label) !== null && _e !== void 0 ? _e : null;
      if (!displayValue2 && isDateFilterProps(meta)) {
        isCustomRange2 = true;
        const formatDateValue = meta.formatDateValue ? meta.formatDateValue : DEFAULT_FORMAT_DATE_VALUE;
        if (filter.$gte && !filter.$lte) {
          displayValue2 = `${meta.rangeOptionStartLabel || DEFAULT_RANGE_OPTION_START_LABEL} ${formatDateValue(new Date(filter.$gte))}`;
        }
        if (filter.$lte && !filter.$gte) {
          displayValue2 = `${meta.rangeOptionEndLabel || DEFAULT_RANGE_OPTION_END_LABEL} ${formatDateValue(new Date(filter.$lte))}`;
        }
        if (filter.$gte && filter.$lte) {
          displayValue2 = `${formatDateValue(new Date(filter.$gte))} - ${formatDateValue(new Date(filter.$lte))}`;
        }
      }
    }
    if (typeof filter === "object" && filter !== null && !Array.isArray(filter) && !isDateComparisonOperator(filter)) {
      const operators = {
        $eq: "=",
        $gt: ">",
        $gte: "≥",
        $lt: "<",
        $lte: "≤"
      };
      const op = Object.keys(filter)[0];
      const opLabel = operators[op] || op;
      const value = filter[op];
      if (typeof value === "number") {
        displayValue2 = `${opLabel} ${value}`;
      }
    }
    return { displayValue: displayValue2, isCustomRange: isCustomRange2 };
  }, [filter, options, meta]);
  return React63.createElement(
    Popover,
    { open, onOpenChange, modal: true },
    React63.createElement(
      "div",
      { className: clx("bg-ui-bg-field flex flex-shrink-0 items-stretch overflow-hidden rounded-md", "txt-compact-small-plus shadow-borders-base") },
      !hasValue && isNew && React63.createElement(Popover.Anchor, null),
      React63.createElement("div", { className: clx("flex items-center px-2 py-1 text-ui-fg-muted", {
        "border-r": hasValue
      }) }, label || id),
      hasValue && React63.createElement(
        React63.Fragment,
        null,
        (type === "select" || type === "multiselect" || type === "radio") && React63.createElement("div", { className: "flex items-center border-r px-2 py-1 text-ui-fg-muted" }, "is"),
        React63.createElement(
          Popover.Trigger,
          { asChild: true },
          React63.createElement("button", { className: clx("flex flex-1 items-center px-2 py-1 outline-none", "hover:bg-ui-bg-base-hover active:bg-ui-bg-base-pressed transition-fg", {
            "text-ui-fg-subtle": displayValue,
            "text-ui-fg-muted": !displayValue,
            "min-w-[80px] justify-center": !displayValue,
            "border-r": true
          }) }, displayValue || " ")
        ),
        React63.createElement(
          "button",
          { type: "button", className: "flex size-7 items-center justify-center text-ui-fg-muted outline-none hover:bg-ui-bg-base-hover active:bg-ui-bg-base-pressed transition-fg", onClick: removeFilter },
          React63.createElement(XMark, null)
        )
      )
    ),
    React63.createElement(Popover.Content, { align: "start", sideOffset: 8, collisionPadding: 16, hideWhenDetached: true, className: "bg-ui-bg-component p-0 outline-none", onOpenAutoFocus: (e) => {
      if (isNew) {
        const target = e.currentTarget;
        if (target) {
          const firstInput = target.querySelector('input:not([type="hidden"]), [role="list"][tabindex="0"]');
          firstInput === null || firstInput === void 0 ? void 0 : firstInput.focus();
        }
      }
    }, onCloseAutoFocus: (e) => {
      e.preventDefault();
    }, onInteractOutside: (e) => {
      const target = e.target;
      if (target.closest('[role="menuitem"]')) {
        e.preventDefault();
      }
    } }, (() => {
      switch (type) {
        case "select":
          return React63.createElement(DataTableFilterSelectContent, { id, filter, options, isNew, onUpdate });
        case "radio":
          return React63.createElement(DataTableFilterRadioContent, { id, filter, options, onUpdate });
        case "date":
          const dateRest = rest;
          return React63.createElement(DataTableFilterDateContent, { id, filter, options, isCustomRange, format: dateRest.format, rangeOptionLabel: dateRest.rangeOptionLabel, disableRangeOption: dateRest.disableRangeOption, rangeOptionStartLabel: dateRest.rangeOptionStartLabel, rangeOptionEndLabel: dateRest.rangeOptionEndLabel, onUpdate });
        case "multiselect":
          const multiselectRest = rest;
          return React63.createElement(DataTableFilterMultiselectContent, { id, filter, options, searchable: multiselectRest.searchable, onUpdate });
        case "string":
          const stringRest = rest;
          return React63.createElement(DataTableFilterStringContent, { id, filter, placeholder: stringRest.placeholder, onUpdate });
        case "number":
          const numberRest = rest;
          return React63.createElement(DataTableFilterNumberContent, { id, filter, placeholder: numberRest.placeholder, includeOperators: numberRest.includeOperators, onUpdate });
        case "custom":
          const customRest = rest;
          return React63.createElement(DataTableFilterCustomContent, { id, filter, onRemove: removeFilter, render: customRest.render, onUpdate });
        default:
          return null;
      }
    })())
  );
};
DataTableFilter.displayName = "DataTable.Filter";
var DataTableFilterDateContent = ({ id, filter, options, format = "date", rangeOptionLabel = DEFAULT_RANGE_OPTION_LABEL, rangeOptionStartLabel = DEFAULT_RANGE_OPTION_START_LABEL, rangeOptionEndLabel = DEFAULT_RANGE_OPTION_END_LABEL, disableRangeOption = false, isCustomRange, onUpdate }) => {
  const currentValue = filter;
  const { instance } = useDataTableContext();
  const [isCustom, setIsCustom] = React63.useState(isCustomRange);
  React63.useEffect(() => {
    setIsCustom(isCustomRange);
  }, [isCustomRange]);
  const selectedValue = React63.useMemo(() => {
    if (!currentValue || isCustom) {
      return void 0;
    }
    return JSON.stringify(currentValue);
  }, [currentValue, isCustom]);
  const onValueChange = React63.useCallback((valueStr) => {
    setIsCustom(false);
    const value = JSON.parse(valueStr);
    if (onUpdate) {
      onUpdate(value);
    } else {
      instance.updateFilter({ id, value });
    }
  }, [instance, id, onUpdate]);
  const onSelectCustom = React63.useCallback(() => {
    setIsCustom(true);
  }, []);
  const onCustomValueChange = React63.useCallback((input, value) => {
    const newCurrentValue = { ...currentValue };
    newCurrentValue[input] = value ? value.toISOString() : void 0;
    if (onUpdate) {
      onUpdate(newCurrentValue);
    } else {
      instance.updateFilter({ id, value: newCurrentValue });
    }
  }, [instance, id, currentValue, onUpdate]);
  const { focusedIndex, setFocusedIndex } = useKeyboardNavigation(options, (index) => {
    if (index === options.length && !disableRangeOption) {
      onSelectCustom();
    } else {
      onValueChange(JSON.stringify(options[index].value));
    }
  }, disableRangeOption ? 0 : 1);
  const granularity = format === "date-time" ? "minute" : "day";
  const maxDate = (currentValue === null || currentValue === void 0 ? void 0 : currentValue.$lte) ? granularity === "minute" ? new Date(currentValue.$lte) : new Date(new Date(currentValue.$lte).setHours(23, 59, 59, 999)) : void 0;
  const minDate = (currentValue === null || currentValue === void 0 ? void 0 : currentValue.$gte) ? granularity === "minute" ? new Date(currentValue.$gte) : new Date(new Date(currentValue.$gte).setHours(0, 0, 0, 0)) : void 0;
  const initialFocusedIndex = isCustom ? options.length : 0;
  const onListFocus = React63.useCallback(() => {
    if (focusedIndex === -1) {
      setFocusedIndex(initialFocusedIndex);
    }
  }, [focusedIndex, initialFocusedIndex]);
  return React63.createElement(
    React63.Fragment,
    null,
    React63.createElement(
      "div",
      { className: "flex flex-col p-1 outline-none", tabIndex: 0, role: "list", onFocus: onListFocus, autoFocus: true },
      options.map((option, idx) => {
        const value = JSON.stringify(option.value);
        const isSelected = selectedValue === value;
        return React63.createElement(OptionButton, { key: idx, index: idx, option, isSelected, isFocused: focusedIndex === idx, onClick: () => onValueChange(value), onMouseEvent: setFocusedIndex, icon: EllipseMiniSolid });
      }),
      !disableRangeOption && React63.createElement(OptionButton, { index: options.length, option: {
        label: rangeOptionLabel,
        value: "__custom"
      }, icon: EllipseMiniSolid, isSelected: isCustom, isFocused: focusedIndex === options.length, onClick: onSelectCustom, onMouseEvent: setFocusedIndex })
    ),
    !disableRangeOption && isCustom && React63.createElement(
      React63.Fragment,
      null,
      React63.createElement(
        "div",
        { className: "flex flex-col py-[3px]" },
        React63.createElement("div", { className: "bg-ui-border-menu-top h-px w-full" }),
        React63.createElement("div", { className: "bg-ui-border-menu-bot h-px w-full" })
      ),
      React63.createElement(
        "div",
        { className: "flex flex-col gap-2 px-2 pb-3 pt-1" },
        React63.createElement(
          "div",
          { className: "flex flex-col gap-1" },
          React63.createElement(Label2, { id: "custom-start-date-label", size: "xsmall", weight: "plus" }, rangeOptionStartLabel),
          React63.createElement(DatePicker, { "aria-labelledby": "custom-start-date-label", granularity, maxValue: maxDate, value: (currentValue === null || currentValue === void 0 ? void 0 : currentValue.$gte) ? new Date(currentValue.$gte) : null, onChange: (value) => onCustomValueChange("$gte", value) })
        ),
        React63.createElement(
          "div",
          { className: "flex flex-col gap-1" },
          React63.createElement(Label2, { id: "custom-end-date-label", size: "xsmall", weight: "plus" }, rangeOptionEndLabel),
          React63.createElement(DatePicker, { "aria-labelledby": "custom-end-date-label", granularity, minValue: minDate, value: (currentValue === null || currentValue === void 0 ? void 0 : currentValue.$lte) ? new Date(currentValue.$lte) : null, onChange: (value) => onCustomValueChange("$lte", value) })
        )
      )
    )
  );
};
var DataTableFilterSelectContent = ({ id, filter = [], options, isNew = false, onUpdate }) => {
  const { instance } = useDataTableContext();
  const [search, setSearch] = React63.useState("");
  const filteredOptions = React63.useMemo(() => {
    if (!search)
      return options;
    const searchLower = search.toLowerCase();
    return options.filter((opt) => opt.label.toLowerCase().includes(searchLower));
  }, [options, search]);
  const onValueChange = React63.useCallback((value) => {
    if (filter === null || filter === void 0 ? void 0 : filter.includes(value)) {
      const newValues = filter === null || filter === void 0 ? void 0 : filter.filter((v) => v !== value);
      const newValue = newValues.length > 0 ? newValues : void 0;
      if (onUpdate) {
        onUpdate(newValue);
      } else {
        instance.updateFilter({
          id,
          value: newValue
        });
      }
    } else {
      const newValue = [...filter !== null && filter !== void 0 ? filter : [], value];
      if (onUpdate) {
        onUpdate(newValue);
      } else {
        instance.updateFilter({
          id,
          value: newValue
        });
      }
    }
  }, [instance, id, filter, onUpdate]);
  return React63.createElement(
    "div",
    { className: "w-[250px]" },
    React63.createElement(
      "div",
      { className: "flex items-center gap-x-2 border-b px-3 py-1.5" },
      React63.createElement(MagnifyingGlass, { className: "h-4 w-4 text-ui-fg-muted" }),
      React63.createElement("input", { value: search, onChange: (e) => setSearch(e.target.value), placeholder: "Search...", className: "h-8 flex-1 bg-transparent text-sm outline-none placeholder:text-ui-fg-muted", autoFocus: true }),
      search && React63.createElement(
        "button",
        { onClick: () => setSearch(""), className: "text-ui-fg-muted hover:text-ui-fg-subtle" },
        React63.createElement(XMarkMini, { className: "h-4 w-4" })
      )
    ),
    React63.createElement(
      "div",
      { className: "max-h-[300px] overflow-auto p-1" },
      filteredOptions.length === 0 && React63.createElement("div", { className: "py-6 text-center text-sm text-ui-fg-muted" }, "No results found"),
      filteredOptions.map((option) => {
        const isSelected = filter === null || filter === void 0 ? void 0 : filter.includes(option.value);
        return React63.createElement(
          "button",
          { key: String(option.value), onClick: () => onValueChange(option.value), className: clx("flex w-full cursor-pointer items-center gap-x-2 rounded-md px-2 py-1.5 text-sm text-left", "hover:bg-ui-bg-base-hover") },
          React63.createElement("div", { className: "flex size-[15px] items-center justify-center" }, isSelected && React63.createElement(CheckMini, null)),
          React63.createElement("span", null, option.label)
        );
      })
    )
  );
};
var DataTableFilterRadioContent = ({ id, filter, options, onUpdate }) => {
  const { instance } = useDataTableContext();
  const onValueChange = React63.useCallback((value) => {
    if (onUpdate) {
      onUpdate(value);
    } else {
      instance.updateFilter({ id, value });
    }
  }, [instance, id, onUpdate]);
  const { focusedIndex, setFocusedIndex } = useKeyboardNavigation(options, (index) => onValueChange(options[index].value));
  const onListFocus = React63.useCallback(() => {
    if (focusedIndex === -1) {
      setFocusedIndex(0);
    }
  }, [focusedIndex]);
  return React63.createElement("div", { className: "flex flex-col p-1 outline-none", role: "list", tabIndex: 0, onFocus: onListFocus, autoFocus: true }, options.map((option, idx) => {
    const isSelected = filter === option.value;
    return React63.createElement(OptionButton, { key: idx, index: idx, option, isSelected, isFocused: focusedIndex === idx, onClick: () => onValueChange(option.value), onMouseEvent: setFocusedIndex, icon: EllipseMiniSolid });
  }));
};
function isDateFilterProps(props) {
  if (!props) {
    return false;
  }
  return props.type === "date";
}
var OptionButton = React63.memo(({ index, option, isSelected, isFocused, onClick, onMouseEvent, icon: Icon }) => React63.createElement(
  "button",
  { type: "button", role: "listitem", className: clx("bg-ui-bg-component txt-compact-small transition-fg flex items-center gap-2 rounded px-2 py-1 outline-none", { "bg-ui-bg-component-hover": isFocused }), onClick, onMouseEnter: () => onMouseEvent(index), onMouseLeave: () => onMouseEvent(-1), tabIndex: -1 },
  React63.createElement("div", { className: "flex size-[15px] items-center justify-center" }, isSelected && React63.createElement(Icon, null)),
  React63.createElement("span", null, option.label)
));
function useKeyboardNavigation(options, onSelect, extraItems = 0) {
  const [focusedIndex, setFocusedIndex] = React63.useState(-1);
  const onKeyDown = React63.useCallback((e) => {
    const totalLength = options.length + extraItems;
    if (document.activeElement.contentEditable === "true") {
      return;
    }
    switch (e.key) {
      case "ArrowDown":
        e.preventDefault();
        setFocusedIndex((prev) => prev < totalLength - 1 ? prev + 1 : prev);
        break;
      case "ArrowUp":
        e.preventDefault();
        setFocusedIndex((prev) => prev > 0 ? prev - 1 : prev);
        break;
      case " ":
      case "Enter":
        e.preventDefault();
        if (focusedIndex >= 0) {
          onSelect(focusedIndex);
        }
        break;
    }
  }, [options.length, extraItems, focusedIndex, onSelect]);
  React63.useEffect(() => {
    window.addEventListener("keydown", onKeyDown);
    return () => {
      window.removeEventListener("keydown", onKeyDown);
    };
  }, [onKeyDown]);
  return { focusedIndex, setFocusedIndex };
}
var DataTableFilterMultiselectContent = ({ id, filter = [], options, searchable = true, onUpdate }) => {
  const { instance } = useDataTableContext();
  const [search, setSearch] = React63.useState("");
  const filteredOptions = React63.useMemo(() => {
    if (!searchable || !search)
      return options;
    const searchLower = search.toLowerCase();
    return options.filter((opt) => opt.label.toLowerCase().includes(searchLower));
  }, [options, search, searchable]);
  const onValueChange = React63.useCallback((value) => {
    if (filter === null || filter === void 0 ? void 0 : filter.includes(value)) {
      const newValues = filter === null || filter === void 0 ? void 0 : filter.filter((v) => v !== value);
      const newValue = newValues.length > 0 ? newValues : void 0;
      if (onUpdate) {
        onUpdate(newValue);
      } else {
        instance.updateFilter({
          id,
          value: newValue
        });
      }
    } else {
      const newValue = [...filter !== null && filter !== void 0 ? filter : [], value];
      if (onUpdate) {
        onUpdate(newValue);
      } else {
        instance.updateFilter({
          id,
          value: newValue
        });
      }
    }
  }, [instance, id, filter, onUpdate]);
  if (!searchable) {
    return React63.createElement(
      "div",
      { className: "w-[250px]" },
      React63.createElement("div", { className: "max-h-[300px] overflow-auto p-1" }, options.map((option) => {
        const isSelected = filter === null || filter === void 0 ? void 0 : filter.includes(option.value);
        return React63.createElement(
          "button",
          { key: String(option.value), onClick: () => onValueChange(option.value), className: clx("flex w-full items-center gap-x-2 rounded-md px-2 py-1.5 text-sm", "hover:bg-ui-bg-base-hover cursor-pointer text-left") },
          React63.createElement(Checkbox, { checked: isSelected, className: "pointer-events-none" }),
          React63.createElement("span", null, option.label)
        );
      }))
    );
  }
  return React63.createElement(
    "div",
    { className: "w-[250px]" },
    React63.createElement(
      "div",
      { className: "flex items-center gap-x-2 border-b px-3 py-1.5" },
      React63.createElement(MagnifyingGlass, { className: "h-4 w-4 text-ui-fg-muted" }),
      React63.createElement("input", { value: search, onChange: (e) => setSearch(e.target.value), placeholder: "Search...", className: "h-8 flex-1 bg-transparent text-sm outline-none placeholder:text-ui-fg-muted", autoFocus: true }),
      search && React63.createElement(
        "button",
        { onClick: () => setSearch(""), className: "text-ui-fg-muted hover:text-ui-fg-subtle" },
        React63.createElement(XMarkMini, { className: "h-4 w-4" })
      )
    ),
    React63.createElement(
      "div",
      { className: "max-h-[300px] overflow-auto p-1" },
      filteredOptions.length === 0 && React63.createElement("div", { className: "py-6 text-center text-sm text-ui-fg-muted" }, "No results found"),
      filteredOptions.map((option) => {
        const isSelected = filter === null || filter === void 0 ? void 0 : filter.includes(option.value);
        return React63.createElement(
          "button",
          { key: String(option.value), onClick: () => onValueChange(option.value), className: clx("flex w-full cursor-pointer items-center gap-x-2 rounded-md px-2 py-1.5 text-sm text-left", "hover:bg-ui-bg-base-hover") },
          React63.createElement(Checkbox, { checked: isSelected, className: "pointer-events-none" }),
          React63.createElement("span", null, option.label)
        );
      })
    )
  );
};
var DataTableFilterStringContent = ({ id, filter, placeholder = "Enter value...", onUpdate }) => {
  const { instance } = useDataTableContext();
  const [value, setValue] = React63.useState(filter || "");
  const timeoutRef = React63.useRef(null);
  const handleChange = React63.useCallback((newValue) => {
    setValue(newValue);
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
    }
    timeoutRef.current = setTimeout(() => {
      const updateValue = newValue.trim() || void 0;
      if (onUpdate) {
        onUpdate(updateValue);
      } else {
        instance.updateFilter({
          id,
          value: updateValue
        });
      }
    }, 500);
  }, [instance, id, onUpdate]);
  React63.useEffect(() => {
    return () => {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
    };
  }, []);
  const handleKeyDown = React63.useCallback((e) => {
    if (e.key === "Enter") {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
      const updateValue = value.trim() || void 0;
      if (onUpdate) {
        onUpdate(updateValue);
      } else {
        instance.updateFilter({
          id,
          value: updateValue
        });
      }
    }
  }, [instance, id, value, onUpdate]);
  return React63.createElement(
    "div",
    { className: "p-3 w-[250px]" },
    React63.createElement(Input, { placeholder, value, onChange: (e) => handleChange(e.target.value), onKeyDown: handleKeyDown, autoFocus: true })
  );
};
var DataTableFilterNumberContent = ({ id, filter, placeholder = "Enter number...", includeOperators = true, onUpdate }) => {
  const { instance } = useDataTableContext();
  const [operator, setOperator] = React63.useState("eq");
  const [value, setValue] = React63.useState("");
  const timeoutRef = React63.useRef(null);
  React63.useEffect(() => {
    if (filter) {
      if (typeof filter === "number") {
        setOperator("eq");
        setValue(String(filter));
      } else if (typeof filter === "object") {
        const op = Object.keys(filter)[0];
        setOperator(op.replace("$", ""));
        setValue(String(filter[op]));
      }
    }
  }, [filter]);
  const handleValueChange = React63.useCallback((newValue) => {
    setValue(newValue);
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
    }
    timeoutRef.current = setTimeout(() => {
      const num = parseFloat(newValue);
      if (!isNaN(num)) {
        const filterValue = includeOperators && operator !== "eq" ? { [`$${operator}`]: num } : num;
        if (onUpdate) {
          onUpdate(filterValue);
        } else {
          instance.updateFilter({
            id,
            value: filterValue
          });
        }
      } else if (newValue === "") {
        if (onUpdate) {
          onUpdate(void 0);
        } else {
          instance.updateFilter({
            id,
            value: void 0
          });
        }
      }
    }, 500);
  }, [instance, id, operator, includeOperators, onUpdate]);
  const handleOperatorChange = React63.useCallback((newOperator) => {
    setOperator(newOperator);
    const num = parseFloat(value);
    if (!isNaN(num)) {
      const filterValue = includeOperators && newOperator !== "eq" ? { [`$${newOperator}`]: num } : num;
      if (onUpdate) {
        onUpdate(filterValue);
      } else {
        instance.updateFilter({
          id,
          value: filterValue
        });
      }
    }
  }, [instance, id, value, includeOperators, onUpdate]);
  React63.useEffect(() => {
    return () => {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
    };
  }, []);
  const handleKeyDown = React63.useCallback((e) => {
    if (e.key === "Enter") {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
      const num = parseFloat(value);
      if (!isNaN(num)) {
        const filterValue = includeOperators && operator !== "eq" ? { [`$${operator}`]: num } : num;
        if (onUpdate) {
          onUpdate(filterValue);
        } else {
          instance.updateFilter({
            id,
            value: filterValue
          });
        }
      }
    }
  }, [instance, id, value, operator, includeOperators, onUpdate]);
  const operators = [
    { value: "eq", label: "Equals" },
    { value: "gt", label: "Greater than" },
    { value: "gte", label: "Greater than or equal" },
    { value: "lt", label: "Less than" },
    { value: "lte", label: "Less than or equal" }
  ];
  return React63.createElement(
    "div",
    { className: "p-3 space-y-3 w-[250px]" },
    includeOperators && React63.createElement(
      Select,
      { value: operator, onValueChange: handleOperatorChange },
      React63.createElement(
        Select.Trigger,
        null,
        React63.createElement(Select.Value, null)
      ),
      React63.createElement(Select.Content, null, operators.map((op) => React63.createElement(Select.Item, { key: op.value, value: op.value }, op.label)))
    ),
    React63.createElement(Input, { type: "number", placeholder, value, onChange: (e) => handleValueChange(e.target.value), onKeyDown: handleKeyDown, autoFocus: !includeOperators })
  );
};
var DataTableFilterCustomContent = ({ id, filter, onRemove, render, onUpdate }) => {
  const { instance } = useDataTableContext();
  const handleChange = React63.useCallback((value) => {
    if (onUpdate) {
      onUpdate(value);
    } else {
      instance.updateFilter({
        id,
        value
      });
    }
  }, [instance, id, onUpdate]);
  return React63.createElement(React63.Fragment, null, render({
    value: filter,
    onChange: handleChange,
    onRemove
  }));
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-filter-menu.js
var React64 = __toESM(require_react());
var DataTableFilterMenu = ({ tooltip, onAddFilter }) => {
  const { instance } = useDataTableContext();
  const enabledFilters = Object.keys(instance.getFiltering());
  const filterOptions = instance.getFilters().filter((filter) => !enabledFilters.includes(filter.id));
  if (!enabledFilters.length && !filterOptions.length) {
    throw new Error("DataTable.FilterMenu was rendered but there are no filters to apply. Make sure to pass filters to 'useDataTable'");
  }
  const Wrapper = tooltip ? Tooltip : React64.Fragment;
  const wrapperProps = tooltip ? { content: tooltip, hidden: filterOptions.length === 0 } : {};
  if (instance.showSkeleton) {
    return React64.createElement(DataTableFilterMenuSkeleton, null);
  }
  return React64.createElement(
    DropdownMenu,
    null,
    React64.createElement(
      Wrapper,
      { ...wrapperProps },
      React64.createElement(
        DropdownMenu.Trigger,
        { asChild: true, disabled: filterOptions.length === 0 },
        React64.createElement(
          IconButton,
          { size: "small" },
          React64.createElement(Funnel, null)
        )
      )
    ),
    React64.createElement(DropdownMenu.Content, { side: "bottom", align: "start" }, filterOptions.map((filter) => {
      const getDefaultValue = () => {
        switch (filter.type) {
          case "select":
          case "multiselect":
            return [];
          case "string":
            return "";
          case "number":
            return null;
          case "date":
            return null;
          case "radio":
            return null;
          case "custom":
            return null;
          default:
            return null;
        }
      };
      return React64.createElement(DropdownMenu.Item, { key: filter.id, onClick: (e) => {
        e.stopPropagation();
        if (onAddFilter) {
          onAddFilter(filter.id, getDefaultValue());
        } else {
          instance.addFilter({ id: filter.id, value: getDefaultValue() });
        }
      } }, filter.label);
    }))
  );
};
DataTableFilterMenu.displayName = "DataTable.FilterMenu";
var DataTableFilterMenuSkeleton = () => {
  return React64.createElement(Skeleton, { className: "size-7" });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-sorting-menu.js
var React65 = __toESM(require_react());
var DataTableSortingMenu = (props) => {
  const { instance } = useDataTableContext();
  const sortableColumns = instance.getAllColumns().filter((column) => column.getCanSort());
  const sorting = instance.getSorting();
  const selectedColumn = React65.useMemo(() => {
    return sortableColumns.find((column) => column.id === (sorting === null || sorting === void 0 ? void 0 : sorting.id));
  }, [sortableColumns, sorting]);
  const setKey = React65.useCallback((key) => {
    instance.setSorting((prev) => {
      var _a;
      return { id: key, desc: (_a = prev === null || prev === void 0 ? void 0 : prev.desc) !== null && _a !== void 0 ? _a : false };
    });
  }, [instance]);
  const setDesc = React65.useCallback((desc) => {
    instance.setSorting((prev) => {
      var _a;
      return {
        id: (_a = prev === null || prev === void 0 ? void 0 : prev.id) !== null && _a !== void 0 ? _a : "",
        desc: desc === "true"
      };
    });
  }, [instance]);
  if (!instance.enableSorting) {
    throw new Error("DataTable.SortingMenu was rendered but sorting is not enabled. Make sure to pass sorting to 'useDataTable'");
  }
  if (!sortableColumns.length) {
    throw new Error("DataTable.SortingMenu was rendered but there are no sortable columns. Make sure to set `enableSorting` to true on at least one column.");
  }
  if (instance.showSkeleton) {
    return React65.createElement(DataTableSortingMenuSkeleton, null);
  }
  const Wrapper = props.tooltip ? Tooltip : React65.Fragment;
  const wrapperProps = props.tooltip ? { content: props.tooltip } : {};
  return React65.createElement(
    DropdownMenu,
    null,
    React65.createElement(
      Wrapper,
      { ...wrapperProps },
      React65.createElement(
        DropdownMenu.Trigger,
        { asChild: true },
        React65.createElement(
          IconButton,
          { size: "small" },
          React65.createElement(DescendingSorting, null)
        )
      )
    ),
    React65.createElement(
      DropdownMenu.Content,
      { side: "bottom" },
      React65.createElement(DropdownMenu.RadioGroup, { value: sorting === null || sorting === void 0 ? void 0 : sorting.id, onValueChange: setKey }, sortableColumns.map((column) => {
        return React65.createElement(DropdownMenu.RadioItem, { onSelect: (e) => e.preventDefault(), value: column.id, key: column.id }, getSortLabel(column));
      })),
      sorting && React65.createElement(
        React65.Fragment,
        null,
        React65.createElement(DropdownMenu.Separator, null),
        React65.createElement(
          DropdownMenu.RadioGroup,
          { value: (sorting === null || sorting === void 0 ? void 0 : sorting.desc) ? "true" : "false", onValueChange: setDesc },
          React65.createElement(
            DropdownMenu.RadioItem,
            { onSelect: (e) => e.preventDefault(), value: "false", className: "flex items-center gap-2" },
            React65.createElement(ArrowUpMini, { className: "text-ui-fg-subtle" }),
            getSortDescriptor("asc", selectedColumn)
          ),
          React65.createElement(
            DropdownMenu.RadioItem,
            { onSelect: (e) => e.preventDefault(), value: "true", className: "flex items-center gap-2" },
            React65.createElement(ArrowDownMini, { className: "text-ui-fg-subtle" }),
            getSortDescriptor("desc", selectedColumn)
          )
        )
      )
    )
  );
};
DataTableSortingMenu.displayName = "DataTable.SortingMenu";
function getSortLabel(column) {
  var _a, _b, _c;
  const meta = column.columnDef.meta;
  let headerValue = void 0;
  if (typeof column.columnDef.header === "string") {
    headerValue = column.columnDef.header;
  }
  return (_c = (_b = (_a = meta === null || meta === void 0 ? void 0 : meta.___sortMetaData) === null || _a === void 0 ? void 0 : _a.sortLabel) !== null && _b !== void 0 ? _b : headerValue) !== null && _c !== void 0 ? _c : column.id;
}
function getSortDescriptor(direction, column) {
  var _a, _b, _c, _d;
  if (!column) {
    return null;
  }
  const meta = column.columnDef.meta;
  switch (direction) {
    case "asc":
      return (_b = (_a = meta === null || meta === void 0 ? void 0 : meta.___sortMetaData) === null || _a === void 0 ? void 0 : _a.sortAscLabel) !== null && _b !== void 0 ? _b : "A-Z";
    case "desc":
      return (_d = (_c = meta === null || meta === void 0 ? void 0 : meta.___sortMetaData) === null || _c === void 0 ? void 0 : _c.sortDescLabel) !== null && _d !== void 0 ? _d : "Z-A";
  }
}
var DataTableSortingMenuSkeleton = () => {
  return React65.createElement(Skeleton, { className: "size-7" });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-filter-bar.js
var import_lodash = __toESM(require_lodash());
var DataTableFilterBar = ({ clearAllFiltersLabel = "Clear all", alwaysShow = false, sortingTooltip, columnsTooltip, children }) => {
  const { instance, enableColumnVisibility } = useDataTableContext();
  const [localFilters, setLocalFilters] = React66.useState([]);
  const parentFilterState = instance.getFiltering();
  const availableFilters = instance.getFilters();
  React66.useEffect(() => {
    setLocalFilters((prevLocalFilters) => {
      const parentIds = Object.keys(parentFilterState);
      const localIds = prevLocalFilters.map((f) => f.id);
      const updatedLocalFilters = prevLocalFilters.filter((f) => parentIds.includes(f.id) || f.isNew).map((f) => {
        if (parentIds.includes(f.id)) {
          const parentValue = parentFilterState[f.id];
          if (!(0, import_lodash.default)(f.value, parentValue) || f.isNew) {
            return {
              id: f.id,
              value: parentValue,
              isNew: false
            };
          }
        }
        return f;
      });
      parentIds.forEach((id) => {
        if (!localIds.includes(id)) {
          updatedLocalFilters.push({
            id,
            value: parentFilterState[id],
            isNew: false
          });
        }
      });
      if (updatedLocalFilters.length !== prevLocalFilters.length) {
        return updatedLocalFilters;
      }
      const hasChanges = updatedLocalFilters.some((f, i) => {
        const prev = prevLocalFilters[i];
        return !prev || f.id !== prev.id || !(0, import_lodash.default)(f.value, prev.value) || f.isNew !== prev.isNew;
      });
      return hasChanges ? updatedLocalFilters : prevLocalFilters;
    });
  }, [parentFilterState]);
  const addLocalFilter = React66.useCallback((id, value) => {
    setLocalFilters((prev) => [...prev, { id, value, isNew: true }]);
  }, []);
  const updateLocalFilter = React66.useCallback((id, value) => {
    setLocalFilters((prev) => prev.map((f) => f.id === id ? { ...f, value, isNew: false } : f));
    if (value !== void 0 && value !== null && value !== "" && !(Array.isArray(value) && value.length === 0)) {
      instance.updateFilter({ id, value });
    }
  }, [instance]);
  const removeLocalFilter = React66.useCallback((id) => {
    setLocalFilters((prev) => prev.filter((f) => f.id !== id));
    if (parentFilterState[id] !== void 0) {
      instance.removeFilter(id);
    }
  }, [instance, parentFilterState]);
  const clearFilters = React66.useCallback(() => {
    setLocalFilters([]);
    instance.clearFilters();
  }, [instance]);
  const filterCount = localFilters.length;
  const hasAvailableFilters = availableFilters.length > 0;
  const sortableColumns = instance.getAllColumns().filter((column) => column.getCanSort());
  const hasSorting = instance.enableSorting && sortableColumns.length > 0;
  if (filterCount === 0 && !hasAvailableFilters && !hasSorting && !enableColumnVisibility && !alwaysShow && !children) {
    return null;
  }
  if (instance.showSkeleton) {
    return React66.createElement(DataTableFilterBarSkeleton, { filterCount });
  }
  return React66.createElement(
    "div",
    { className: "bg-ui-bg-subtle flex w-full flex-nowrap items-center justify-between gap-2 overflow-x-auto border-t px-6 py-2" },
    React66.createElement(
      "div",
      { className: "flex flex-nowrap items-center gap-2 md:flex-wrap" },
      localFilters.map((localFilter) => React66.createElement(DataTableFilter, { key: localFilter.id, id: localFilter.id, filter: localFilter.value, isNew: localFilter.isNew, onUpdate: (value) => updateLocalFilter(localFilter.id, value), onRemove: () => removeLocalFilter(localFilter.id) })),
      hasAvailableFilters && React66.createElement(DataTableFilterMenu, { onAddFilter: addLocalFilter })
    ),
    React66.createElement(
      "div",
      { className: "flex flex-shrink-0 items-center gap-2" },
      hasSorting && React66.createElement(DataTableSortingMenu, { tooltip: sortingTooltip }),
      enableColumnVisibility && React66.createElement(DataTableColumnVisibilityMenu, { tooltip: columnsTooltip }),
      children
    )
  );
};
DataTableFilterBar.displayName = "DataTable.FilterBar";
var DataTableFilterBarSkeleton = ({ filterCount }) => {
  return React66.createElement(
    "div",
    { className: "bg-ui-bg-subtle flex w-full flex-nowrap items-center gap-2 overflow-x-auto border-t px-6 py-2 md:flex-wrap" },
    Array.from({ length: filterCount }).map((_, index) => React66.createElement(Skeleton, { key: index, className: "h-7 w-[180px]" })),
    filterCount > 0 ? React66.createElement(Skeleton, { className: "h-7 w-[66px]" }) : null
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-pagination.js
var React67 = __toESM(require_react());
var DataTablePagination = (props) => {
  const { instance } = useDataTableContext();
  if (!instance.enablePagination) {
    throw new Error("DataTable.Pagination was rendered but pagination is not enabled. Make sure to pass pagination to 'useDataTable'");
  }
  if (instance.showSkeleton) {
    return React67.createElement(DataTablePaginationSkeleton, null);
  }
  return React67.createElement(Table.Pagination, { translations: props.translations, className: "flex-shrink-0", canNextPage: instance.getCanNextPage(), canPreviousPage: instance.getCanPreviousPage(), pageCount: instance.getPageCount(), count: instance.rowCount, nextPage: instance.nextPage, previousPage: instance.previousPage, pageIndex: instance.pageIndex, pageSize: instance.pageSize });
};
DataTablePagination.displayName = "DataTable.Pagination";
var DataTablePaginationSkeleton = () => {
  return React67.createElement(
    "div",
    null,
    React67.createElement(
      "div",
      { className: "flex items-center justify-between p-4" },
      React67.createElement(Skeleton, { className: "h-7 w-[138px]" }),
      React67.createElement(
        "div",
        { className: "flex items-center gap-x-2" },
        React67.createElement(Skeleton, { className: "h-7 w-24" }),
        React67.createElement(Skeleton, { className: "h-7 w-11" }),
        React67.createElement(Skeleton, { className: "h-7 w-11" })
      )
    )
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-search.js
var React68 = __toESM(require_react());
var DataTableSearch = (props) => {
  const { className, ...rest } = props;
  const { instance } = useDataTableContext();
  if (!instance.enableSearch) {
    throw new Error("DataTable.Search was rendered but search is not enabled. Make sure to pass search to 'useDataTable'");
  }
  if (instance.showSkeleton) {
    return React68.createElement(DataTableSearchSkeleton, null);
  }
  return React68.createElement(Input, { size: "small", type: "search", value: instance.getSearch(), onChange: (e) => instance.onSearchChange(e.target.value), className: clx({
    "pr-[calc(15px+2px+8px)]": instance.isLoading
  }, className), ...rest });
};
DataTableSearch.displayName = "DataTable.Search";
var DataTableSearchSkeleton = () => {
  return React68.createElement(Skeleton, { className: "h-7 w-[128px]" });
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-table.js
var React72 = __toESM(require_react());

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/types.js
var DataTableEmptyState;
(function(DataTableEmptyState2) {
  DataTableEmptyState2["EMPTY"] = "EMPTY";
  DataTableEmptyState2["FILTERED_EMPTY"] = "FILTERED_EMPTY";
  DataTableEmptyState2["POPULATED"] = "POPULATED";
})(DataTableEmptyState || (DataTableEmptyState = {}));

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-sorting-icon.js
var React69 = __toESM(require_react());
var DataTableSortingIcon = (props) => {
  const isAscending = props.direction === "asc";
  const isDescending = props.direction === "desc";
  const isSorted = isAscending || isDescending;
  return React69.createElement(
    "svg",
    { width: "16", height: "15", viewBox: "0 0 16 15", fill: "none", xmlns: "http://www.w3.org/2000/svg", className: clx("opacity-0 transition-opacity group-hover:opacity-100", {
      "opacity-100": isSorted
    }) },
    React69.createElement("path", { d: "M5.82651 5.75C5.66344 5.74994 5.50339 5.71269 5.36308 5.64216C5.22277 5.57162 5.10736 5.47039 5.02891 5.34904C4.95045 5.22769 4.91184 5.09067 4.9171 4.95232C4.92236 4.81397 4.97131 4.67936 5.05882 4.56255L7.64833 1.10788C7.73055 0.998207 7.84403 0.907911 7.97827 0.845354C8.11252 0.782797 8.26318 0.75 8.41632 0.75C8.56946 0.75 8.72013 0.782797 8.85437 0.845354C8.98862 0.907911 9.1021 0.998207 9.18432 1.10788L11.7744 4.56255C11.862 4.67939 11.9109 4.81405 11.9162 4.95245C11.9214 5.09085 11.8827 5.2279 11.8042 5.34926C11.7257 5.47063 11.6102 5.57185 11.4698 5.64235C11.3294 5.71285 11.1693 5.75003 11.0061 5.75H5.82651Z", className: clx("fill-ui-fg-muted", {
      "fill-ui-fg-subtle": isAscending
    }) }),
    React69.createElement("path", { d: "M11.0067 9.25C11.1698 9.25006 11.3299 9.28731 11.4702 9.35784C11.6105 9.42838 11.7259 9.52961 11.8043 9.65096C11.8828 9.77231 11.9214 9.90933 11.9162 10.0477C11.9109 10.186 11.8619 10.3206 11.7744 10.4374L9.18492 13.8921C9.10271 14.0018 8.98922 14.0921 8.85498 14.1546C8.72074 14.2172 8.57007 14.25 8.41693 14.25C8.26379 14.25 8.11312 14.2172 7.97888 14.1546C7.84464 14.0921 7.73115 14.0018 7.64894 13.8921L5.05882 10.4374C4.97128 10.3206 4.92233 10.1859 4.9171 10.0476C4.91186 9.90915 4.95053 9.7721 5.02905 9.65074C5.10758 9.52937 5.22308 9.42815 5.36347 9.35765C5.50387 9.28715 5.664 9.24997 5.82712 9.25H11.0067Z", className: clx("fill-ui-fg-muted", {
      "fill-ui-fg-subtle": isDescending
    }) })
  );
};
DataTableSortingIcon.displayName = "DataTable.SortingIcon";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-sortable-header-cell.js
var React70 = __toESM(require_react());
var DataTableSortableHeaderCell = React70.forwardRef(({ id, children, className, style: propStyle, isFirstColumn, ...props }, ref) => {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
    id
  });
  const transformStyle = transform ? {
    x: transform.x,
    y: 0,
    scaleX: 1,
    scaleY: 1
  } : null;
  const style = {
    ...propStyle,
    transform: transformStyle ? CSS.Transform.toString(transformStyle) : void 0,
    transition,
    opacity: isDragging ? 0.8 : 1,
    zIndex: isDragging ? 50 : isFirstColumn ? 1 : void 0
  };
  const combineRefs = (element) => {
    setNodeRef(element);
    if (ref) {
      if (typeof ref === "function") {
        ref(element);
      } else {
        ref.current = element;
      }
    }
  };
  return React70.createElement(Table.HeaderCell, { ref: combineRefs, style, className: clx(className, "group/header-cell bg-ui-bg-base"), ...attributes, ...listeners, ...props }, children);
});
DataTableSortableHeaderCell.displayName = "DataTableSortableHeaderCell";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-non-sortable-header-cell.js
var React71 = __toESM(require_react());
var DataTableNonSortableHeaderCell = React71.forwardRef(({ id, children, className, style: propStyle, isFirstColumn, ...props }, ref) => {
  const { setNodeRef, transform, transition } = useSortable({
    id,
    disabled: true
    // Disable dragging
  });
  const transformStyle = transform ? {
    x: transform.x,
    y: 0,
    scaleX: transform.scaleX,
    scaleY: transform.scaleY
  } : null;
  const style = {
    ...propStyle,
    transform: transformStyle ? CSS.Transform.toString(transformStyle) : void 0,
    transition
  };
  const combineRefs = (element) => {
    setNodeRef(element);
    if (ref) {
      if (typeof ref === "function") {
        ref(element);
      } else {
        ref.current = element;
      }
    }
  };
  return React71.createElement(Table.HeaderCell, { ref: combineRefs, style, className, ...props }, children);
});
DataTableNonSortableHeaderCell.displayName = "DataTableNonSortableHeaderCell";

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-table.js
var DataTableTable = (props) => {
  const hoveredRowId = React72.useRef(null);
  const isKeyDown = React72.useRef(false);
  const [showStickyBorder, setShowStickyBorder] = React72.useState(false);
  const scrollableRef = React72.useRef(null);
  const { instance } = useDataTableContext();
  const pageIndex = instance.pageIndex;
  const columns = instance.getAllColumns();
  const hasSelect = columns.find((c) => c.id === "select");
  const hasActions = columns.find((c) => c.id === "action");
  const sortableItems = React72.useMemo(() => {
    if (instance.columnOrder && instance.columnOrder.length > 0) {
      return instance.columnOrder;
    }
    return columns.map((col) => col.id);
  }, [columns, instance.columnOrder]);
  const sensors = useSensors(useSensor(PointerSensor, {
    activationConstraint: {
      distance: 8
    }
  }), useSensor(KeyboardSensor, {
    coordinateGetter: sortableKeyboardCoordinates
  }));
  const handleDragEnd = (event) => {
    const { active, over } = event;
    if (active.id !== (over === null || over === void 0 ? void 0 : over.id) && (over === null || over === void 0 ? void 0 : over.id)) {
      const activeId = active.id;
      const overId = over.id;
      if (activeId === "select" || activeId === "action") {
        return;
      }
      if (overId === "select" || overId === "action") {
        return;
      }
      const currentOrder = instance.columnOrder && instance.columnOrder.length > 0 ? instance.columnOrder : columns.map((col) => col.id);
      const oldIndex = currentOrder.indexOf(activeId);
      const newIndex = currentOrder.indexOf(overId);
      if (oldIndex !== -1 && newIndex !== -1) {
        const newOrder = arrayMove(currentOrder, oldIndex, newIndex);
        instance.setColumnOrderFromArray(newOrder);
      }
    }
  };
  React72.useEffect(() => {
    const onKeyDownHandler = (event) => {
      const isEditableElementFocused = getIsEditableElementFocused();
      if (event.key.toLowerCase() === "x" && hoveredRowId && !isKeyDown.current && !isEditableElementFocused) {
        isKeyDown.current = true;
        const row = instance.getRowModel().rows.find((r) => r.id === hoveredRowId.current);
        if (row && row.getCanSelect()) {
          row.toggleSelected();
        }
      }
    };
    const onKeyUpHandler = (event) => {
      if (event.key.toLowerCase() === "x") {
        isKeyDown.current = false;
      }
    };
    document.addEventListener("keydown", onKeyDownHandler);
    document.addEventListener("keyup", onKeyUpHandler);
    return () => {
      document.removeEventListener("keydown", onKeyDownHandler);
      document.removeEventListener("keyup", onKeyUpHandler);
    };
  }, [hoveredRowId, instance]);
  const handleHorizontalScroll = (e) => {
    const scrollLeft = e.currentTarget.scrollLeft;
    if (scrollLeft > 0) {
      setShowStickyBorder(true);
    } else {
      setShowStickyBorder(false);
    }
  };
  React72.useEffect(() => {
    var _a;
    (_a = scrollableRef.current) === null || _a === void 0 ? void 0 : _a.scroll({ top: 0, left: 0 });
  }, [pageIndex]);
  if (instance.showSkeleton) {
    return React72.createElement(DataTableTableSkeleton, { pageSize: instance.pageSize });
  }
  return React72.createElement(
    "div",
    { className: "flex w-full flex-1 flex-col overflow-hidden" },
    instance.emptyState === DataTableEmptyState.POPULATED && (instance.enableColumnOrder ? React72.createElement(
      DndContext,
      { sensors, collisionDetection: closestCenter, onDragEnd: handleDragEnd },
      React72.createElement(
        "div",
        { ref: scrollableRef, onScroll: handleHorizontalScroll, className: "min-h-0 w-full flex-1 overflow-auto overscroll-auto border-y" },
        React72.createElement(
          Table,
          { className: "relative isolate w-full" },
          React72.createElement(Table.Header, { className: "shadow-ui-border-base sticky inset-x-0 top-0 z-[1] w-full border-b-0 border-t-0 shadow-[0_1px_1px_0]", style: { transform: "translate3d(0,0,0)" } }, instance.getHeaderGroups().map((headerGroup) => React72.createElement(
            Table.Row,
            { key: headerGroup.id, className: clx("border-b-0", {
              "[&_th:last-of-type]:w-[1%] [&_th:last-of-type]:whitespace-nowrap": hasActions,
              "[&_th:first-of-type]:w-[1%] [&_th:first-of-type]:whitespace-nowrap": hasSelect
            }) },
            React72.createElement(SortableContext, { items: sortableItems, strategy: horizontalListSortingStrategy }, headerGroup.headers.map((header, idx) => {
              var _a, _b;
              const canSort = header.column.getCanSort();
              const sortDirection = header.column.getIsSorted();
              const sortHandler = header.column.getToggleSortingHandler();
              const isActionHeader = header.id === "action";
              const isSelectHeader = header.id === "select";
              const isSpecialHeader = isActionHeader || isSelectHeader;
              const isDraggable = !isSpecialHeader;
              const Wrapper = canSort ? "button" : "div";
              const isFirstColumn = hasSelect ? idx === 1 : idx === 0;
              const headerAlign = ((_b = (_a = header.column.columnDef.meta) === null || _a === void 0 ? void 0 : _a.___alignMetaData) === null || _b === void 0 ? void 0 : _b.headerAlign) || "left";
              const isRightAligned = headerAlign === "right";
              const isCenterAligned = headerAlign === "center";
              const HeaderCellComponent = isDraggable ? DataTableSortableHeaderCell : DataTableNonSortableHeaderCell;
              return React72.createElement(
                HeaderCellComponent,
                { key: header.id, id: header.id, isFirstColumn, className: clx("whitespace-nowrap", {
                  "w-[calc(20px+24px+24px)] min-w-[calc(20px+24px+24px)] max-w-[calc(20px+24px+24px)]": isSelectHeader,
                  "w-[calc(28px+24px+4px)] min-w-[calc(28px+24px+4px)] max-w-[calc(28px+24px+4px)]": isActionHeader,
                  "after:absolute after:inset-y-0 after:right-0 after:h-full after:w-px after:bg-transparent after:content-['']": isFirstColumn,
                  "after:bg-ui-border-base": showStickyBorder && isFirstColumn,
                  "bg-ui-bg-subtle sticky": isFirstColumn || isSelectHeader,
                  "left-0": isSelectHeader || isFirstColumn && !hasSelect,
                  "left-[calc(20px+24px+24px)]": isFirstColumn && hasSelect
                }), style: !isSpecialHeader ? {
                  width: header.column.columnDef.size,
                  maxWidth: header.column.columnDef.maxSize,
                  minWidth: header.column.columnDef.minSize
                } : void 0 },
                React72.createElement(
                  Wrapper,
                  { type: canSort ? "button" : void 0, onClick: canSort ? sortHandler : void 0, onMouseDown: canSort ? (e) => e.stopPropagation() : void 0, className: clx("group flex cursor-default items-center gap-2", {
                    "cursor-pointer": canSort,
                    "w-full": isRightAligned || isCenterAligned,
                    "w-fit": !isRightAligned && !isCenterAligned,
                    "justify-end": isRightAligned,
                    "justify-center": isCenterAligned
                  }) },
                  canSort && isRightAligned && React72.createElement(DataTableSortingIcon, { direction: sortDirection }),
                  flexRender(header.column.columnDef.header, header.getContext()),
                  canSort && !isRightAligned && React72.createElement(DataTableSortingIcon, { direction: sortDirection })
                )
              );
            }))
          ))),
          React72.createElement(Table.Body, { className: "border-b-0 border-t-0" }, instance.getRowModel().rows.map((row) => {
            return React72.createElement(Table.Row, { key: row.id, onMouseEnter: () => hoveredRowId.current = row.id, onMouseLeave: () => hoveredRowId.current = null, onClick: (e) => {
              var _a;
              return (_a = instance.onRowClick) === null || _a === void 0 ? void 0 : _a.call(instance, e, row);
            }, className: clx("group/row last-of-type:border-b-0", {
              "cursor-pointer": !!instance.onRowClick
            }) }, row.getVisibleCells().map((cell, idx) => {
              const isSelectCell = cell.column.id === "select";
              const isActionCell = cell.column.id === "action";
              const isSpecialCell = isSelectCell || isActionCell;
              const isFirstColumn = hasSelect ? idx === 1 : idx === 0;
              return React72.createElement(Table.Cell, { key: cell.id, className: clx("items-stretch truncate whitespace-nowrap", {
                "w-[calc(20px+24px+24px)] min-w-[calc(20px+24px+24px)] max-w-[calc(20px+24px+24px)]": isSelectCell,
                "w-[calc(28px+24px+4px)] min-w-[calc(28px+24px+4px)] max-w-[calc(28px+24px+4px)]": isActionCell,
                "bg-ui-bg-base group-hover/row:bg-ui-bg-base-hover transition-fg sticky h-full": isFirstColumn || isSelectCell,
                "after:absolute after:inset-y-0 after:right-0 after:h-full after:w-px after:bg-transparent after:content-['']": isFirstColumn,
                "after:bg-ui-border-base": showStickyBorder && isFirstColumn,
                "left-0": isSelectCell || isFirstColumn && !hasSelect,
                "left-[calc(20px+24px+24px)]": isFirstColumn && hasSelect
              }), style: !isSpecialCell ? {
                width: cell.column.columnDef.size,
                maxWidth: cell.column.columnDef.maxSize,
                minWidth: cell.column.columnDef.minSize
              } : void 0 }, flexRender(cell.column.columnDef.cell, cell.getContext()));
            }));
          }))
        )
      )
    ) : React72.createElement(
      "div",
      { ref: scrollableRef, onScroll: handleHorizontalScroll, className: "min-h-0 w-full flex-1 overflow-auto overscroll-auto border-y" },
      React72.createElement(
        Table,
        { className: "relative isolate w-full" },
        React72.createElement(Table.Header, { className: "shadow-ui-border-base sticky inset-x-0 top-0 z-[1] w-full border-b-0 border-t-0 shadow-[0_1px_1px_0]", style: { transform: "translate3d(0,0,0)" } }, instance.getHeaderGroups().map((headerGroup) => React72.createElement(Table.Row, { key: headerGroup.id, className: clx("border-b-0", {
          "[&_th:last-of-type]:w-[1%] [&_th:last-of-type]:whitespace-nowrap": hasActions,
          "[&_th:first-of-type]:w-[1%] [&_th:first-of-type]:whitespace-nowrap": hasSelect
        }) }, headerGroup.headers.map((header, idx) => {
          var _a, _b;
          const canSort = header.column.getCanSort();
          const sortDirection = header.column.getIsSorted();
          const sortHandler = header.column.getToggleSortingHandler();
          const isActionHeader = header.id === "action";
          const isSelectHeader = header.id === "select";
          const isSpecialHeader = isActionHeader || isSelectHeader;
          const Wrapper = canSort ? "button" : "div";
          const isFirstColumn = hasSelect ? idx === 1 : idx === 0;
          const headerAlign = ((_b = (_a = header.column.columnDef.meta) === null || _a === void 0 ? void 0 : _a.___alignMetaData) === null || _b === void 0 ? void 0 : _b.headerAlign) || "left";
          const isRightAligned = headerAlign === "right";
          const isCenterAligned = headerAlign === "center";
          return React72.createElement(
            Table.HeaderCell,
            { key: header.id, className: clx("whitespace-nowrap", {
              "w-[calc(20px+24px+24px)] min-w-[calc(20px+24px+24px)] max-w-[calc(20px+24px+24px)]": isSelectHeader,
              "w-[calc(28px+24px+4px)] min-w-[calc(28px+24px+4px)] max-w-[calc(28px+24px+4px)]": isActionHeader,
              "after:absolute after:inset-y-0 after:right-0 after:h-full after:w-px after:bg-transparent after:content-['']": isFirstColumn,
              "after:bg-ui-border-base": showStickyBorder && isFirstColumn,
              "bg-ui-bg-subtle sticky": isFirstColumn || isSelectHeader,
              "left-0": isSelectHeader || isFirstColumn && !hasSelect,
              "left-[calc(20px+24px+24px)]": isFirstColumn && hasSelect
            }), style: !isSpecialHeader ? {
              width: header.column.columnDef.size,
              maxWidth: header.column.columnDef.maxSize,
              minWidth: header.column.columnDef.minSize
            } : void 0 },
            React72.createElement(
              Wrapper,
              { type: canSort ? "button" : void 0, onClick: canSort ? sortHandler : void 0, onMouseDown: canSort ? (e) => e.stopPropagation() : void 0, className: clx("group flex cursor-default items-center gap-2", {
                "cursor-pointer": canSort,
                "w-full": isRightAligned || isCenterAligned,
                "w-fit": !isRightAligned && !isCenterAligned,
                "justify-end": isRightAligned,
                "justify-center": isCenterAligned
              }) },
              canSort && isRightAligned && React72.createElement(DataTableSortingIcon, { direction: sortDirection }),
              flexRender(header.column.columnDef.header, header.getContext()),
              canSort && !isRightAligned && React72.createElement(DataTableSortingIcon, { direction: sortDirection })
            )
          );
        })))),
        React72.createElement(Table.Body, { className: "border-b-0 border-t-0" }, instance.getRowModel().rows.map((row) => {
          return React72.createElement(Table.Row, { key: row.id, onMouseEnter: () => hoveredRowId.current = row.id, onMouseLeave: () => hoveredRowId.current = null, onClick: (e) => {
            var _a;
            return (_a = instance.onRowClick) === null || _a === void 0 ? void 0 : _a.call(instance, e, row);
          }, className: clx("group/row last-of-type:border-b-0", {
            "cursor-pointer": !!instance.onRowClick
          }) }, row.getVisibleCells().map((cell, idx) => {
            const isSelectCell = cell.column.id === "select";
            const isActionCell = cell.column.id === "action";
            const isSpecialCell = isSelectCell || isActionCell;
            const isFirstColumn = hasSelect ? idx === 1 : idx === 0;
            return React72.createElement(Table.Cell, { key: cell.id, className: clx("items-stretch truncate whitespace-nowrap", {
              "w-[calc(20px+24px+24px)] min-w-[calc(20px+24px+24px)] max-w-[calc(20px+24px+24px)]": isSelectCell,
              "w-[calc(28px+24px+4px)] min-w-[calc(28px+24px+4px)] max-w-[calc(28px+24px+4px)]": isActionCell,
              "bg-ui-bg-base group-hover/row:bg-ui-bg-base-hover transition-fg sticky h-full": isFirstColumn || isSelectCell,
              "after:absolute after:inset-y-0 after:right-0 after:h-full after:w-px after:bg-transparent after:content-['']": isFirstColumn,
              "after:bg-ui-border-base": showStickyBorder && isFirstColumn,
              "left-0": isSelectCell || isFirstColumn && !hasSelect,
              "left-[calc(20px+24px+24px)]": isFirstColumn && hasSelect
            }), style: !isSpecialCell ? {
              width: cell.column.columnDef.size,
              maxWidth: cell.column.columnDef.maxSize,
              minWidth: cell.column.columnDef.minSize
            } : void 0 }, flexRender(cell.column.columnDef.cell, cell.getContext()));
          }));
        }))
      )
    )),
    React72.createElement(DataTableEmptyStateDisplay, { state: instance.emptyState, props: props.emptyState })
  );
};
DataTableTable.displayName = "DataTable.Table";
var DefaultEmptyStateContent = ({ heading, description }) => React72.createElement(
  "div",
  { className: "flex size-full flex-col items-center justify-center gap-2" },
  React72.createElement(Text, { size: "base", weight: "plus" }, heading),
  React72.createElement(Text, null, description)
);
var DataTableEmptyStateDisplay = ({ state, props }) => {
  var _a;
  if (state === DataTableEmptyState.POPULATED) {
    return null;
  }
  const content = state === DataTableEmptyState.EMPTY ? props === null || props === void 0 ? void 0 : props.empty : props === null || props === void 0 ? void 0 : props.filtered;
  return React72.createElement("div", { className: "flex min-h-[250px] w-full flex-1 flex-col items-center justify-center border-y px-6 py-4" }, (_a = content === null || content === void 0 ? void 0 : content.custom) !== null && _a !== void 0 ? _a : React72.createElement(DefaultEmptyStateContent, { heading: content === null || content === void 0 ? void 0 : content.heading, description: content === null || content === void 0 ? void 0 : content.description }));
};
var DataTableTableSkeleton = ({ pageSize = 10 }) => {
  return React72.createElement(
    "div",
    { className: "flex w-full flex-1 flex-col overflow-hidden" },
    React72.createElement(
      "div",
      { className: "min-h-0 w-full flex-1 overscroll-auto border-y" },
      React72.createElement(
        "div",
        { className: "flex flex-col divide-y" },
        React72.createElement(Skeleton, { className: "h-12 w-full" }),
        Array.from({ length: pageSize }, (_, i) => i).map((row) => React72.createElement(Skeleton, { key: row, className: "h-12 w-full rounded-none" }))
      )
    )
  );
};
function getIsEditableElementFocused() {
  const activeElement = !!document ? document.activeElement : null;
  const isEditableElementFocused = activeElement instanceof HTMLInputElement || activeElement instanceof HTMLTextAreaElement || (activeElement === null || activeElement === void 0 ? void 0 : activeElement.getAttribute("contenteditable")) === "true";
  return isEditableElementFocused;
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/components/data-table-toolbar.js
var React73 = __toESM(require_react());
var DataTableToolbar = (props) => {
  var _a, _b, _c;
  const { instance } = useDataTableContext();
  const hasFilters = instance.getFilters().length > 0;
  return React73.createElement(
    "div",
    { className: "flex flex-col divide-y" },
    React73.createElement("div", { className: clx("flex items-center px-6 py-4", props.className) }, props.children),
    React73.createElement(DataTableFilterBar, { clearAllFiltersLabel: (_a = props.translations) === null || _a === void 0 ? void 0 : _a.clearAll, alwaysShow: hasFilters, sortingTooltip: (_b = props.translations) === null || _b === void 0 ? void 0 : _b.sort, columnsTooltip: (_c = props.translations) === null || _c === void 0 ? void 0 : _c.columns }, props.filterBarContent)
  );
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/context/data-table-context-provider.js
var React74 = __toESM(require_react());
var DataTableContextProvider = ({ instance, children }) => {
  return React74.createElement(DataTableContext.Provider, { value: {
    instance,
    enableColumnVisibility: instance.enableColumnVisibility,
    enableColumnOrder: instance.enableColumnOrder
  } }, children);
};

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/data-table.js
var Root10 = ({ instance, children, className }) => {
  return React75.createElement(
    DataTableContextProvider,
    { instance },
    React75.createElement("div", { className: clx("relative flex min-h-0 flex-1 flex-col", className) }, children)
  );
};
Root10.displayName = "DataTable";
var DataTable = Object.assign(Root10, {
  Table: DataTableTable,
  Toolbar: DataTableToolbar,
  Search: DataTableSearch,
  SortingMenu: DataTableSortingMenu,
  FilterMenu: DataTableFilterMenu,
  FilterBar: DataTableFilterBar,
  ColumnVisibilityMenu: DataTableColumnVisibilityMenu,
  Pagination: DataTablePagination,
  CommandBar: DataTableCommandBar
});

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/blocks/data-table/use-data-table.js
var React76 = __toESM(require_react());
var useDataTable = ({ rowCount = 0, filters, commands, rowSelection, sorting, filtering, pagination, search, onRowClick, autoResetPageIndex = true, isLoading = false, columnVisibility, columnOrder, ...options }) => {
  var _a, _b, _c, _d, _e, _f;
  const { state: sortingState, onSortingChange } = sorting !== null && sorting !== void 0 ? sorting : {};
  const { state: filteringState, onFilteringChange } = filtering !== null && filtering !== void 0 ? filtering : {};
  const { state: paginationState, onPaginationChange } = pagination !== null && pagination !== void 0 ? pagination : {};
  const { state: rowSelectionState, onRowSelectionChange, enableRowSelection } = rowSelection !== null && rowSelection !== void 0 ? rowSelection : {};
  const { state: columnVisibilityState, onColumnVisibilityChange } = columnVisibility !== null && columnVisibility !== void 0 ? columnVisibility : {};
  const { state: columnOrderState, onColumnOrderChange } = columnOrder !== null && columnOrder !== void 0 ? columnOrder : {};
  const autoResetPageIndexHandler = React76.useCallback(() => {
    return autoResetPageIndex ? () => paginationState && (onPaginationChange === null || onPaginationChange === void 0 ? void 0 : onPaginationChange({ ...paginationState, pageIndex: 0 })) : void 0;
  }, [autoResetPageIndex, paginationState, onPaginationChange]);
  const sortingStateHandler = React76.useCallback(() => {
    return onSortingChange ? (updaterOrValue) => {
      var _a2;
      (_a2 = autoResetPageIndexHandler()) === null || _a2 === void 0 ? void 0 : _a2();
      onSortingChangeTransformer(onSortingChange, sortingState)(updaterOrValue);
    } : void 0;
  }, [onSortingChange, sortingState, autoResetPageIndexHandler]);
  const rowSelectionStateHandler = React76.useCallback(() => {
    return onRowSelectionChange ? (updaterOrValue) => {
      onRowSelectionChangeTransformer(onRowSelectionChange, rowSelectionState)(updaterOrValue);
    } : void 0;
  }, [onRowSelectionChange, rowSelectionState, autoResetPageIndexHandler]);
  const filteringStateHandler = React76.useCallback(() => {
    return onFilteringChange ? (updaterOrValue) => {
      var _a2;
      (_a2 = autoResetPageIndexHandler()) === null || _a2 === void 0 ? void 0 : _a2();
      onFilteringChangeTransformer(onFilteringChange, filteringState)(updaterOrValue);
    } : void 0;
  }, [onFilteringChange, filteringState, autoResetPageIndexHandler]);
  const paginationStateHandler = React76.useCallback(() => {
    return onPaginationChange ? onPaginationChangeTransformer(onPaginationChange, paginationState) : void 0;
  }, [onPaginationChange, paginationState]);
  const columnVisibilityStateHandler = React76.useCallback(() => {
    return onColumnVisibilityChange ? (updaterOrValue) => {
      const value = typeof updaterOrValue === "function" ? updaterOrValue(columnVisibilityState !== null && columnVisibilityState !== void 0 ? columnVisibilityState : {}) : updaterOrValue;
      onColumnVisibilityChange(value);
    } : void 0;
  }, [onColumnVisibilityChange, columnVisibilityState]);
  const columnOrderStateHandler = React76.useCallback(() => {
    return onColumnOrderChange ? (updaterOrValue) => {
      const value = typeof updaterOrValue === "function" ? updaterOrValue(columnOrderState !== null && columnOrderState !== void 0 ? columnOrderState : []) : updaterOrValue;
      onColumnOrderChange(value);
    } : void 0;
  }, [onColumnOrderChange, columnOrderState]);
  const instance = useReactTable({
    ...options,
    getCoreRowModel: getCoreRowModel(),
    state: {
      rowSelection: rowSelectionState !== null && rowSelectionState !== void 0 ? rowSelectionState : {},
      sorting: sortingState ? [sortingState] : void 0,
      columnFilters: Object.entries(filteringState !== null && filteringState !== void 0 ? filteringState : {}).map(([id, filter]) => ({
        id,
        value: filter
      })),
      pagination: paginationState,
      columnVisibility: columnVisibilityState !== null && columnVisibilityState !== void 0 ? columnVisibilityState : {},
      columnOrder: columnOrderState !== null && columnOrderState !== void 0 ? columnOrderState : []
    },
    enableRowSelection,
    rowCount,
    onColumnFiltersChange: filteringStateHandler(),
    onRowSelectionChange: rowSelectionStateHandler(),
    onSortingChange: sortingStateHandler(),
    onPaginationChange: paginationStateHandler(),
    onColumnVisibilityChange: columnVisibilityStateHandler(),
    onColumnOrderChange: columnOrderStateHandler(),
    manualSorting: true,
    manualPagination: true,
    manualFiltering: true
  });
  const getSorting = React76.useCallback(() => {
    var _a2, _b2;
    return (_b2 = (_a2 = instance.getState().sorting) === null || _a2 === void 0 ? void 0 : _a2[0]) !== null && _b2 !== void 0 ? _b2 : null;
  }, [instance]);
  const setSorting = React76.useCallback((sortingOrUpdater) => {
    var _a2, _b2, _c2;
    const currentSort = (_b2 = (_a2 = instance.getState().sorting) === null || _a2 === void 0 ? void 0 : _a2[0]) !== null && _b2 !== void 0 ? _b2 : null;
    const newSorting = typeof sortingOrUpdater === "function" ? sortingOrUpdater(currentSort) : sortingOrUpdater;
    (_c2 = autoResetPageIndexHandler()) === null || _c2 === void 0 ? void 0 : _c2();
    instance.setSorting([newSorting]);
  }, [instance, autoResetPageIndexHandler]);
  const getFilters = React76.useCallback(() => {
    return filters !== null && filters !== void 0 ? filters : [];
  }, [filters]);
  const getFilterOptions = React76.useCallback((id) => {
    const filter = getFilters().find((filter2) => filter2.id === id);
    if (!filter) {
      return null;
    }
    return filter.options || null;
  }, [getFilters]);
  const getFilterMeta = React76.useCallback((id) => {
    return getFilters().find((filter) => filter.id === id) || null;
  }, [getFilters]);
  const getFiltering = React76.useCallback(() => {
    var _a2;
    const state = (_a2 = instance.getState().columnFilters) !== null && _a2 !== void 0 ? _a2 : [];
    return Object.fromEntries(state.map((filter) => [filter.id, filter.value]));
  }, [instance]);
  const addFilter = React76.useCallback((filter) => {
    var _a2;
    const currentFilters = getFiltering();
    const newFilters = { ...currentFilters, [filter.id]: filter.value };
    (_a2 = autoResetPageIndexHandler()) === null || _a2 === void 0 ? void 0 : _a2();
    onFilteringChange === null || onFilteringChange === void 0 ? void 0 : onFilteringChange(newFilters);
  }, [onFilteringChange, getFiltering, autoResetPageIndexHandler]);
  const removeFilter = React76.useCallback((id) => {
    var _a2;
    const currentFilters = getFiltering();
    delete currentFilters[id];
    (_a2 = autoResetPageIndexHandler()) === null || _a2 === void 0 ? void 0 : _a2();
    onFilteringChange === null || onFilteringChange === void 0 ? void 0 : onFilteringChange(currentFilters);
  }, [onFilteringChange, getFiltering, autoResetPageIndexHandler]);
  const clearFilters = React76.useCallback(() => {
    var _a2;
    (_a2 = autoResetPageIndexHandler()) === null || _a2 === void 0 ? void 0 : _a2();
    onFilteringChange === null || onFilteringChange === void 0 ? void 0 : onFilteringChange({});
  }, [onFilteringChange, autoResetPageIndexHandler]);
  const updateFilter = React76.useCallback((filter) => {
    addFilter(filter);
  }, [addFilter]);
  const { state: searchState, onSearchChange, debounce = 300 } = search !== null && search !== void 0 ? search : {};
  const [localSearch, setLocalSearch] = React76.useState(searchState !== null && searchState !== void 0 ? searchState : "");
  const timeoutRef = React76.useRef();
  React76.useEffect(() => {
    setLocalSearch(searchState !== null && searchState !== void 0 ? searchState : "");
  }, [searchState]);
  const getSearch = React76.useCallback(() => {
    return localSearch;
  }, [localSearch]);
  const debouncedSearchChange = React76.useMemo(() => {
    if (!onSearchChange) {
      return void 0;
    }
    return (value) => {
      var _a2;
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
      if (debounce <= 0) {
        (_a2 = autoResetPageIndexHandler()) === null || _a2 === void 0 ? void 0 : _a2();
        onSearchChange(value);
        return;
      }
      timeoutRef.current = setTimeout(() => {
        var _a3;
        (_a3 = autoResetPageIndexHandler()) === null || _a3 === void 0 ? void 0 : _a3();
        onSearchChange(value);
      }, debounce);
    };
  }, [onSearchChange, debounce, autoResetPageIndexHandler]);
  React76.useEffect(() => {
    return () => {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
    };
  }, []);
  const onSearchChangeHandler = React76.useCallback((search2) => {
    setLocalSearch(search2);
    debouncedSearchChange === null || debouncedSearchChange === void 0 ? void 0 : debouncedSearchChange(search2);
  }, [debouncedSearchChange]);
  const getCommands = React76.useCallback(() => {
    return commands !== null && commands !== void 0 ? commands : [];
  }, [commands]);
  const getRowSelection = React76.useCallback(() => {
    return instance.getState().rowSelection;
  }, [instance]);
  const rows = instance.getRowModel().rows;
  const emptyState = React76.useMemo(() => {
    const hasRows = rows.length > 0;
    const hasSearch = Boolean(searchState);
    const hasFilters = Object.keys(filteringState !== null && filteringState !== void 0 ? filteringState : {}).length > 0;
    if (hasRows) {
      return DataTableEmptyState.POPULATED;
    }
    if (hasSearch || hasFilters) {
      return DataTableEmptyState.FILTERED_EMPTY;
    }
    return DataTableEmptyState.EMPTY;
  }, [rows, searchState, filteringState]);
  const showSkeleton = React76.useMemo(() => {
    return isLoading === true && rows.length === 0;
  }, [isLoading, rows]);
  const enablePagination = !!pagination;
  const enableFiltering = !!filtering;
  const enableSorting = !!sorting;
  const enableSearch = !!search;
  const enableColumnVisibility = !!columnVisibility;
  const enableColumnOrder = !!columnOrder;
  const setColumnOrderFromArray = React76.useCallback((order) => {
    instance.setColumnOrder(order);
  }, [instance]);
  return {
    // Table
    getHeaderGroups: instance.getHeaderGroups,
    getRowModel: instance.getRowModel,
    getAllColumns: instance.getAllColumns,
    setColumnVisibility: instance.setColumnVisibility,
    setColumnOrder: instance.setColumnOrder,
    // Pagination
    enablePagination,
    getCanNextPage: instance.getCanNextPage,
    getCanPreviousPage: instance.getCanPreviousPage,
    nextPage: instance.nextPage,
    previousPage: instance.previousPage,
    getPageCount: instance.getPageCount,
    pageIndex: (_c = (_b = (_a = instance.getState()) === null || _a === void 0 ? void 0 : _a.pagination) === null || _b === void 0 ? void 0 : _b.pageIndex) !== null && _c !== void 0 ? _c : 0,
    pageSize: (_f = (_e = (_d = instance.getState()) === null || _d === void 0 ? void 0 : _d.pagination) === null || _e === void 0 ? void 0 : _e.pageSize) !== null && _f !== void 0 ? _f : 10,
    rowCount,
    // Search
    enableSearch,
    getSearch,
    onSearchChange: onSearchChangeHandler,
    // Sorting
    enableSorting,
    getSorting,
    setSorting,
    // Filtering
    enableFiltering,
    getFilters,
    getFilterOptions,
    getFilterMeta,
    getFiltering,
    addFilter,
    removeFilter,
    clearFilters,
    updateFilter,
    // Commands
    getCommands,
    getRowSelection,
    // Handlers
    onRowClick,
    // Empty State
    emptyState,
    // Loading
    isLoading,
    showSkeleton,
    // Column Visibility
    enableColumnVisibility,
    // Column Order
    enableColumnOrder,
    columnOrder: instance.getState().columnOrder,
    setColumnOrderFromArray
  };
};
function onSortingChangeTransformer(onSortingChange, state) {
  return (updaterOrValue) => {
    const value = typeof updaterOrValue === "function" ? updaterOrValue(state ? [state] : []) : updaterOrValue;
    const columnSort = value[0];
    onSortingChange(columnSort);
  };
}
function onRowSelectionChangeTransformer(onRowSelectionChange, state) {
  return (updaterOrValue) => {
    const value = typeof updaterOrValue === "function" ? updaterOrValue(state !== null && state !== void 0 ? state : {}) : updaterOrValue;
    onRowSelectionChange(value);
  };
}
function onFilteringChangeTransformer(onFilteringChange, state) {
  return (updaterOrValue) => {
    const value = typeof updaterOrValue === "function" ? updaterOrValue(Object.entries(state !== null && state !== void 0 ? state : {}).map(([id, filter]) => ({
      id,
      value: filter
    }))) : updaterOrValue;
    const transformedValue = Object.fromEntries(value.map((filter) => [filter.id, filter.value]));
    onFilteringChange(transformedValue);
  };
}
function onPaginationChangeTransformer(onPaginationChange, state) {
  return (updaterOrValue) => {
    const value = typeof updaterOrValue === "function" ? updaterOrValue(state !== null && state !== void 0 ? state : { pageIndex: 0, pageSize: 10 }) : updaterOrValue;
    onPaginationChange(value);
  };
}

// node_modules/@medusajs/dashboard/node_modules/@medusajs/ui/dist/esm/hooks/use-toggle-state/use-toggle-state.js
var React77 = __toESM(require_react());

export {
  clx,
  IconButton,
  Alert,
  Avatar,
  Badge,
  Button,
  Checkbox,
  Tooltip,
  TooltipProvider,
  Copy,
  CodeBlock,
  OtpInput,
  Kbd,
  CommandBar,
  Container,
  Text,
  CurrencyInput2 as CurrencyInput,
  Popover,
  DatePicker,
  Divider,
  Drawer,
  DropdownMenu,
  FocusModal,
  Heading,
  Hint2 as Hint,
  I18nProvider,
  InlineTip,
  Input,
  Label2 as Label,
  ProgressTabs,
  Prompt,
  RadioGroup2 as RadioGroup,
  Select,
  StatusBadge,
  Switch,
  Table,
  Textarea,
  Toaster,
  DataTable,
  useDataTable,
  createDataTableColumnHelper,
  createDataTableCommandHelper,
  createDataTableFilterHelper,
  usePrompt,
  toast
};
//# sourceMappingURL=chunk-XG4NVSXW.js.map
