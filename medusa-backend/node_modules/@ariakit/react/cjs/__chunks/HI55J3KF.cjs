"use strict";Object.defineProperty(exports, "__esModule", {value: true});

var _62Q6LBYZcjs = require('./62Q6LBYZ.cjs');















var _7FULUV5Lcjs = require('./7FULUV5L.cjs');

// ../ariakit-react-components/dist/command/command.js
var _react = require('react');
var TagName = "button";
function isNativeClick(event) {
  if (!event.isTrusted) return false;
  const element = event.currentTarget;
  if (event.key === "Enter") return _7FULUV5Lcjs.isButton.call(void 0, element) || element.tagName === "SUMMARY" || element.tagName === "A";
  if (event.key === " ") return _7FULUV5Lcjs.isButton.call(void 0, element) || element.tagName === "SUMMARY" || element.tagName === "INPUT" || element.tagName === "SELECT";
  return false;
}
var symbol = /* @__PURE__ */ Symbol("command");
var useCommand = _7FULUV5Lcjs.createHook.call(void 0, function useCommand2({ clickOnEnter = true, clickOnSpace = true, ...props }) {
  const ref = _react.useRef.call(void 0, null);
  const [isNativeButton, setIsNativeButton] = _react.useState.call(void 0, false);
  _react.useEffect.call(void 0, () => {
    if (!ref.current) return;
    setIsNativeButton(_7FULUV5Lcjs.isButton.call(void 0, ref.current));
  }, []);
  const [active, setActive] = _react.useState.call(void 0, false);
  const activeRef = _react.useRef.call(void 0, false);
  const disabled = _7FULUV5Lcjs.disabledFromProps.call(void 0, props);
  const [isDuplicate, metadataProps] = _7FULUV5Lcjs.useMetadataProps.call(void 0, props, symbol, true);
  _7FULUV5Lcjs.useSafeLayoutEffect.call(void 0, () => {
    if (!disabled) return;
    activeRef.current = false;
    setActive(false);
  }, [disabled]);
  const onKeyDownProp = props.onKeyDown;
  const onKeyDown = _7FULUV5Lcjs.useEvent.call(void 0, (event) => {
    onKeyDownProp == null ? void 0 : onKeyDownProp(event);
    const element = event.currentTarget;
    if (event.defaultPrevented) return;
    if (isDuplicate) return;
    if (disabled) return;
    if (!_7FULUV5Lcjs.isSelfTarget.call(void 0, event)) return;
    if (_7FULUV5Lcjs.isTextField.call(void 0, element)) return;
    if (element.isContentEditable) return;
    const isEnter = clickOnEnter && event.key === "Enter";
    const isSpace = clickOnSpace && event.key === " ";
    const shouldPreventEnter = event.key === "Enter" && !clickOnEnter;
    const shouldPreventSpace = event.key === " " && !clickOnSpace;
    if (shouldPreventEnter || shouldPreventSpace) {
      event.preventDefault();
      return;
    }
    if (isEnter || isSpace) {
      const nativeClick = isNativeClick(event);
      if (isEnter) {
        if (!nativeClick) {
          event.preventDefault();
          const { view, ...eventInit } = event;
          const click = () => _7FULUV5Lcjs.fireClickEvent.call(void 0, element, eventInit);
          if (_7FULUV5Lcjs.isFirefox.call(void 0, )) _7FULUV5Lcjs.queueBeforeEvent.call(void 0, element, "keyup", click);
          else queueMicrotask(click);
        }
      } else if (isSpace) {
        activeRef.current = true;
        if (!nativeClick) {
          event.preventDefault();
          setActive(true);
        }
      }
    }
  });
  const onKeyUpProp = props.onKeyUp;
  const onKeyUp = _7FULUV5Lcjs.useEvent.call(void 0, (event) => {
    onKeyUpProp == null ? void 0 : onKeyUpProp(event);
    if (event.defaultPrevented) return;
    if (isDuplicate) return;
    const isSpace = clickOnSpace && event.key === " ";
    if (!activeRef.current || !isSpace) return;
    const nativeClick = isNativeClick(event);
    activeRef.current = false;
    if (!nativeClick) setActive(false);
    if (disabled) return;
    if (event.metaKey) return;
    if (nativeClick) return;
    event.preventDefault();
    const element = event.currentTarget;
    const { view, ...eventInit } = event;
    queueMicrotask(() => _7FULUV5Lcjs.fireClickEvent.call(void 0, element, eventInit));
  });
  props = {
    "data-active": active || void 0,
    type: isNativeButton ? "button" : void 0,
    ...metadataProps,
    ...props,
    ref: _7FULUV5Lcjs.useMergeRefs.call(void 0, ref, props.ref),
    onKeyDown,
    onKeyUp
  };
  props = _62Q6LBYZcjs.useFocusable.call(void 0, props);
  return props;
});
var Command = _7FULUV5Lcjs.forwardRef.call(void 0, function Command2(props) {
  return _7FULUV5Lcjs.createElement.call(void 0, TagName, useCommand(props));
});




exports.useCommand = useCommand; exports.Command = Command;
